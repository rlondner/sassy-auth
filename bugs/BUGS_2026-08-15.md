# Bug Catalog — 2026-08-15 daily code review

**Review window:** last ~24h of repository activity (2026-08-14 ~15:30 UTC → 2026-08-15 ~08:30 UTC).
**GitHub access:** ✅ `gh` CLI works (keyring `gho_…`, `repo` scope); `git fetch origin master`
succeeded. `curl api.github.com` and the `.env.local` PAT paths remain unreliable — use `gh`.

**Result: 2 new bugs (0254, 0255), both PR-hygiene / test-coverage, both Medium-or-lower.**
Highest prior real bug number was **0253** → next is **0254**.

## Scope of the window

- **`master` is still frozen at `a78d4a7`** (2026-07-15, the 2FA merge) — **31 days**, no merge, no
  direct pushes.
- **One new PR in the window: [#324](https://github.com/rlondner/sassy-auth/pull/324)** — Jules
  "🎨 Palette: Add localized ARIA labels to table row action buttons" (opened 2026-08-15 07:36 UTC).
  Admin-only: 5 table components + 1 test + `en.json`/`fr.json`. **No `apps/auth-server` / seed
  edits this time** — a cleaner PR than the #303/#306/#316 lineage.
- No other PR updated after the 2026-08-14 review (the 15:30 UTC updates on #322/#323 were that
  run's own outputs). No new commits (`git log --since` in-window = empty).

## What PR #324 changed

- All 5 admin tables (`apps`/`orgs`/`permissions`/`roles`/`users`): the `more_vert` row-action
  button's `aria-label="more actions"` → `aria-label={t('common.moreActions')}`.
- `apps/admin/messages/en.json`: adds `common.moreActions = "More actions"`.
- `apps/admin/messages/fr.json`: adds `common.moreActions = "Plus d'actions"` **and translates the
  top-level `common` block** (`save/edit/delete/confirm` English placeholders → French). That
  **fixes [bug-0244](./bug-0244.md)** (in the PR head, not on `master`).
- `apps-table.test.tsx`: one added assertion that the localized `moreActions` label renders (×2 rows).

All 5 tables use root-scope `useTranslations()` (verified), so `t('common.moreActions')` resolves
correctly — **no translation-scope defect.** The change is functionally correct.

## New bugs

| ID | Severity | Summary | Fixed |
|----|----------|---------|-------|
| [bug-0254](./bug-0254.md) | 🟠 Medium | PR #324 duplicates the still-open PR #316's `common.moreActions` aria-label localization (same 5 tables, same message keys, same `fr.json` `common` fix) → guaranteed merge conflict. #324 is the *clean* subset (no bug-0250/0251 auth/seed payload); #316 does more (copy-button localization) but is blocked. Recurring Jules duplicate-generation pattern (cf. 0245, 0248). | false |
| [bug-0255](./bug-0255.md) | 🟢 Low | #324 localizes the row-action label in 5 tables but adds a test assertion for only 1 (apps-table); the 4 sibling table suites are left unverified. | false |

## Status of prior open findings (unchanged this run — all in unmerged PRs)

- 🔴 **[bug-0250](./bug-0250.md)** (Critical — seed `twoFactorPromptedAt:new Date()` suppresses the
  2FA enrollment prompt) — **still present in #316/#315**, still the blocker on the tooltip PR.
  **Not** carried by #324.
- 🟡 **[bug-0251](./bug-0251.md)** (scope creep — #316 bundles `auth.config.ts` + seeds under a
  cosmetic title) — stands.
- 🟡 **[bug-0253](./bug-0253.md)** (native-`title` copy confirmation never shows "Copied!" under a
  stationary pointer) — stands in #316 `b3acc18`. #324 does not touch the copy button, so it
  neither fixes nor worsens this.
- 🔴 **[bug-0243](./bug-0243.md)** (`apps/admin` Jest suite red on `master`; standalone fix #287
  never merged) — carried forward; CI still not gating.

## Missing tests / risk notes

- **bug-0255**: 4 of 5 tables have no assertion on the localized row-action label.
- No test still exists that a freshly-seeded platform admin is routed to
  `/login/two-factor-prompt` (would catch the still-open **bug-0250**).
- The `apps/admin` suite remains red on `master` (**bug-0243**), so none of these admin table
  assertions actually gate CI today.

## PRs created this run

Per the task's "one PR per new bug" rule (each `bugs/bug-NNNN` branch is off `master` with only its
one `.md`):

- `bugs/bug-0254` → **[PR #325](https://github.com/rlondner/sassy-auth/pull/325)** (bug-0254.md)
- `bugs/bug-0255` → **[PR #326](https://github.com/rlondner/sassy-auth/pull/326)** (bug-0255.md)
- Review bundle (this file + CHANGELOG/TODO/CR) → `daily-review-2026-08-15` →
  **[PR #327](https://github.com/rlondner/sassy-auth/pull/327)**.
