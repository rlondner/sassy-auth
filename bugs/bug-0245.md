# bug-0245: Redundant Jules PR — PR #286 head is an empty commit and duplicates PR #285's test edit

**Severity:** Minor
**Area:** repo hygiene / automation (Jules bot)
**Commits/PRs:**
- `09391e7` — head of `origin/pr/286` (`palette-checkbox-focus-a11y-…`): **empty commit** (no file changes)
- `7c4e605` — parent of `09391e7`: the actual "focus states on checkboxes" change
- `c55d026` — head of `origin/pr/285`: "Localize useCopyFeedback"

Two problems in the last 24h of Jules-bot output:

1. **Empty PR head.** PR #286's tip commit `09391e7` has an empty diff — `git diff 09391e7^
   09391e7` is empty. The real work is in its parent `7c4e605`. An empty head commit is
   process noise, confuses "what does this PR change?" tooling, and is the same family as
   **bug-0090** (Jules duplicate PRs).

2. **Duplicate test edit → guaranteed merge conflict.** Both PR #285 (`c55d026`) and PR #286
   (`7c4e605`) add the *identical* two lines to
   `apps/admin/components/__tests__/app-create-drawer.test.tsx`:
   ```
   +        requireTwoFactor: false,
   +        twoFactorTrustDays: null,
   ```
   Each PR independently patched the stale assertion (the underlying defect is **bug-0243**).
   Whichever merges second will conflict on that file, and the second PR's own "fix" becomes
   redundant.

**Impact:** Wasted review cycles, a misleading empty commit, and a merge conflict between the
two open palette PRs. Root cause of the duplicated edit is that both PRs are compensating for
the already-broken test (bug-0243) rather than the test being fixed once on `master`.

**Fix:** (a) Fix bug-0243 on `master` first so neither PR needs the test edit; (b) rebase/drop
the empty `09391e7` from PR #286; (c) revisit the Jules PR-generation flow (bug-0090) to stop
emitting empty head commits and overlapping edits.

**Fixed:** false
