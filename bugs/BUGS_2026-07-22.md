# Bug Catalog — 2026-07-22 daily code review

**Review window:** last 24h of repository activity (2026-07-21 ~08:00 → 2026-07-22 ~08:00 UTC).

**Result: 0 new bugs.**

## Why the window is empty

`master` has **not advanced since `a78d4a7` (2026-07-15)**. Every commit inside the
review window is one of two things, and neither introduces new, un-reviewed source code:

1. **The previous run's own output** — the 2026-07-21 daily review filed bugs 0243–0245 and
   a CHANGELOG/README/TODO bundle on branches that are still **unmerged** (PRs #287, #288,
   #289, #290). These are documentation commits, not code under review.
2. **Two Jules-bot "Palette" commits** — `7c4e605` and `09391e7`
   ("🎨 Palette: Add standardized focus states to native checkboxes", PR #286). These were
   **already reviewed in `BUGS_2026-07-21.md`** and are the subject of **bug-0245** (PR #286's
   tip is an empty commit duplicating PR #285's test edit). No re-filing.

## Numbering note

Highest existing bug number is **0245** (`BUGS_2026-07-21.md`, in unmerged PR #290). No new
`bug-NNNN` files are created today, so the next number remains **0246**.

## Verification performed this run

- **bug-0243 (Warning) confirmed still open on `master`.** `apps/admin/components/app-create-drawer.tsx:55-61`
  submits five fields (`name`, `url`, `callbackUrl`, `twoFactorTrustDays`, `requireTwoFactor`),
  but `apps/admin/components/__tests__/app-create-drawer.test.tsx:33-37` still asserts an exact
  `toHaveBeenCalledWith({ name, url, callbackUrl: null })`. The admin Jest suite therefore fails
  on `master`. The fix lives only in unmerged PRs; **master is red**. Escalated in
  `todo/TODO_2026-07-22.md`.

## No PRs created

The task's "create a PR per new bug entry" step is conditional on new bug entries. With **0 new
bugs**, no bug branches or bug PRs were opened. Fabricating bugs to manufacture PRs would be
dishonest and is deliberately avoided.
