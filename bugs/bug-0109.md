# bug-0109: playwright.config.ts workers ternary is dead branch

**Severity:** Minor
**File:** `apps/admin-e2e/playwright.config.ts` (line 15)

`workers: CI_TESTS ? 1 : 1` — both branches evaluate to `1`. The local value should be `2` per the comment, or the ternary should be removed.

**Fix:** Change to `workers: CI_TESTS ? 1 : 2` or remove the ternary.
