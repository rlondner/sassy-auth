# Bug Catalog

Bugs surfaced by the **2026-06-24 daily code review**. No new commits in the last 24 hours (6th consecutive day with no commits). Working tree is unchanged from the 2026-06-22 review. Exhaustive re-scan found no new issues. Numbering continues from `BUGS_2026-06-23.md` (last: bug-0115).

Severity legend:
- :red_circle: **Critical** -- blocks shipping; tenant isolation, credential leakage, or auth bypass.
- :yellow_circle: **Warning** -- fix soon; correctness or privacy gap.
- :large_blue_circle: **Minor** -- small correctness / lint hygiene.
- :white_circle: **Info** -- advisory, not actionable mechanically.

---

## No new bugs found

The uncommitted working tree was re-scanned exhaustively against the following checklist:

1. **`resolveIssuer()` / discovery / token service** — No new issues beyond bug-0115 (URL validation). The `NEST_GLOBAL_PREFIX` export and `ISSUER_PLACEHOLDER` constant DRY refactor are correct. Tests cover trailing-slash stripping, placeholder fallback, and issuer agreement between discovery and JWT. Bug-0115's missing validation (empty string, whitespace, non-URL, wrong protocol) is already catalogued.

2. **Toast / onSuccess / refresh integration** — Sonner is correctly mounted inside `ThemeProvider`/`NextIntlClientProvider`. The `Toaster` component forwards `next-themes` resolved theme. All 15 toast calls use i18n keys. `onSuccess` callbacks are correctly wired in all 10 drawer/table pairs. The `refresh()` pattern is consistent: 4 tables use `useCallback` wrapping a list server action; `users-table` uses `router.refresh()`. Bug-0114 (unhandled refresh errors) remains the only open issue in this area.

3. **Roles permission expansion** (`platform.users.manage` added to list/get) — Tests updated consistently in `roles.service.spec.ts` and `permissions-matrix.ts`. Write operations (`create`/`update`/`delete`) remain gated on `platform.roles.manage` only — no over-broadening. The expansion is a deliberate design choice to let user admins populate the role picker.

4. **E2E `raceSuccessOrError` scoping** — All 5 page objects were updated identically. The `errorScope` locator correctly targets `[data-sonner-toaster]`, `[role="dialog"]`, and `[role="alertdialog"]`, avoiding the Next.js Dev Tools phantom alert. The null-null timeout case (bug-0108) remains open but is pre-existing.

5. **OAuth E2E route mock** — The predicate `(url) => url.origin === redirectUriOrigin && url.pathname === '/cb'` correctly narrows the stub to the exact callback path, preventing interception of the authorize endpoint when origins overlap.

6. **French i18n** — All 15 toast strings in `fr.json` remain untranslated English copies (bug-0112).

7. **Playwright config** — `workers: CI_TESTS ? 1 : 1` dead ternary persists (bug-0113).

---

## Summary

| New bugs | 0 |
|----------|---|
| Total open | **113** (unchanged from 2026-06-23) |
| Fixed since last review | 0 |
| Days without a fix merged | **28** |

## Previous bugs status update

### No change from 2026-06-23

All 113 prior bugs (bug-0001 through bug-0115, excluding bug-0027/bug-0078 which were fixed earlier) remain open. The uncommitted working tree is unchanged from the 2026-06-22 review. No bugs were fixed, merged, or addressed in the last 24 hours.

### Critical bugs still open

| Bug | Days open | Summary |
|-----|-----------|---------|
| bug-0001 | 28 | RBAC not org-scoped — `checkPermission` doesn't constrain by orgId |
| bug-0038 | 22 | JWT `permissions` → `scope` breaking change, no migration path |
| bug-0039 | 22 | In-memory OAuth authorization code store; lost on restart |
| bug-0054 | 19 | `redirect_uri` not validated against app's registered URL |
| bug-0074 | 18 | Inactive users can authenticate and receive JWTs |
| bug-0094 | 6 | `checkPermissionForApp` silently grants when `targetAppId` is undefined |

PR convention: `fix/bug-NNNN-<short-name>` branch, `fix(bug-NNNN): <summary>` PR title.
