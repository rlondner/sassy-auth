# Bug Catalog — 2026-08-17 daily code review

**Review window:** activity since the 2026-08-15 review (2026-08-15 ~08:30 UTC → 2026-08-17 ~08:00 UTC).
**GitHub access:** ✅ `git fetch origin master` and `gh` CLI (keyring `gho_…`, `repo` scope) both
work this run. `curl api.github.com` / `.env.local` PAT paths remain unreliable — use `gh`.

**Result: 3 new bugs (0256 Medium, 0257 Medium, 0258 Low), all pre-merge hygiene / scope-creep /
test-quality.** Highest prior real bug number was **0255** → next is **0256**.

## Scope of the window

- **`master` is still frozen at `a78d4a7`** (2026-07-15, the 2FA merge) — **33 days**, no merge, no
  direct pushes. `git log --since="24 hours ago"` on `master` = empty.
- **Two new PRs in the window**, both Jules bot, both admin-UI titled but both touching
  `apps/auth-server`:
  - **[#328](https://github.com/rlondner/sassy-auth/pull/328)** — "🎨 Palette: Add localized ARIA
    labels and tooltips to row action triggers" (2026-08-16 07:40 UTC), 17 files, `+121/−50`.
  - **[#329](https://github.com/rlondner/sassy-auth/pull/329)** — "🎨 Palette: Standardize
    ShareLinkDialog copy feedback hook" (2026-08-17 07:09 UTC), 4 files, `+80/−5`.
- PRs #325/#326/#327 updated at 2026-08-16 02:06 UTC were the **2026-08-15 review's own outputs**
  being pushed — not new activity.

## What the two PRs changed

### PR #329 — ShareLinkDialog copy-feedback refactor (mostly good)

- Rewrites `share-link-dialog.tsx` to use the existing **`useCopyFeedback`** hook
  (`apps/admin/lib/use-copy-feedback.ts`, **present on `master`** — verified) instead of inline
  `useState` + `setTimeout`. This removes the unmount `setTimeout` leak and is a genuine, correct
  cleanup. ✅
- Adds a real `share-link-dialog.test.tsx` (renders title/description/read-only input; asserts copy
  → "Copied!" feedback). ✅ First dedicated test for this dialog.
- Updates `app-create-drawer.test.tsx` to expect `requireTwoFactor: false, twoFactorTrustDays: null`
  in the `createAppAction` payload — this is the [bug-0243](./bug-0243.md) fix (in the PR head).
- **BUT** also adds `autoSignIn: false` to `apps/auth-server/src/auth/auth.config.ts` — unrelated to
  copy feedback (see bug-0256).

### PR #328 — row-action tooltips (duplicate + heavily over-scoped)

- Re-does the same `aria-label={t('common.moreActions')}` + `en/fr.json` localization already in the
  open PRs #316 and #324 (superset of #324, overlaps #316) → three-way conflict (bug-0257).
- Wraps each row-action trigger in `<Tooltip>`; globally stubs the UI Tooltip primitives in
  `jest.setup.ts` (bug-0258).
- Also fixes bug-0243's `app-create-drawer.test.tsx` assertion.
- Rewrites `apps/admin/app/login/actions.ts` to thread the fresh session token into the
  `get-session` / `two-factor-status` fetches (robustness improvement, **no test**), adds e2e
  2FA-prompt skip handling in `auth-state.setup.ts`, and adds `autoSignIn: false` to
  `auth.config.ts` (bug-0256).

## New bugs

| ID | Severity | Summary | Fixed |
|----|----------|---------|-------|
| [bug-0256](./bug-0256.md) | 🟠 Medium | `autoSignIn: false` is added to `auth.config.ts` by **three** open PRs (#316, #328, #329) — an unstated, untested auth-server behavior change smuggled under cosmetic titles → guaranteed 3-way merge conflict on one line. Continuation of the auth-server-smuggling lineage (bug-0246/0247/0251). | false |
| [bug-0257](./bug-0257.md) | 🟠 Medium | PR #328 is a **third** overlapping row-action-tooltip/`common.moreActions` localization PR (dup of #316 and #324; extends bug-0254 to three-way) and re-bundles an untested `login/actions.ts` rewrite + `auth.config.ts` + e2e-setup edits under a "row action tooltips" title. | false |
| [bug-0258](./bug-0258.md) | 🟢 Low | PR #328 globally stubs the `@sassy-auth/ui` Tooltip primitives in `jest.setup.ts`, blinding the whole admin Jest suite to real tooltip behavior (provider requirement, `asChild` composition) — escalates bug-0251, masks bug-0252. | false |

## Not filed (verified, deliberately)

- **PR #329's `useCopyFeedback` refactor** — the hook exists on `master`
  (`git cat-file -e master:apps/admin/lib/use-copy-feedback.ts` → present), the refactor is correct
  and well-tested. No bug.
- **PR #328's `login/actions.ts` cookie rewrite is NOT evidence of a `master` defect.** On `master`,
  `forwardSessionCookie` sets the session cookie on the Next.js cookie store and the subsequent
  `cookies().toString()` call **does** reflect that just-set value within the same request, so the
  2FA-status lookup is not actually reading a stale cookie. #328's change is a legitimate
  explicit-threading refactor, but there is no shipped bug to file for it — only the missing test /
  scope-creep angle (folded into bug-0257). *(Consistent with the false-positive discipline that
  retracted bug-0249.)*

## Status of prior open findings (unchanged this run — all in unmerged PRs)

- 🔴 **[bug-0250](./bug-0250.md)** (Critical — seed `twoFactorPromptedAt:new Date()` suppresses the
  2FA enrollment prompt) — **still in #316/#315**; **not** carried by #328/#329. Notably, #328's
  e2e `auth-state.setup.ts` now *skips* a 2FA prompt, implying seeded admins hit it (the correct
  direction; opposite to the #316 seed suppression).
- 🟡 **[bug-0251](./bug-0251.md)** (#316 bundles `auth.config.ts` + seeds under a cosmetic title) —
  stands; bug-0256/0257 are the same pattern recurring in #328/#329.
- 🟡 **[bug-0253](./bug-0253.md)** (native-`title` copy confirmation never shows "Copied!" under a
  stationary pointer, #316 `b3acc18`) — stands. Note #329 moves `ShareLinkDialog` to a reactive
  `copied` state via `useCopyFeedback`, which is the *correct* pattern bug-0253 asked for on the
  table copy button (but #329 only fixes the dialog, not the tables).
- 🟠 **[bug-0254](./bug-0254.md)** / 🟢 **[bug-0255](./bug-0255.md)** (the #324↔#316 duplication and
  its test-coverage gap) — stand; #328 turns the duplication three-way (bug-0257).
- 🔴 **[bug-0243](./bug-0243.md)** (`apps/admin` Jest suite red on `master`; standalone fix #287
  never merged) — carried forward. Both #328 and #329 include the assertion fix, but on `master` it
  is still red, so CI still isn't gating the admin table assertions.

## Missing tests / risk notes

- **bug-0257**: #328's `login/actions.ts` cookie-forwarding rewrite ships with no test.
- **bug-0258**: the added row-action-tooltip assertions run against a stub, not the real Radix
  primitive; the double-`asChild` concern (bug-0252) remains unverifiable by the suite.
- No test still exists that a freshly-seeded platform admin is routed to
  `/login/two-factor-prompt` (would catch the still-open **bug-0250**). #328's e2e skip-handler
  actually depends on that routing happening, but doesn't assert it.
- The `apps/admin` suite remains red on `master` (**bug-0243**), so none of the admin assertions in
  #328/#329 gate CI today.

## PRs created this run

Per the task's "one PR per new bug" rule (each `bugs/bug-NNNN` branch off `master` with only its
one `.md`):

- `bugs/bug-0256` → **PR** (bug-0256.md)
- `bugs/bug-0257` → **PR** (bug-0257.md)
- `bugs/bug-0258` → **PR** (bug-0258.md)
- Review bundle (this file + CHANGELOG/TODO/CR/README) → `daily-review-2026-08-17` → **PR**.

*(PR numbers are recorded in [CHANGELOG.md](../CHANGELOG.md) and
[project memory](../../.claude) after creation.)*
