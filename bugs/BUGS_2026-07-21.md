# Bug Catalog

Bugs surfaced by the **2026-07-21 daily code review**. The review window covers the last 24h
of repository activity: three Jules-bot "Palette" commits (`c55d026`, `7c4e605`, `09391e7`)
landed on unmerged PR branches (#285, #286). `master` itself has not advanced since
`a78d4a7` (2026-07-15). 3 new bugs found (0 critical, 1 warning, 2 minor).

**Numbering note:** The previous catalog (`BUGS_2026-07-09.md`) reached `bug-0235`; a
subsequent manual session filed `bug-0236`–`bug-0242` (2026-07-20). Highest existing number
is **0242**, so this catalog starts at **0243**.

Severity legend:
- :red_circle: **Critical** — blocks shipping; tenant isolation, credential leakage, or auth bypass.
- :yellow_circle: **Warning** — fix soon; correctness or CI-health gap.
- :large_blue_circle: **Minor** — small correctness / i18n / hygiene.
- :white_circle: **Info** — advisory, not actionable mechanically.

---

## New bugs (3)

### bug-0243 :yellow_circle: Warning — `app-create-drawer` unit test stale vs 2FA create payload (fails on `master`)

**Files:** `apps/admin/components/__tests__/app-create-drawer.test.tsx:33-39`, `apps/admin/components/app-create-drawer.tsx:57-61`

The create drawer submits `twoFactorTrustDays` and `requireTwoFactor` (added with per-app 2FA
"2b", merged `a78d4a7`) but the "creates app" test still asserts `toHaveBeenCalledWith({name,
url, callbackUrl})` — an exact match — so the test fails on `master`. Two independent Jules
PRs (#285, #286) each had to patch it (see bug-0245), confirming `master` merged the 2b
feature with this suite red. Points to a missing/skipped CI gate for the `apps/admin` Jest
suites.

**Fix:** Add `twoFactorTrustDays: null` and `requireTwoFactor: false` to the assertion; ensure
the admin Jest suite gates merges to `master`.

**Fixed:** false

See [bug-0243.md](./bug-0243.md).

---

### bug-0244 :large_blue_circle: Minor — French `common.*` action labels are untranslated English

**Files:** `apps/admin/messages/fr.json:15-19`

`common.save`/`edit`/`delete`/`confirm` render English (`"Save"`, `"Edit"`, `"Delete"`,
`"Confirm"`) in the French locale — the generic Save/Edit/Delete/Confirm button labels reused
across dialogs and drawers. Same class as bug-0112 / bug-0239 but the `common` subtree. The
2026-07-20 `useCopyFeedback` commit (`c55d026`) added a correctly-translated `common.copyError`
right beside them yet left these four English.

**Fix:** Translate the four `common.*` values; add a byte-identical-value i18n lint (see
bug-0239) extended to `common.*`.

**Fixed:** false

See [bug-0244.md](./bug-0244.md).

---

### bug-0245 :large_blue_circle: Minor — Redundant Jules PR: PR #286 head is empty and duplicates PR #285's test edit

**Commits:** `09391e7` (empty head of PR #286), `7c4e605` (real change), `c55d026` (PR #285)

PR #286's tip `09391e7` is an empty commit; the real work is its parent `7c4e605`. Both #285
and #286 add the *identical* `requireTwoFactor: false` / `twoFactorTrustDays: null` lines to
`app-create-drawer.test.tsx` (each compensating for bug-0243), so the second to merge will
conflict. Same automation family as bug-0090 (Jules duplicate PRs).

**Fix:** Fix bug-0243 on `master` so neither PR needs the edit; drop the empty `09391e7`;
revisit the Jules PR-generation flow.

**Fixed:** false

See [bug-0245.md](./bug-0245.md).
