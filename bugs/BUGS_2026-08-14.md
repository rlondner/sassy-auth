# Bug Catalog — 2026-08-14 daily code review

**Review window:** last ~24h of repository activity (to 2026-08-14 ~15:23 UTC).
**GitHub access:** ✅ this run `git fetch origin` **succeeded** (refreshed `pr/316…321` +
Jules branches) **and** the `gh` CLI works (keyring `gho_…` token). `curl api.github.com`
remains proxy-blocked; the `.env.local` PAT is still rejected for authenticated pushes — but a
plain fetch over the keyring credential worked this time. See `todo/TODO_2026-08-14.md`.

**Result: 1 confirmed new bug (0253).** Highest prior bug number was **0252** → next is **0253**.

## Scope of the window

- **Strict 24h window (since 2026-08-13 15:23 UTC): _no_ new commits or PR updates.** `master` is
  still frozen at `a78d4a7` (2026-07-15, the 2FA merge) — **30 days** with no merge. No direct
  pushes to `master`.
- **One change unreviewed by the prior run:** PR **#316** was force-pushed to head **`b3acc18`**
  (2026-08-13 10:14 UTC) — **9 minutes after** the 2026-08-13 review pulled its data (head
  `42a518d`, 09:07 UTC). That revision was therefore never reviewed. It is reviewed here.
- **No new PRs** beyond those the 2026-08-13 run already created (#318–#321). #317 stays closed.

## What `b3acc18` changed (the unreviewed PR #316 revision)

It **replaces the Radix `<Tooltip>` with a native `title=` attribute** on the copy button and the
`more_vert` row-action button across all 5 admin tables, and removes the now-unused
`Tooltip/TooltipTrigger/TooltipContent` imports and the corresponding test mocks. Diff scope:
the 5 table components + their 5 test files only (`+98 / −142`). **The seed and `auth.config.ts`
edits were not touched.**

Effect on the 2026-08-13 findings:

| Prior bug | Status after `b3acc18` |
|-----------|------------------------|
| [bug-0249](./bug-0249.md) (retracted "missing TooltipProvider") | Permanently moot — no Radix Tooltip remains. |
| [bug-0250](./bug-0250.md) 🔴 **Critical** (seed `twoFactorPromptedAt: new Date()` suppresses the 2FA prompt) | **Persists unchanged** — `seed.ts:120`, `demo-multitenant.ts:103`, `demo-resource-server.ts:163`; `autoSignIn:false` at `auth.config.ts:146`. Still the blocker on #316. |
| [bug-0251](./bug-0251.md) 🟡 (scope creep + mocks-away-its-primitive) | **Partly resolved:** the Tooltip primitive and its passthrough test mocks are gone, so the "coverage of a stub" half no longer applies. **The scope-creep half stands** — the PR still silently bundles the `auth.config.ts` + 3 seed edits under a cosmetic title. |
| [bug-0252](./bug-0252.md) 🟡 (nested double-`asChild`) | **Resolved by revision** — the nested `asChild` composition is gone (native button, no Radix Slot). Fix lives in the PR head, **not on `master`**; `bug-0252.md` left `Fixed: false` because it never merged. |

## New bug

| ID | Severity | Summary | Fixed |
|----|----------|---------|-------|
| [bug-0253](./bug-0253.md) | 🟡 Minor (a11y / UX) | The native-`title` rewrite (`b3acc18`) binds the copy button's `title`/`aria-label` to the transient `copied` state, but a native `title` won't update while the pointer stays on the button (browsers sample it at hover-start) and mutating `aria-label` doesn't re-announce to SR. So PR #316's own goal — a "dynamic copied announcement" — no longer works; only the icon swap survives. | false |

## Verification performed this run

- **`b3acc18` diff read in full** (`git diff 42a518d..b3acc18`): Radix Tooltip → native `title`
  in all 5 tables; test mocks for `Tooltip*` removed.
- **bug-0250 persistence confirmed** — `git grep twoFactorPromptedAt b3acc18 -- apps/auth-server`
  still shows the three seed writes and `autoSignIn:false`; `git diff --stat master b3acc18 --
  apps/auth-server` = 4 files, +4 (unchanged from the reviewed head).
- **bug-0253 grounded** — copy button's `title`/`aria-label` bound to reactive `copied` from
  `useCopyFeedback()` (`apps/admin/lib/use-copy-feedback.ts`, reactive `useState`); native-`title`
  non-update-under-stationary-pointer is standard browser behavior (not runtime-executed here).

## Missing tests / risk notes

- No test asserts the copy **confirmation** is conveyed by anything other than a mutated `title`
  (would have caught bug-0253). After the rewrite the tables have **no** tooltip/aria assertion at
  all — the removed mocks were the only thing referencing the primitive.
- Still no test that a freshly-seeded platform admin is routed to `/login/two-factor-prompt`
  (would catch the still-open bug-0250).
- `master`'s `apps/admin` Jest suite has been red since `a78d4a7` (bug-0243) and CI is not gating
  on it — carried forward, unchanged.

## PRs created this run

Per the task's "one PR per new bug" rule:

- `bugs/bug-0253` → **PR #322** (bug-0253.md) — _see CR for the live link._

Detail in [../code_reviews/CR_2026-08-14.md](../code_reviews/CR_2026-08-14.md) and
[../CHANGELOG.md](../CHANGELOG.md).
