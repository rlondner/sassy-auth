# Bug Catalog

Bugs surfaced by the **2026-06-26 daily code review**. No new commits in the last 24 hours (8th consecutive day with no commits). Working tree is unchanged from the 2026-06-25 review. Exhaustive re-scan found no new issues. Numbering continues from `BUGS_2026-06-25.md` (last: bug-0116).

Severity legend:
- :red_circle: **Critical** -- blocks shipping; tenant isolation, credential leakage, or auth bypass.
- :yellow_circle: **Warning** -- fix soon; correctness or privacy gap.
- :large_blue_circle: **Minor** -- small correctness / lint hygiene.
- :white_circle: **Info** -- advisory, not actionable mechanically.

---

## New bugs (0)

No new bugs found. All uncommitted changes have been reviewed across auth-server (token/discovery, roles service, configure-nest-app), admin UI (15 drawer/table CRUD surfaces, Toaster integration, layout), and E2E tests (5 page objects, OAuth flow spec, Playwright config).

---

## Summary

| New bugs | 0 |
|----------|---|
| Total open | **114** (unchanged from 2026-06-25) |
| Fixed since last review | 0 |
| Days without a fix merged | **30** |

## Scan details

The uncommitted working tree was re-scanned across three areas (auth-server, admin UI, E2E tests).

1. **Auth-server token/discovery** -- `resolveIssuer()` DRY refactor, `NEST_GLOBAL_PREFIX` export, `DiscoveryController` constructor warning, `TokenService` shared issuer. All correct. bug-0115 (missing URL validation) remains open.

2. **Auth-server roles service** -- `platform.users.manage` added to `listRoles`/`getRole` permission gates (read-only widening for admin UI role picker). `createRole`/`updateRole`/`deleteRole` still require `platform.roles.manage` only. Tests updated consistently. Permissions matrix updated. No new bugs.

3. **Admin UI toast/refresh integration** -- All 15 drawer/table CRUD surfaces follow the toast/onSuccess/refresh pattern. bug-0116 (view drawer not closed on user delete from row dropdown) remains open. bug-0112 (French translations), bug-0114 (unhandled refresh errors) remain open.

4. **E2E page objects** -- `raceSuccessOrError` scoping fix applied identically across all 5 page objects. OAuth route stub correctly narrowed. bug-0113 (dead ternary) remains open.

## Previous bugs status update

### No change from 2026-06-25

All 114 prior bugs (bug-0001 through bug-0116, excluding bug-0027/bug-0078 which were fixed earlier) remain open. No bugs were fixed, merged, or addressed in the last 24 hours.

### Critical bugs still open

| Bug | Days open | Summary |
|-----|-----------|---------|
| bug-0001 | 30 | RBAC not org-scoped -- `checkPermission` doesn't constrain by orgId |
| bug-0038 | 23 | JWT `permissions` -> `scope` breaking change, no migration path |
| bug-0039 | 23 | In-memory OAuth authorization code store; lost on restart |
| bug-0054 | 20 | `redirect_uri` not validated against app's registered URL |
| bug-0074 | 19 | Inactive users can authenticate and receive JWTs |
| bug-0094 | 7 | `checkPermissionForApp` silently grants when `targetAppId` is undefined |

**Fixed:** false

PR convention: `fix/bug-NNNN-<short-name>` branch, `fix(bug-NNNN): <summary>` PR title.
