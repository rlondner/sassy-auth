# bug-0145: View drawers lack fetch cancellation guard

**Files:** `permission-view-drawer.tsx:32-39`, `role-view-drawer.tsx:28-35`

**Problem:** No `cancelled` flag in fetch effects. Rapid drawer switching can show stale entity data when the old response arrives after the new one.

**Fix:** Add a `cancelled` flag, matching the pattern in `role-create-drawer.tsx`.
