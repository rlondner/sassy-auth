# bug-0142: UserViewDrawer edit-mode useEffect missing deps

**File:** `apps/admin/components/user-view-drawer.tsx:97-121`

**Problem:** Edit-mode useEffect reads `roles` and `directPermissions` but they're missing from the dependency array `[editing, user?.id, appId]`. Stale closure captures empty arrays if data loads after Edit is clicked.

**Fix:** Add `roles` and `directPermissions` to the dependency array.
