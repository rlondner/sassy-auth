# bug-0144: SaInvitation missing expiresAt index

**File:** `packages/db/prisma/schema.prisma` (SaInvitation model)

**Problem:** No index on `expiresAt`. Expiry-based queries (resendInvitation, future cleanup) do full table scans.

**Fix:** Add `@@index([expiresAt])` to the SaInvitation model and generate a migration.
