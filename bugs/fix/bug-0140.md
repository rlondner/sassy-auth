# bug-0140: listUsers returns unbounded results (no pagination)

**File:** `apps/auth-server/src/users/users.service.ts:79`

**Problem:** `listUsers` has no `take`/`skip` — returns ALL matching rows. Every other list endpoint has pagination with default `pageSize: 25`.

**Fix:** Add `page` and `pageSize` parameters matching the pattern in `listOrgs`/`listRoles`.
