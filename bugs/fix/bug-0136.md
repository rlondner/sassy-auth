# bug-0136: apiFetch doesn't handle HTTP 401

**File:** `apps/admin/lib/api.ts:18`

**Problem:** `apiFetch` throws generic `Error("API error 401")` on session expiry instead of redirecting to `/login`.

**Fix:** Add `if (res.status === 401) redirect('/login')` before the generic error throw.
