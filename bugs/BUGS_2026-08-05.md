# Bug Catalog — 2026-08-05 daily code review

**Review window:** last 24h of repository activity (2026-08-04 ~08:00 → 2026-08-05 ~08:00 UTC).
**GitHub access:** ✅ restored this run via the `github` MCP connector (previous runs 2026-07-27 →
2026-08-04 were blocked by proxy/PAT — see those CR docs). PR list and PR #306 diff were pulled
live from `origin`.

**Result: 2 new bugs (0247, 0248).** Highest prior bug number was **0246**; next is **0249**.

## New activity reviewed

`master` is still frozen at `a78d4a7` (2026-07-15). The only genuinely new item in the window is
**PR #306** (`jules-...-28989156`, Jules bot — "🎨 Palette: Interactive Show/Hide Password Toggle
for LoginForm"), created 2026-08-05 07:49 UTC. Its full diff was reviewed.

The password-toggle feature itself is fine (correct `type="button"`, aria-labels, `pr-10`
padding, and it ships a unit test). The problems are what the PR **bundles around** it.

## New bugs

| ID | Severity | Summary | Fixed |
|----|----------|---------|-------|
| [bug-0247](./bug-0247.md) | Warning (security) | PR #306 adds a second, divergent **unfenced session-gate bypass** (`process.env.SEEDING === '1'`) to `auth.config.ts` — reintroduces bug-0246 under a renamed var, no `NODE_ENV` fence, no test, no docs. Two open PRs (#303 `SEED_RUNNING`, #306 `SEEDING`) now carry incompatible bypasses of the same hook. | false |
| [bug-0248](./bug-0248.md) | Minor (process) | Three open PRs (#294, #296, #306) implement the **same** login password toggle → guaranteed conflicts + wasted review. PR #306 further exhibits **scope creep**: it silently bundles the bug-0243 test fix and an unrelated `fr.json` reformat alongside the auth bypass under a UI title. | false |

## Verification performed this run

- **bug-0247 confirmed new.** On `master` (`a78d4a7`) `auth.config.ts` `create.before` calls
  `evaluateSessionGate` unconditionally — no `SEEDING`/`SEED_RUNNING` anywhere in the tree. The
  bypass exists only in PR #306's diff (and, under a different name, PR #303 / bug-0246).
- **bug-0248 confirmed.** Live `list_pull_requests` shows #294 (2026-07-24), #296 (2026-07-27),
  #306 (2026-08-05) all open, all based on `a78d4a7`, all editing `login-form.tsx`.
- **bug-0243 remains open on `master`.** PR #306 carries a fix for it
  (`app-create-drawer.test.tsx` now asserts the 5-field payload) but only as a bundled,
  hard-to-discover side change; the standalone fix (PR #287) is still unmerged. `master` stays
  red until one of them lands.

## Missing tests / risk notes

- **bug-0247**: no regression test asserting the session gate still runs under
  `NODE_ENV=production` when the seed flag is set. This is the test that would have caught both
  the #303 and #306 variants.
- The admin Jest suite has now been independently patched by **three** separate Jules PRs
  (#285/#286 → bug-0245, and now #306) because bug-0243's real fix never merged. CI is clearly
  not gating on this suite.

## PRs created this run

Per the task's "one PR per new bug" rule, two bug PRs were opened off `master` via the GitHub
connector:

- `bugs/bug-0247` → **PR #307** (bug-0247.md)
- `bugs/bug-0248` → **PR #308** (bug-0248.md)

Detail in [../code_reviews/CR_2026-08-05.md](../code_reviews/CR_2026-08-05.md) and
[CHANGELOG.md](../CHANGELOG.md).
