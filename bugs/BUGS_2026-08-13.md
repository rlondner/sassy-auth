# Bug Catalog — 2026-08-13 daily code review

**Review window:** last ~24h of repository activity (PR #316 updated 2026-08-13 09:07 UTC).
**GitHub access:** ✅ available this run via the `gh` CLI (keyring token — **not** the rejected
`.env.local` PAT). `gh pr list` / `gh pr view` / `gh pr diff` pulled PR metadata and diffs live
from `origin`. `curl api.github.com` remains proxy-blocked; a raw `git fetch` still fails on the
`.env.local` PAT — see the infra note in `todo/TODO_2026-08-13.md`.

**Result: 4 new bugs (0249–0252).** Highest prior bug number was **0248**; next is **0253**.

## New activity reviewed

`master` is still frozen at `a78d4a7` (2026-07-15 — the 2FA merge; **29 days** with no merge).
The only new activity in the window is **PR #316** (`palette/accessible-table-tooltips-…`,
Jules bot — "🎨 Palette: Standardized Table Copy and Row Action Accessible Tooltips"),
created 2026-08-13 08:09 UTC, 18 files, +163 / −79. Its full diff was reviewed.

The tooltip/a11y intent is reasonable (localized `aria-label`s, dynamic "copied" announcement),
but the PR ships a **confirmed runtime crash**, a **Critical seed-security regression**, and
repeats the UI-PR-scope-creep pattern already documented in bug-0247/0248.

## New bugs

| ID | Severity | Summary | Fixed |
|----|----------|---------|-------|
| [bug-0249](./bug-0249.md) | 🔴 Critical (runtime crash) | PR #316 renders Radix `<Tooltip>` in all 5 admin tables with **no `TooltipProvider`** anywhere in `apps/admin`. Radix `Tooltip.Root` (`@radix-ui/react-tooltip@1.2.8`, provider context created with no default) **throws** `` `Tooltip` must be used within `TooltipProvider` `` at render → Apps/Orgs/Roles/Permissions/Users pages crash. Tests mock the primitive to passthroughs, so they stay green. Absent on `master`. | false |
| [bug-0250](./bug-0250.md) | 🔴 Critical (security posture) | Seed scripts (`seed.ts`, `demo-multitenant.ts`, `demo-resource-server.ts`) set `twoFactorPromptedAt: new Date()`, so `shouldPromptTwoFactor` returns `false` and the 2FA enrollment prompt is suppressed for every seeded user (platform admins + demo) for `TWO_FACTOR_TRUST_DAYS` (14). Not cleared by re-seed. Present in #316 **and** #315; absent on `master`. | false |
| [bug-0251](./bug-0251.md) | 🟡 Warning (process) | Scope creep: a cosmetically-titled "table tooltips" PR silently bundles `auth.config.ts` (`autoSignIn: false`) + three seed-file changes (bug-0250), and mocks away the exact `@sassy-auth/ui` primitive it introduces so CI can't catch bug-0249. Recurrence of bug-0247/0248 + bug-0093. Overlaps/conflicts with open PRs #315 and #314. | false |
| [bug-0252](./bug-0252.md) | 🟡 Minor (a11y correctness) | Nested double-`asChild` (`TooltipTrigger asChild` → `DropdownMenuTrigger asChild` → one `<button>`) on the row-action trigger across all 5 tables — fragile Radix Slot prop/ref merge; tooltip may not dismiss when the menu opens. Secondary to bug-0249 (blocked until a provider is mounted). | false |

## Verification performed this run

- **bug-0249 confirmed (Critical).** Read the installed Radix source:
  `createTooltipContext(PROVIDER_NAME)` (no default) + react-context consumer
  `throw new Error(... must be used within ...)`. Confirmed `grep -rn TooltipProvider apps/admin`
  → 0 matches and PR #316 adds none; #316 is the first `<Tooltip>` usage in `apps/admin`.
- **bug-0250 confirmed (Critical).** `shouldPromptTwoFactor` returns `true` only when
  `promptedAt` is null; seeding a recent timestamp flips it to `false`. `git grep` confirms the
  seed writes are **absent on `master`** and present on #316/#315.
- **bug-0251 confirmed.** PR #316 diff touches `apps/auth-server/**` (auth.config + 3 seeds)
  with no mention in title/body; test diffs add `Tooltip: Passthrough` mocks.
- **bug-0252** static-confirmed from the diff (identical in all 5 tables); behavioral repro is
  gated on fixing bug-0249 first.

## Missing tests / risk notes

- The five table test suites **mock the Tooltip primitive to passthroughs**, so neither the
  missing-provider crash (bug-0249) nor the nested-`asChild` composition (bug-0252) is covered.
  A single integration test rendering one real table inside a real `<TooltipProvider>` would have
  caught bug-0249 before merge.
- No test asserts a freshly-seeded platform admin is still routed to `/login/two-factor-prompt`
  (would have caught bug-0250).

## PRs created this run

Per the task's "one PR per new bug" rule, four bug-report PRs were opened off `master`:

- `bugs/bug-0249` → **PR [#317](https://github.com/rlondner/sassy-auth/pull/317)** (bug-0249.md)
- `bugs/bug-0250` → **PR [#318](https://github.com/rlondner/sassy-auth/pull/318)** (bug-0250.md)
- `bugs/bug-0251` → **PR [#319](https://github.com/rlondner/sassy-auth/pull/319)** (bug-0251.md)
- `bugs/bug-0252` → **PR [#320](https://github.com/rlondner/sassy-auth/pull/320)** (bug-0252.md)

Detail in [../code_reviews/CR_2026-08-13.md](../code_reviews/CR_2026-08-13.md) and
[CHANGELOG.md](../CHANGELOG.md).
