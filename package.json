{
  "name": "sassy-auth",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "setup:resource-server-fastapi": "uv sync --directory apps/resource-server-fastapi",
    "dev:resource-server-fastapi": "uv run --directory apps/resource-server-fastapi uvicorn app.main:app --port 8010 --reload"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
