import { bootApp, closeApp } from './harness';

describe('roles matrix (placeholder)', () => {
  beforeAll(async () => { await bootApp(); });
  afterAll(async () => { await closeApp(); });

  it.skip('matrix populated in PR 3', () => {});
});
