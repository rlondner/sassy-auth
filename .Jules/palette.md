# Palette's Journal

## 2025-05-14 - Button Loading States
**Learning:** Adding a native loading state to core components like Button improves consistency across the app compared to manual '...' text replacements.
**Action:** Use the `loading` prop in `Button` instead of conditional text rendering for async operations.
