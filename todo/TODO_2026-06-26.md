# TODO — 2026-06-26

Daily review follow-ups. No new commits (8th consecutive day). No new bugs found today.

## Urgent: commit or discard uncommitted work

The working tree has carried the same uncommitted changes for 8+ days (since 2026-06-18/19). These changes include:

1. **Sonner toast integration** (15 CRUD surfaces) -- ready to commit minus bug-0112 (French translations)
2. **`resolveIssuer()` DRY refactor** -- ready to commit minus bug-0115 (URL validation)
3. **`NEST_GLOBAL_PREFIX` export** -- ready to commit (no blocking issues)
4. **E2E `raceSuccessOrError` scoping fix** -- ready to commit (no blocking issues)
5. **OAuth E2E route stub narrowing** -- ready to commit (no blocking issues)
6. **Roles `platform.users.manage` read expansion** -- ready to commit (tests updated)
7. **Playwright `workers: 1 : 1` dead ternary** -- bug-0113, should be reverted to `1 : 2`

**Recommendation:** Commit items 3-6 immediately (no blocking bugs). Items 1-2 need minor fixes first. Item 7 should be reverted.

## Critical bugs -- 30 days without a fix

| Bug | Priority | Effort | Summary |
|-----|----------|--------|---------|
| bug-0001 | P0 | Medium | Add `orgId` scoping to `checkPermission` |
| bug-0074 | P0 | Low | Check `saUser.status` before issuing JWT |
| bug-0054 | P0 | Low | Validate `redirect_uri` against app's registered URL |
| bug-0094 | P0 | Low | Handle `undefined` targetAppId in `checkPermissionForApp` |
| bug-0039 | P1 | Medium | Replace in-memory OAuth code store with DB/Redis |
| bug-0038 | P1 | Medium | Add migration path for JWT `permissions` -> `scope` |

## Bugs to fix before committing current working tree

| Bug | File | Fix |
|-----|------|-----|
| bug-0112 | `messages/fr.json` | Translate 15 toast i18n strings to French |
| bug-0113 | `playwright.config.ts` | Revert workers to `CI_TESTS ? 1 : 2` |
| bug-0115 | `oauth-metadata.ts` | Add URL validation to `resolveIssuer()` |
| bug-0116 | `users-table.tsx` | Add `setViewOpen(false)` to inline delete handler |

## Project health summary

- **Day 30** with zero fixes merged to production
- **114 open bugs** (6 critical, 108 non-critical)
- **8 consecutive days** with no commits
- **Uncommitted changes** aging since 2026-06-18
- No PRs merged since initial development phase
