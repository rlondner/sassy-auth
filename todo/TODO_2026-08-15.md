# TODO — 2026-08-15 daily code review

`gh` (keyring `gho_…`) and `git fetch origin` both work this run. Window activity: **one new PR,
#324** — Jules "🎨 Palette: Add localized ARIA labels to table row action buttons." Unlike the
#303/#306/#316 lineage it is **admin-only** (no `apps/auth-server`/seed edits) and is functionally
correct. Two new findings, both pre-merge hygiene: **bug-0254** (it duplicates the still-open
#316) and **bug-0255** (test coverage). `master` still frozen at `a78d4a7` — **31 days**.

## 🟠 Medium — resolve the #324 / #316 overlap (bug-0254)

- [ ] **Pick one carrier for the row-action a11y label.** #324 and #316 change the identical
      `aria-label={t('common.moreActions')}` lines in all 5 tables and add the same `en/fr` keys →
      guaranteed merge conflict. **Prefer #324** for the label + `fr.json` `common` fix: it carries
      **no** bug-0250/0251 auth-server/seed payload.
- [ ] **Then reduce #316 to its unique safe delta only** — the copy-button `common.copy`/`copied`
      localization — after stripping the `auth.config.ts` (`autoSignIn:false`) + 3 seed edits per
      **bug-0251**, and rebasing onto whatever #324 lands. Or close #316's a11y half and re-file the
      copy-button work on top of #324.
- [ ] **Note:** landing #324 also closes **bug-0244** (fr.json `common` save/edit/delete/confirm
      were English placeholders → French) — the safest path to that fix, since #316's identical fix
      is gated behind the Critical bug-0250.

## 🟢 Low — test parity (bug-0255)

- [ ] **Add the `moreActions` aria-label assertion to the 4 sibling table suites**
      (`orgs`/`permissions`/`roles`/`users`-`table.test.tsx`), matching apps-table's added test.
      Three lines each, reusing the existing `withIntl`/`en` fixtures. Or parametrize one test over
      all five tables.

## 🔴 Carried forward — still-open blockers (not in the 24h window, unchanged)

- [ ] **bug-0250 (Critical)** — remove the seed 2FA-prompt suppression
      (`twoFactorPromptedAt:new Date()` in `seed.ts:120` / `demo-multitenant.ts:103` /
      `demo-resource-server.ts:163`). Still present in #316 and #315. Leave the column null or gate
      behind `SEED_SUPPRESS_2FA_PROMPT && NODE_ENV !== 'production'`; add a test that a seeded admin
      still hits `/login/two-factor-prompt`.
- [ ] **bug-0253** — the native-`title` copy confirmation never shows "Copied!" under a stationary
      pointer (#316 `b3acc18`). Restore a reactive Radix `<Tooltip>` for the copy button, or add a
      visually-hidden `aria-live="polite"` announcement.
- [ ] **bug-0243** — `apps/admin` Jest suite red on `master` since `a78d4a7`; standalone fix #287
      never merged. CI is not gating on it, which is why bug-0255's coverage gap wouldn't fail a
      build anyway. Merge #287 (or fold into the next admin PR).

## ⚙️ Process — the real bottleneck

- [ ] **Merge, or the backlog compounds.** `master` has not advanced in **31 days**; every Jules PR
      is re-deriving work an earlier unmerged PR already did (bug-0245, bug-0248, and now bug-0254).
      Triage: land the safe admin-only PRs (#324, and the a11y subsets of #295/#311/#313), close the
      duplicates, and quarantine the auth/seed-bundling PRs (#303/#306/#315/#316) until bug-0250 is
      stripped.
- [ ] **Add Jules guardrails:** (a) forbid `palette/*` PRs from touching `apps/auth-server/**`;
      (b) skip generating a PR whose file+line set is already covered by an open `palette/*` PR;
      (c) require a non-mock assertion for every changed user-facing surface.
