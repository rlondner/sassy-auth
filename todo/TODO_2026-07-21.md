# TODO — 2026-07-21 daily code review

Review window: last 24h. Activity was three Jules-bot "Palette" commits on **unmerged** PR
branches (#285 "Localize useCopyFeedback", #286 "Checkbox focus states"). `master` is
unchanged since `a78d4a7` (2026-07-15). No production code merged in the window.

## Bugs filed today (see bugs/BUGS_2026-07-21.md)

- [ ] **bug-0243** (Warning) — Fix stale `app-create-drawer` test; add admin Jest CI gate.
- [ ] **bug-0244** (Minor) — Translate French `common.save/edit/delete/confirm`.
- [ ] **bug-0245** (Minor) — Drop empty commit `09391e7`; dedupe overlapping Jules PRs.

## Risky patterns spotted

- **A failing unit test reached `master`.** bug-0243 shows the 2b feature merged with
  `app-create-drawer.test.tsx` red. Two separate bots then "fixed" it in unrelated PRs. The
  real gap is a merge gate: confirm the `apps/admin` Jest suite actually runs and blocks on
  every PR to `master`. This is the highest-priority follow-up.
- **Two open PRs edit the same test lines** (#285 and #286 both patch
  `app-create-drawer.test.tsx`). They will conflict. Merge order matters; ideally fix
  bug-0243 on `master` first, then rebase both.
- **Empty head commit in a PR** (`09391e7`). Jules occasionally emits a no-op tip commit
  (bug-0245, related to bug-0090). Worth a guard in the Jules flow to reject empty commits.
- **Recurring partial-translation slips** in `fr.json` (bug-0244, after bug-0112 / bug-0239).
  The same root cause keeps recurring because there is still no lint that flags a French
  value identical to its English counterpart. Land that lint once and this class disappears.

## Missing tests / coverage gaps

- No CI gate visibly enforcing the admin Jest suite on `master` (inferred from bug-0243).
- No i18n parity/identity lint for `en.json` ↔ `fr.json` (would have caught bug-0112, 0239,
  0244).
- `useCopyFeedback` test (`c55d026`) uses **real timers** with a 50ms reset and `waitFor`
  timeouts — potentially flaky under CI load. Prefer `jest.useFakeTimers()` for the reset
  assertion. Low priority; note for when PR #285 is reviewed.

## Follow-ups / housekeeping

- [ ] Review & merge PR #285 and #286 in the right order (see risky patterns).
- [ ] Address the still-open manually-filed bugs from 2026-07-20 (bug-0239 … bug-0242):
      French `users.*` toasts, raw OTP error key, `TWO_FACTOR_TRUST_DAYS` docs, dead
      `login.otp.resend` key. bug-0240 is partially addressed by today's README update
      (env-var table) — still needs the `.env.example` line.
- [ ] `master` has not advanced in 6 days despite active PR generation — confirm the merge
      pipeline is not stuck.
