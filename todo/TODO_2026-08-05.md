# TODO — 2026-08-05 daily code review

GitHub access was restored this run (github MCP connector), so a real review happened for the
first time since 2026-07-26. One genuinely new PR (**#306**) landed in the window and produced
two bugs (0247, 0248). Everything else remains **merge-hygiene debt** — the frozen-`master`
backlog keeps growing.

## 🔴 High — security regression risk in flight

- [ ] **Resolve the two divergent seed session-gate bypasses (bug-0247 / bug-0246).** PR #303
      adds `SEED_RUNNING === "true"`; PR #306 adds `SEEDING === '1'` — both in the same
      `auth.config.ts` `create.before` hook, both **unfenced by `NODE_ENV`**. Pick ONE flag,
      fence it (`&& process.env.NODE_ENV !== 'production'`), add a regression test that the gate
      still runs in production, and document the flag in `.env.example` / README as
      *seed-internal, never deploy*. Do not merge either PR as-is.
- [ ] **Do not let PR #306 reach `master` under its current scope.** A UI-labeled PR must not
      silently carry a production auth bypass. Split it (see bug-0248).

## 🟠 Medium — PR hygiene / de-duplication

- [ ] **Close the duplicate password-toggle PRs.** #294, #296, #306 all implement the same
      login toggle (bug-0248). Keep one (de-scoped), close the other two.
- [ ] **Land bug-0243's real fix on `master`.** The admin Jest suite has been red since
      `a78d4a7` and has now been patched by three separate Jules PRs because the standalone fix
      (PR #287) never merged. Merge #287 (or cherry-pick the `app-create-drawer.test.tsx` edit)
      so `master` goes green, then the bundled copies become no-ops.
- [ ] **Add a Jules guardrail:** (a) skip generating a feature that already has an open PR;
      (b) forbid UI-labeled `palette/*` PRs from touching `apps/auth-server`.

## 🟡 Low — merge backlog (root cause of the drift, unchanged from 2026-07-22)

`master` has not advanced since **2026-07-15**. The daily review's own output keeps piling up in
unmerged PRs (#287–#290 from 07-21, #305 from 08-04, and now today's bug PRs), so each run
re-reviews a frozen commit and local `CHANGELOG.md` / `README.md` drift ahead of `master`.

- [ ] **Merge or close the review backlog:** #287 (bug-0243), #288 (bug-0244), #289 (bug-0245),
      #290 (07-21 docs bundle), #304 (bug-0246), #305 (08-04 review docs).
- [ ] **Prune the stale branch backlog** (`fix/bug-01xx`, `bugs/bug-024x`, `palette/*`,
      `daily-review-*`) — dozens of unmerged branches make review-window math unreliable.
- [ ] **Gate CI on the `apps/admin` Jest suite** so a red suite can't merge (it merged red in
      `a78d4a7`).

## ✅ Infra note (resolved this run)

- The github MCP connector worked this session, unblocking the review that CR_2026-07-27 →
  CR_2026-08-04 could not perform. The scheduled task should **prefer the github MCP** over
  `curl api.github.com` (still proxy-blocked) and over the rejected `.env.local` PAT. Consider
  updating the task prompt to codify MCP-first.

## Reference

- New bugs: `bugs/BUGS_2026-08-05.md` (bug-0247, bug-0248). Highest bug number now **0248**.
- Review detail: `code_reviews/CR_2026-08-05.md`.
