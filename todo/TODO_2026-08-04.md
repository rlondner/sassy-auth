# TODO — 2026-08-04 daily code review

Window activity: **PR #303** only (Jules "Palette" bot). `master` still frozen at `a78d4a7`
(2026-07-15). Items below are the risky patterns / missing tests / merge-hygiene surfaced this
run.

## 🔴 High

- [ ] **Fix bug-0246 — fence the `SEED_RUNNING` session-gate bypass.** PR #303 lets a single env
      var disable the account-status gate in production auth code (`auth.config.ts`). Gate it on
      `NODE_ENV !== 'production'` (or keep the seed out of the prod auth path), add a regression
      test, and document the flag. See `bugs/bug-0246.md` / PR #304.
- [ ] **`master` is still red (bug-0243).** The admin Jest suite fails on `master` because
      `app-create-drawer.test.tsx` asserts a 3-field create payload while the component sends 5.
      PR #303 *does* fix the test, but it is unmerged. Land the fix (via #303 or a focused PR)
      and **make CI block on the `apps/admin` Jest suite** — it has now shipped red through
      multiple merges.

## 🟠 Medium — risky patterns in PR #303

- [ ] **Scope creep: a "UI tooltip" PR carries backend auth/seed changes.** PR #303 is titled
      "🎨 Palette: Unified tooltips…" but also edits `auth.config.ts` (the SEED_RUNNING bypass),
      three `seed/*.ts` files (seeding `twoFactorPromptedAt`), and flips the entire admin app to
      double-quote/Prettier style across 18 files. Net diff is **+2427/−1223**, of which the
      substantive backend change is ~10 lines. This is exactly how bug-0246 slipped in under
      cosmetic noise. **Split UI polish, localization, seed/test-infra, and reformatting into
      separate PRs** so the security-relevant lines are reviewable.
- [ ] **Wholesale reformatting with no enforced style.** The repo has **no** `.prettierrc` / no
      ESLint quote rule (checked), so PR #303's single→double-quote conversion is not a lint fix —
      it is opt-in churn that diverges admin files from the single-quote style still used in
      `auth-server`. Either commit to a repo-wide Prettier config (and reformat in one dedicated
      PR) or drop the reformatting from feature PRs.

## 🟡 Missing tests

- [ ] **Tooltip provider dependency is untested / mocked away.** The table tests replace
      `Tooltip`/`TooltipProvider`/`TooltipTrigger`/`TooltipContent` with passthrough stubs, so they
      cannot catch a missing-`TooltipProvider` regression. The app happens to be safe only because
      `SidebarProvider` supplies a global provider — an invisible, load-bearing coupling. Add at
      least one test (or a lint rule) that renders a table tooltip against the **real** Radix
      components inside the real shell, so removing the sidebar's provider would fail a test rather
      than crash at runtime.
- [ ] **No regression test for the session gate under `SEED_RUNNING`.** Assert the gate still runs
      (and still rejects a non-active user) when `NODE_ENV=production` regardless of
      `SEED_RUNNING`. Covered by bug-0246's suggested fix.

## 🟠 Medium — unmerged review backlog (root cause of the drift)

`master` has not advanced since **2026-07-15**. Everything produced since — daily reviews and
Jules Palette PRs alike — is piling up unmerged, so each run re-reviews the same frozen commit
and `master`'s `CHANGELOG.md`/`README.md` fall further behind (still top out at 2026-07-08 on
`master`).

- [ ] **Triage the Jules "Palette" PR backlog #292–#303** (a dozen open UI PRs). Several overlap
      (repeated checkbox-focus / loading-state / sidebar-focus PRs). Land the good ones, close the
      redundant/superseded ones. #303 in particular should **not** merge as-is (see scope-creep and
      bug-0246 above).
- [ ] **Land or close the stacked daily-review PRs** (#284, #290, #291, and this run's PR) plus the
      2026-07-20/21 bug PRs (#280–#283, #287–#289). Until these merge, the review docs on `master`
      stay a month stale.
- [ ] **Prune the stale branch backlog.** Dozens of `fix/bug-01xx`, `bugs/bug-02xx`, `palette/*`,
      and `daily-review-*` branches remain unmerged; the volume makes review-window math unreliable.

## 🟡 Low — process

- [ ] **Prior daily runs left uncommitted artifacts.** `code_reviews/CR_2026-07-{21,22,27,30,31}.md`
      and `CR_2026-08-{03,04}.md` sit untracked in the working tree — evidence that runs on those
      dates produced a code-review file but never completed the full commit/PR workflow (no
      matching `BUGS_`/`TODO_`/CHANGELOG for 07-27 → 08-03). Confirm the scheduled task reliably
      reaches the push+PR step, and either commit or `.gitignore` the `code_reviews/` artifacts.

## Reference

- 1 new bug this run — see `bugs/BUGS_2026-08-04.md` (bug-0246, PR #304).
- Highest bug number is now **0246**.
