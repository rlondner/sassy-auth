# TODO — 2026-07-07

Follow-up items from the daily code review. Multi-agent deep sweep across server actions, admin security, auth-server bootstrap, i18n, shared UI accessibility, page authorization, seed scripts, and TypeScript configuration. 22 new bugs found (0 critical, 10 warning, 12 minor).

**Update (hardening batch shipped later same day):** 9 bugs closed — bug-0189, bug-0192, bug-0193, bug-0194, bug-0195, bug-0198, bug-0202, bug-0204, bug-0208, bug-0210. 3 additional bugs discovered during verification and filed as bug-0211..0213 (see `bugs/BUGS_2026-07-07.md`).

## Critical — Existing (9 open, 0 new)

All 9 critical bugs from prior reviews remain open. Day 6 with zero commits and zero critical fixes.

- [ ] **bug-0001** — `checkPermission` not org-scoped — cross-tenant access
- [ ] **bug-0038** — JWT `permissions` → `scope` breaking change, no migration
- [ ] **bug-0039** — In-memory OAuth code store — blocks horizontal scaling
- [ ] **bug-0054** — `redirect_uri` validation bypass (RFC 6749 §10.6)
- [ ] **bug-0074** — Inactive/pending users can authenticate and obtain JWTs
- [ ] **bug-0094** — `checkPermissionForApp` silent grant when targetAppId undefined
- [ ] **bug-0147** — username/phoneNumber non-unique login collision
- [ ] **bug-0148** — publicId 'placeholder' race on concurrent creates
- [ ] **bug-0183** — `createPermission` allows `platform.*` names — privilege escalation

## Warning — New (10)

### Production hardening

- [x] **bug-0193** — Add `app.enableShutdownHooks()` to auth-server `main.ts` for graceful SIGTERM handling. ✅ Fixed in hardening batch.
- [x] **bug-0194** — Add `forbidNonWhitelisted: true` to `ValidationPipe` in `configure-nest-app.ts`. ✅ Fixed in hardening batch.
- [x] **bug-0192** — Add `@MaxLength(256)` to `DirectLoginDto.password` and `identifier` fields. ✅ Fixed in hardening batch.
- [ ] **bug-0191** — Add security headers (`X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`) and `Cache-Control: no-store` on authenticated admin pages via `next.config.ts` `headers()` or middleware.

### Error handling

- [x] **bug-0189** — Wrap `signOutAction` fetch in try/catch; always delete session cookie and redirect on failure. ✅ Fixed in hardening batch.
- [ ] **bug-0190** — Add try/catch to `getUserRolesAction`, `getEffectivePermissionsAction`, `getUserDirectPermissionsAction`, `getRolesAction`, `getAppPermissionsAction` in users/actions.ts.
- [ ] **bug-0196** — Switch UsersPage from `Promise.all` to `Promise.allSettled` and add `AccessDeniedPanel` check consistent with other pages.

### i18n

- [x] **bug-0195** — Replace 4 hardcoded English strings in `accept-invite-form.tsx` with `t()` calls; add keys to en.json and fr.json. ✅ Fixed in hardening batch.

### TypeScript configuration

- [ ] **bug-0197** — Enable `strict: true` in auth-server `tsconfig.json`. Fix resulting type errors. The auth-server is the most security-critical package and should have the strongest type safety.
- [x] **bug-0198** — Add `tsconfig-paths` to `packages/db` devDependencies so `db:seed` doesn't rely on pnpm hoisting. ✅ Fixed in hardening batch.

## Minor — New (12)

- [ ] **bug-0199** — Fix apps `mapError` to use generic `forbidden` instead of `platformProtected` for 403 on create/update.
- [ ] **bug-0200** — Remove dead-code self-delete detection in `deleteUserAction` (blocked by bug-0050).
- [ ] **bug-0201** — Remove dead-code `message.includes('already')` in `createUserAction` (blocked by bug-0050).
- [x] **bug-0202** — Remove `: any` annotation on BetterAuth `auth` export in `auth.config.ts`. ✅ Fixed in hardening batch.
- [ ] **bug-0203** — Link `FormField` hint text to input via `aria-describedby` with proper `id`.
- [x] **bug-0204** — Change `CardTitle` from `<div>` to `<h3>` for screen reader heading navigation. ✅ Fixed in hardening batch.
- [ ] **bug-0205** — Pass `canWrite` prop to `RoleViewDrawer` and gate edit/delete buttons.
- [ ] **bug-0206** — Update `selected` state after table data refresh to avoid stale drawer data.
- [ ] **bug-0207** — Add `loading.tsx` to `apps/admin/app/(admin)/` for navigation loading indicator.
- [x] **bug-0208** — Fix seed `else if` chain to update both `isSystem` and `publicId` in a single pass. ✅ Fixed in hardening batch.
- [ ] **bug-0209** — Add dummy scrypt verification on user-not-found path in `directLogin` to prevent timing-based user enumeration.
- [x] **bug-0210** — Add `.catch()` to `bootstrap()` call in `main.ts` to prevent silent startup failures. ✅ Fixed in hardening batch.

## Newly discovered — from hardening batch verification (3)

- [ ] **bug-0211** — Regenerate `@sassy-auth/db` Prisma client so `SaApp.callbackUrl` is in the client type; unblocks `apps.service.spec.ts` and `apps.controller.spec.ts` (both currently fail to compile).
- [ ] **bug-0212** — Update `app-create-drawer.test.tsx:33` assertion to include `callbackUrl: null` in the expected payload (drift from `032a357` merge).
- [ ] **bug-0213** — Add `jest.mock('next/navigation', ...)` to `users-table.test.tsx` (matches sibling convention); unblocks 4 tests.

## Recommended priority

1. **bug-0193** (shutdown hooks) — one-line fix, prevents 502s on deploy
2. **bug-0192** (login password MaxLength) — one-line fix, prevents scrypt DoS
3. **bug-0194** (forbidNonWhitelisted) — one-line fix, hardens API validation
4. **bug-0210** (bootstrap .catch) — one-line fix, prevents silent startup failure
5. **bug-0189** (signOutAction error handling) — small fix, critical UX improvement
6. **bug-0191** (security headers) — moderate fix, standard security hardening
7. **bug-0197** (strict tsconfig) — larger fix, but improves type safety across the auth-server
8. **bug-0190** (server action error handling) — systematic fix across 5 actions
9. **bug-0195** (accept-invite i18n) — small fix, visible to French users
10. **bug-0196** (UsersPage error handling) — moderate fix, consistency with other pages
