# TODO — 2026-06-24

Follow-up items from the daily code review. No new commits in the last 6 days. Working tree unchanged since 2026-06-22. No new bugs found.

## Priority 1: Commit the working tree changes

The uncommitted changes have been sitting for 72+ hours and represent solid improvements:
- Sonner toast integration (15 success toasts across all CRUD operations)
- `onSuccess` / refresh pattern for all drawer/table pairs
- `resolveIssuer()` DRY refactor (prevents discovery/JWT issuer drift)
- `NEST_GLOBAL_PREFIX` exported as single source of truth
- E2E `raceSuccessOrError` scoping fix (blocks Next.js Dev Tools phantom alert)
- OAuth E2E route mock narrowing (fixes test hang when origins overlap)
- Roles read permission expansion to include `platform.users.manage`

**Before committing**, fix these 4 open bugs in the working tree:

1. **bug-0112** (Warning, ~5 min) — Translate the 15 French toast i18n strings in `apps/admin/messages/fr.json`.
2. **bug-0113** (Minor, ~1 min) — Restore `workers: CI_TESTS ? 1 : 2` in `apps/admin-e2e/playwright.config.ts` or document the intentional single-worker choice.
3. **bug-0114** (Minor, ~10 min) — Wrap async `refresh()` calls in admin tables (apps, orgs, permissions, roles) with try/catch and show an error toast on failure.
4. **bug-0115** (Warning, ~15 min) — Add URL validation to `resolveIssuer()` in `oauth-metadata.ts`: reject empty, whitespace, non-URL, and non-HTTP(S) values with a clear startup error.

## Priority 2: Critical bugs (day 28 with zero fixes merged)

These 6 critical bugs have been open since the earliest reviews and block production deployment:

1. **bug-0001** — RBAC not org-scoped: `checkPermission` doesn't constrain by orgId. *(Day 28)*
2. **bug-0038** — JWT `permissions` → `scope` breaking change with no migration path. *(Day 22)*
3. **bug-0039** — In-memory OAuth authorization code store; lost on restart. *(Day 22)*
4. **bug-0054** — `redirect_uri` not validated against app's registered URL. *(Day 19)*
5. **bug-0074** — Inactive users can authenticate and receive JWTs. *(Day 18)*
6. **bug-0094** — `checkPermissionForApp` silently grants when `targetAppId` is undefined. *(Day 6)*

## Priority 3: Code quality observations

1. **`raceSuccessOrError` is duplicated in 5 E2E page objects** — identical function in `apps.page.ts`, `orgs.page.ts`, `permissions.page.ts`, `roles.page.ts`, `users.page.ts`. Extract to a shared `e2e-helpers.ts` utility.
2. **Roles permission surface expansion** — `platform.users.manage` now grants read access to all roles and their full permission details via `listRoles` and `getRole`. This is intentional (user admin role picker), but worth documenting in an architecture decision record that user-management admins can see all role definitions.

## Observations

- **28 consecutive days** with zero bug fixes merged to master.
- **113 open bugs** in the catalog (unchanged from 2026-06-23).
- **Working tree unchanged** for 72+ hours — no development activity since the toast/refresh integration was last modified around 2026-06-22.
- The risk of losing uncommitted work grows each day it remains uncommitted. Consider committing the current changes (even with known minor bugs) and fixing the bugs in follow-up commits.
