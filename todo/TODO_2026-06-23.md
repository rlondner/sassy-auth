# TODO — 2026-06-23

Follow-up items from the daily code review. No new commits in the last 24 hours; deeper scan of the uncommitted working tree changes found 1 new bug.

## Priority: Fix before committing working tree changes

1. **bug-0112** (Warning) — Translate the 15 French toast i18n strings in `fr.json`. Currently all English copies. Quick fix — takes 5 minutes.
2. **bug-0113** (Minor) — Decide on Playwright workers config: restore `CI_TESTS ? 1 : 2` or intentionally set `workers: 1` with an explaining comment.
3. **bug-0114** (Minor) — Wrap `refresh()` calls in admin tables (apps, orgs, permissions, roles) in try/catch and show an error toast on failure.
4. **bug-0115** (Warning) — Add URL validation to `resolveIssuer()` in `oauth-metadata.ts` so malformed `BETTER_AUTH_URL` values fail fast at startup rather than producing invalid issuer values in discovery docs and JWTs.

## Priority: Critical bugs still open (day 27 with zero fixes merged)

These 6 critical bugs have been open since the earliest reviews and block production deployment:

1. **bug-0001** — RBAC not org-scoped: `checkPermission` doesn't constrain by orgId. *(Day 27)*
2. **bug-0038** — JWT `permissions` → `scope` breaking change with no migration path. *(Day 21)*
3. **bug-0039** — In-memory OAuth authorization code store; lost on restart. *(Day 21)*
4. **bug-0054** — `redirect_uri` not validated against app's registered URL. *(Day 18)*
5. **bug-0074** — Inactive users can authenticate and receive JWTs. *(Day 17)*
6. **bug-0094** — `checkPermissionForApp` silently grants when `targetAppId` is undefined. *(Day 5)*

## Priority: Commit the working tree changes

The uncommitted toast/onSuccess/refresh integration is a solid UX improvement. Once bugs 0112-0115 are addressed, the changes should be committed. The longer the working tree stays uncommitted, the higher the risk of accidental loss or merge conflicts.

## Observations

- **27 consecutive days** with zero bug fixes merged to master.
- **113 open bugs** in the catalog (93 from first 10 days, 18 from day 23, 3 from day 26, 1 from day 27).
- **Working tree unchanged** since 2026-06-22 — no development activity in the last 48+ hours.
- The `resolveIssuer()` refactor in the working tree is a good architectural improvement (DRY, prevents issuer drift), but needs the validation fix from bug-0115 before it ships.
