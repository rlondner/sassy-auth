# TODO — 2026-07-22 daily code review

No new source activity landed in the last 24h. The follow-ups below are all **process /
merge-hygiene** items, and they are the reason the daily review keeps reviewing a frozen tree.

## 🔴 High — `master` is red and frozen

- [ ] **Fix bug-0243 on `master`.** The admin Jest suite fails on `master` because
      `app-create-drawer.test.tsx:33-37` asserts an exact 3-field payload while the component
      now sends 5 fields (`twoFactorTrustDays`, `requireTwoFactor`). Confirmed still failing this
      run. Add `twoFactorTrustDays: null` and `requireTwoFactor: false` to the assertion.
- [ ] **Gate merges on the `apps/admin` Jest suite.** The per-app 2FA feature (`a78d4a7`) merged
      with this suite red, and two independent Jules PRs each had to patch it (bug-0245). CI is
      clearly not running / not blocking on this suite.

## 🟠 Medium — unmerged review backlog (root cause of the drift)

`master` has not advanced since **2026-07-15**. Everything the daily review has produced since
is piling up in open PRs that never merge, so each run re-reviews the same frozen commit and the
local `CHANGELOG.md` / `README.md` fall further behind (they still top out at 2026-07-08 on
`master`; the 07-21 versions exist only in PR #290).

- [ ] **Merge or close the 2026-07-21 review PRs:** #287 (bug-0243), #288 (bug-0244),
      #289 (bug-0245), #290 (CHANGELOG + README + BUGS/TODO bundle).
- [ ] **Resolve the Jules palette PRs #285 / #286.** #286's tip `09391e7` is an empty commit and
      both PRs add the *identical* test edit — the second to merge will conflict (bug-0245).
      Fixing bug-0243 on `master` makes both edits unnecessary; drop the empty commit.
- [ ] **Decide the fate of the large stale branch backlog.** `git branch -r` shows dozens of
      unmerged `fix/bug-01xx`, `bugs/bug-024x`, `palette/*`, and `daily-review-*` branches. Either
      land them or prune them; the volume is making review-window math unreliable.

## 🟡 Low — process

- [ ] **Automate CHANGELOG/README sync on merge**, or have the daily review commit directly to a
      branch that actually gets merged, so `master` docs stop lagging a week behind.
- [ ] **Confirm the daily-review scheduled task can push + open PRs.** Prior runs' PRs exist, so
      `git fetch`/push has worked at least intermittently, but the accumulation suggests the
      merge step (human or bot) is the bottleneck, not the filing step.

## Reference

- No new bugs — see `bugs/BUGS_2026-07-22.md`.
- Highest bug number remains **0245**.
