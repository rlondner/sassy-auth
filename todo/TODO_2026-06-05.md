# TODO — Follow-ups from 2026-06-05 Daily Review

Living list of risky patterns, missing tests, and design-level follow-ups surfaced by the daily code review. Mechanical fixes are tracked in [BUGS_2026-06-05.md](../bugs/BUGS_2026-06-05.md); this file collects everything that needs **judgement, planning, or larger context** before action.

## PR backlog — 31 open, 0 merged (day 10)

The bug-fix PR backlog continues to grow with zero implementation. 31 PRs are open, all with empty branches (no implementation commits). This is the 10th day with no fixes merged.

**Recommendation:** At this point the stub-PR-per-bug pattern is creating overhead without value. Consider:
1. Closing all empty stub PRs.
2. Prioritizing the 3 critical bugs (bug-0001, bug-0038, bug-0039) for actual implementation.
3. Re-creating PRs only when code is ready.

## New bugs from today

- [ ] **bug-0050 — `apiFetch` discards error details** — Admin UI shows "API error 400" instead of server-provided messages like "Email already exists." Every admin mutation is affected.
- [ ] **bug-0051 — Invitation token in Error messages** — `validateInvitation` includes the raw token in `throw new Error(...)`, leaking it to Sentry, error boundaries, and console logs.
- [ ] **bug-0052 — Effective permissions use fake IDs** — `getEffectivePermissions` client wrapper returns `Permission[]` with `id: name` (a name string) and `appId: ''`. Latent until someone adds inline actions to the effective permissions view.
- [ ] **bug-0053 — setTimeout without cleanup in 10 components** — Drawer copy handlers and the accept-invite form fire timers without tracking or cleanup on unmount.

## Carried from previous reviews (still open)

### Critical — must resolve before PKCE ships

- [ ] **bug-0001 — RBAC not org-scoped** — `checkPermission` now supports `targetOrgId`, but orgs/roles/permissions services still don't pass it consistently.
- [ ] **bug-0038 — JWT breaking change needs migration plan** — `permissions` array -> `scope` string rename.
- [ ] **bug-0039 — Move authorization codes to persistent storage** — In-memory `Map` is lost on restart.
- [ ] **bug-0041 — Add RFC 7636 format validation** — `code_verifier` lacks format check.

### Missing test coverage (unchanged — no tests added)

- [ ] No PKCE format validation tests
- [ ] No code expiration boundary test
- [ ] No cross-app code exchange test
- [ ] No code reuse e2e test
- [ ] FastAPI OAuth routes untested
- [ ] `safe-next.ts` missing edge cases
- [ ] No integration test for demo seed
- [ ] Previous gaps from 2026-06-02 and earlier still open

### Risky patterns (unchanged)

- [ ] FastAPI token exposure to JavaScript (bug-0042)
- [ ] JWKS cache lifespan (10-minute window)
- [ ] TOCTOU in `resolveAppScopedIds`
- [ ] Audit log asymmetry
- [ ] Hardcoded `take: 50` in detail views
- [ ] Redundant count queries

### Admin UI follow-ups (updated)

- [ ] `LOGIN_NEXT_ALLOWED_ORIGINS` env var undocumented in `.env.example`
- [ ] Direct permissions missing app context in view drawer (bug-0036)
- [ ] No loading indicator during delete operations
- [ ] No toast/success feedback after mutations
- [ ] Duplicated pagination component
- [ ] `NAME_REGEX` defined locally in multiple files
- [ ] Server actions have no Sentry integration
- [ ] Sidebar `startsWith` matching (bug-0033, PR exists)
- [ ] **NEW:** Error messages from auth-server not surfaced (bug-0050)
- [ ] **NEW:** Effective permissions display uses synthetic IDs (bug-0052)

### Configuration / ops follow-ups (unchanged)

- [ ] `.env.example` missing: `LOGIN_NEXT_ALLOWED_ORIGINS`, `SEED_DEMO`, `E2E_ADMIN_EMAIL`, `E2E_ADMIN_PASSWORD`, `CI_TESTS`, `JWT_TOKEN_LIFETIME_SECONDS`
- [ ] No `pnpm lint` or type-check in CI
- [ ] No Python linting/formatting or CI workflow for FastAPI resource server

---

*Generated as part of the 2026-06-05 daily code review. See [CHANGELOG.md](../CHANGELOG.md), [BUGS_2026-06-05.md](../bugs/BUGS_2026-06-05.md) for the bug catalog.*
