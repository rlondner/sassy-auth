# TODO -- Follow-ups from 2026-06-18 Daily Review

Living list of risky patterns, missing tests, and design-level follow-ups surfaced by the daily code review. Mechanical fixes are tracked in [BUGS_2026-06-18.md](../bugs/BUGS_2026-06-18.md); this file collects everything that needs **judgement, planning, or larger context** before action.

## PR backlog -- 93 open, 0 merged (day 22)

The PR backlog has grown to 93 open PRs, all with empty bug-fix branches. The Jules bot added 10+ new PRs (#100–#109) for tooltip/loading-state work, most of which are duplicates. This is the 22nd consecutive day with no fixes merged.

**Recommendation (escalated):**
1. **Immediately close all duplicate Jules bot PRs** — keep one per task (#107 for tooltips), close the rest (#100–#106, #108, #109).
2. **Close all 82 empty bug-fix stub PRs** — they serve no purpose without implementation code.
3. **Triage the 93 bugs by severity** — 5 Critical, ~38 Warning, ~35 Minor.
4. **Fix the 5 Critical bugs first** (bug-0001, bug-0038, bug-0039, bug-0054, bug-0074) — these represent auth bypass, token security, and tenant isolation failures.
5. **Investigate and fix auth-server build errors** — the CI workaround (bug-0092) should be temporary, not permanent.
6. **Review Jules bot configuration** — the bot is producing duplicate branches and PRs instead of fixing existing bugs.

## Changes reviewed today

4 commits by `google-labs-jules[bot]` (2026-06-17) on `palette/table-action-tooltips` branch (PR #107):
- Tooltip wrappers on 5 table action buttons with i18n
- `TooltipProvider` added to root layout
- French translation fixes for common actions
- CI e2e.yml: added workspace build step (excluding auth-server)
- Auth-server seed script: added `--transpile-only`
- Jest tooltip mocks in setup and 5 test files

## New bugs from today (4 bugs)

### Warning -- fix soon

- [ ] **bug-0090 -- Jules bot duplicate PRs** -- 6+ overlapping PRs (#100–#109) for the same tooltip/loading-state tasks. Close duplicates and investigate bot retry behavior.
- [ ] **bug-0091 -- Seed script change in UI PR** -- `--transpile-only` added to auth-server seed in a tooltip PR. Revert from PR #107 and submit separately with rationale.
- [ ] **bug-0092 -- CI masks auth-server build errors** -- `e2e.yml` uses `--filter=!@sassy-auth/auth-server` to skip a broken build. Investigate and fix the build errors instead of excluding them.

### Minor

- [ ] **bug-0093 -- Jest UI mock fragility** -- Global `jest.mock('@sassy-auth/ui')` overridden by 5 file-level mocks; tooltip components must be duplicated in every new test file. Extract a shared mock factory.

## Risky patterns observed

### Auth-server build health unknown
The `e2e.yml` exclusion filter (`--filter=!@sassy-auth/auth-server`) and the `--transpile-only` seed flag both suggest the auth-server has type/build issues that haven't been addressed. This should be investigated immediately — build errors in the core auth server are a red flag.

### Jules bot generating noise, not fixes
The bot has created 10 new PRs in the last 48 hours, all for cosmetic UI improvements (tooltips, loading states), while 5 critical security bugs remain unaddressed. The bot also appears to retry failed tasks by creating new branches instead of fixing the existing attempt, leading to duplicate PRs.

### Test coverage for new tooltip behavior
The tooltip additions have jest mocks but no actual Playwright e2e tests verifying that tooltips appear on hover. The test mocks stub out tooltip components entirely, so they verify that the app doesn't crash with tooltips, but not that tooltips actually work.

## Carried forward from previous reviews

All items from TODO_2026-06-07.md remain open. Key unresolved items:
- 5 Critical bugs (bug-0001, bug-0038, bug-0039, bug-0054, bug-0074)
- 82 empty stub PRs to close
- Rate limiting (bug-0080) still not implemented
- Client_secret verification (bug-0082) still a false security signal
