# TODO — 2026-08-13 daily code review

GitHub access worked this run via the **`gh` CLI** (keyring token), so PR #316 was reviewed live.
One new PR in the window (**#316**) produced **three confirmed bugs (0250–0252)**, one Critical,
plus one **retracted false positive (0249)**. Everything else is the ongoing frozen-`master`
merge-hygiene backlog.

> ⚠️ **bug-0249 retracted.** I initially filed a Critical "Tooltip has no `TooltipProvider` →
> tables crash" (PR #317) — it was a **false positive** (the same one the 2026-08-04 run hit):
> `SidebarProvider` (packages/ui) supplies a `TooltipProvider` around the admin shell, so the
> tables render fine. PR #317 was closed + branch deleted. **Lesson for next run: when checking
> for a missing React provider, trace providers injected by shared shell/layout components from
> *other packages* — never conclude "missing" from a text search of `apps/admin` alone.**

## 🔴 High — blocker on the only new PR (#316)

- [ ] **bug-0250 — remove the seed 2FA-prompt suppression.** `twoFactorPromptedAt: new Date()` in
      `seed.ts` / `demo-multitenant.ts` / `demo-resource-server.ts` suppresses the 2FA enrollment
      prompt for every seeded user (platform admins + demo) for 14 days. Present in **#315 and
      #316** — fix both. Leave the column null, or gate behind `SEED_SUPPRESS_2FA_PROMPT`
      (`&& NODE_ENV !== 'production'`). Add a test that a seeded admin still hits
      `/login/two-factor-prompt`.

## 🟠 Medium — PR hygiene / de-duplication

- [ ] **bug-0251 — split PR #316.** Separate the table tooltip/a11y change from the auth-config +
      seed edits it silently bundles. Review any intentional auth change on its own.
- [ ] **De-duplicate #314 / #315 / #316.** All three touch `auth.config.ts` + seeds + admin
      drawer/table surface; #315 and #316 carry the identical bug-0250 change. Pick one to carry
      `autoSignIn:false`; resolve the overlapping validation/FormField work so the rest rebase or
      close.
- [ ] **bug-0252 — verify (then fix if needed) the nested double-`asChild` row-action trigger.**
      Runtime check: open a table, click the `more_vert` menu, confirm the tooltip dismisses. If
      it doesn't, let `DropdownMenuTrigger` render the button rather than stacking two `asChild`
      Slots on a nested `<button>`.
- [ ] **De-mock the table tests** so a newly introduced `@sassy-auth/ui` primitive is exercised
      for real, not as a passthrough stub (bug-0093 / bug-0251 pattern). Require at least one
      integration test that renders the real primitive inside the real shell.
- [ ] **Add Jules guardrails:** (a) forbid `palette/*` UI PRs from modifying `apps/auth-server/**`;
      (b) skip generating a feature that already has an open PR; (c) no full-mock-only coverage
      for new UI primitives.

## 🟡 Low — merge backlog (root cause of the drift, unchanged since 2026-07-22)

`master` has not advanced since **2026-07-15** (29 days). The daily-review output keeps piling up
in unmerged PRs, so each run re-reviews a frozen commit and local `CHANGELOG.md` / `README.md`
drift ahead of `master`.

- [ ] **Merge or close the review/bug backlog** (bug PRs from 07-21 → today's #318–#320, plus the
      daily-review docs PRs #305/#309, etc.).
- [ ] **Land bug-0243's real fix on `master`** (admin Jest suite red since `a78d4a7`) so `master`
      goes green and the bundled copies become no-ops.
- [ ] **Prune the stale branch backlog** (`fix/bug-01xx`, `bugs/bug-02xx`, `palette/*`,
      `daily-review-*`, `jules-*`) — dozens of unmerged branches make review-window math
      unreliable.
- [ ] **Gate CI on the `apps/admin` Jest suite** so a red suite can't merge.

## 🔧 Infra note (this run)

- ✅ **`gh` CLI worked** (keyring `gho_…` token) — `gh pr list/view/diff` pulled live data.
  **Standardize the scheduled task on `gh`**; it succeeds where `curl api.github.com` (proxy
  403) and the `.env.local` PAT (`git fetch` → `Invalid username or token`) still fail.
- [ ] **Rotate the `.env.local` `GITHUB_PAT`** (fine-grained: `Contents:read`,
      `Pull requests:read`, `Metadata:read` on `rlondner/sassy-auth`) and/or **allowlist
      `api.github.com`** so `git fetch` refreshes local refs — right now Jules branches never
      reach the local clone, so `git log --since` sees nothing and all PR data must come from
      `gh`.

## Reference

- New bugs: `bugs/BUGS_2026-08-13.md` (bug-0249 – bug-0252). Highest bug number now **0252**.
- Review detail: `code_reviews/CR_2026-08-13.md`.
