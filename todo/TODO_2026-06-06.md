# TODO -- Follow-ups from 2026-06-06 Daily Review

Living list of risky patterns, missing tests, and design-level follow-ups surfaced by the daily code review. Mechanical fixes are tracked in [BUGS_2026-06-06.md](../bugs/BUGS_2026-06-06.md); this file collects everything that needs **judgement, planning, or larger context** before action.

## PR backlog -- 46+ open, 0 merged (day 11)

The bug-fix PR backlog continues to grow with zero implementation. 46 PRs are open, all with empty branches. This is the 11th day with no fixes merged. Today's review adds 20 more bugs, bringing the total to 73.

**Recommendation:** The stub-PR-per-bug pattern has created significant overhead (46+ open PRs, all empty). Consider:
1. Closing all empty stub PRs immediately.
2. Triaging the 73 bugs by severity -- 4 Critical, ~30 Warning, ~30 Minor.
3. Implementing the 4 Critical bugs first (bug-0001, bug-0038, bug-0039, bug-0054).
4. Re-creating PRs only when code is ready.

## New bugs from today (20 bugs)

### Critical -- must fix immediately

- [ ] **bug-0054 -- `oauthAuthorize` redirect_uri not validated** -- Authorization code interception: attacker-supplied `redirect_uri` accepted without origin check, leaking auth codes to external domains.

### Warning -- fix soon

- [ ] **bug-0055 -- deleteUser orphans BetterAuth records** -- Deleted users' BetterAuth `User`, `Session`, and `Account` rows persist; email is locked; active sessions survive.
- [ ] **bug-0056 -- setLocaleAction doesn't validate locale** -- Arbitrary locale string written to cookie; path traversal possible via dynamic import.
- [ ] **bug-0057 -- setLocaleAction open redirect** -- `pathname` parameter passed directly to `redirect()` without validation.
- [ ] **bug-0058 -- Users page no permission check** -- Any authenticated user can view all users, emails, phone numbers across all orgs.
- [ ] **bug-0059 -- username/phoneNumber not unique** -- `findFirst` may return wrong user when usernames collide across orgs.
- [ ] **bug-0060 -- detectIdentifierType misclassifies** -- Numeric usernames (e.g., employee IDs) treated as phone numbers; login fails silently.
- [ ] **bug-0061 -- updateUserAction missing error handling** -- Only mutation action without try/catch; throws unhandled exceptions.
- [ ] **bug-0062 -- Seed hardcoded password, no production guard** -- Known credentials created if seed runs against production DB.
- [ ] **bug-0063 -- SentryExceptionFilter leaks invitation tokens** -- Unsanitized URL in Sentry `scope.setExtra('path', ...)`.
- [ ] **bug-0064 -- Delete menu uses data-disabled** -- Radix event handling not suppressed; keyboard activation could bypass guard.
- [ ] **bug-0065 -- signIn ignores next param** -- Post-login redirect always goes to `/users`; middleware's `next` logic is dead code.
- [ ] **bug-0066 -- DataTable sortable headers inaccessible** -- No keyboard focus/activation; WCAG 2.1 SC 2.1.1 violation.
- [ ] **bug-0067 -- Invitation token format not validated** -- Any string accepted and queried against DB; PII disclosed via GET response.

### Minor

- [ ] **bug-0068 -- TOCTOU race in update/delete services** -- `findUnique` then `update/delete` not atomic; P2025 on concurrent requests.
- [ ] **bug-0069 -- Middleware PUBLIC_PATHS prefix too broad** -- `/login-anything` treated as public.
- [ ] **bug-0070 -- Users table action buttons are no-ops** -- Clickable menu items with no handlers.
- [ ] **bug-0071 -- DataTable clickable rows inaccessible** -- Same class as bug-0066.
- [ ] **bug-0072 -- acceptInvitation raw errors to user** -- Technical error messages shown in UI.
- [ ] **bug-0073 -- OAuth codes never purged** -- Memory leak for abandoned authorization flows.

## Additional items found (not tracked as standalone bugs)

These items were found during the deep scan but are lower priority or are feature gaps rather than bugs:

### Database / schema

- [ ] FK RESTRICT on `SaOrg->SaApp`, `SaPermission->SaApp`, `SaRole->SaApp` with no UI handling -- delete app/org with children produces opaque P2003 error
- [ ] `Verification` table missing indexes on `identifier` and `expiresAt` -- full-table scans under load
- [ ] `Account` table missing compound unique index on `(providerId, accountId)` -- duplicate OAuth account links theoretically possible
- [ ] `Session` table missing index on `userId` -- slow session management queries
- [ ] When bug-0026 is fixed (permission name unique per app), seed.ts `findUnique` on name alone will break -- update seed simultaneously

### UI / accessibility

- [ ] `UserAvatar` produces alt text of single space `" "` for empty names; fallback span missing ARIA role/label
- [ ] `StatusChip` decorative dot missing `aria-hidden="true"`
- [ ] French translations incomplete (~30 strings still in English)
- [ ] Password validation in `accept-invite-form.tsx` hardcoded in English, not using i18n
- [ ] `auth.config.ts` exports `auth` typed as `any`, disabling all type safety for BetterAuth API calls

### Configuration

- [ ] `.env.example` missing: `JWT_TOKEN_LIFETIME_SECONDS`, `SEED_DEMO`, `LOGIN_NEXT_ALLOWED_ORIGINS`, `NODE_ENV`
- [ ] `turbo.json` test task missing `env` passthrough -- stale cached results when env vars change
- [ ] `turbo.json` has no `lint` task definition but root `package.json` has `"lint": "turbo lint"` (no-op)
- [ ] No `pnpm lint` or type-check in CI
- [ ] No Python linting/formatting or CI workflow for FastAPI resource server
- [ ] GitHub Actions e2e artifacts not uploaded on cancelled runs

## Carried from previous reviews (still open)

### Critical -- must resolve before PKCE ships

- [ ] **bug-0001 -- RBAC not org-scoped** -- `checkPermission` supports `targetOrgId` but services don't pass it consistently.
- [ ] **bug-0038 -- JWT breaking change needs migration plan** -- `permissions` array -> `scope` string.
- [ ] **bug-0039 -- Move authorization codes to persistent storage** -- In-memory `Map` lost on restart.
- [ ] **bug-0041 -- Add RFC 7636 format validation** -- `code_verifier` lacks format check.
- [ ] **bug-0054 -- `oauthAuthorize` redirect_uri not validated** -- NEW, Critical.

### Missing test coverage (unchanged -- no tests added)

- [ ] No PKCE format validation tests
- [ ] No code expiration boundary test
- [ ] No cross-app code exchange test
- [ ] No code reuse e2e test
- [ ] FastAPI OAuth routes untested
- [ ] `safe-next.ts` missing edge cases
- [ ] No integration test for demo seed
- [ ] Previous gaps from 2026-06-02 and earlier still open

### Risky patterns (unchanged)

- [ ] FastAPI token exposure to JavaScript (bug-0042)
- [ ] JWKS cache lifespan (10-minute window)
- [ ] TOCTOU in `resolveAppScopedIds`
- [ ] Audit log asymmetry
- [ ] Hardcoded `take: 50` in detail views
- [ ] Redundant count queries

---

*Generated as part of the 2026-06-06 daily code review. See [CHANGELOG.md](../CHANGELOG.md), [BUGS_2026-06-06.md](../bugs/BUGS_2026-06-06.md) for the bug catalog.*
