# TODO — 2026-08-14 daily code review

`git fetch origin` **worked** this run (keyring credential) and refreshed the PR refs; `gh` also
works. The strict 24h window had **no new activity** — `master` is still frozen at `a78d4a7`
(2026-07-15, **30 days**). The only thing the prior run hadn't seen is PR **#316**'s force-push to
head **`b3acc18`** (2026-08-13 10:14 UTC, 9 min after the 08-13 review's data pull), reviewed here.
It swaps Radix Tooltip → native `title`, which **resolves bug-0252** and **moots the test-mock
half of bug-0251**, but **leaves the Critical bug-0250 fully intact** and introduces one new Minor
finding (**bug-0253**).

## 🔴 High — blocker on the only active PR (#316), unchanged from 2026-08-13

- [ ] **bug-0250 — remove the seed 2FA-prompt suppression.** `twoFactorPromptedAt: new Date()` in
      `seed.ts:120` / `demo-multitenant.ts:103` / `demo-resource-server.ts:163` suppresses the 2FA
      enrollment prompt for every seeded user (platform admins + demo) for 14 days. **Still present
      unchanged in #316's latest head `b3acc18`, and in #315.** Leave the column null, or gate
      behind `SEED_SUPPRESS_2FA_PROMPT` (`&& NODE_ENV !== 'production'`). Add a test that a seeded
      admin still hits `/login/two-factor-prompt`.

## 🟠 Medium — PR hygiene / the new finding

- [ ] **bug-0253 — fix the copy-confirmation regression.** The native-`title` rewrite binds the
      copy button's `title`/`aria-label` to the transient `copied` state, but a native `title`
      doesn't update under a stationary pointer and `aria-label` mutations don't re-announce — so
      "Copied!" is never shown in the hover→click path. Restore a reactive Radix `<Tooltip>` for
      the copy button (the `TooltipProvider` is in scope via `SidebarProvider`; bug-0249 was
      retracted), **or** keep `title` static + add a visually-hidden `aria-live="polite"`
      announcement. Add a test that the confirmation isn't carried by a mutated `title`.
- [ ] **bug-0251 — split PR #316.** The native-tooltip rewrite fixed the test-mock half, but the
      PR still silently bundles `auth.config.ts` (`autoSignIn:false`) + 3 seed edits under a
      cosmetic title. Separate the auth/seed change and review it on its own.
- [ ] **De-duplicate #314 / #315 / #316.** All three touch `auth.config.ts` + seeds + admin
      table/drawer surface; #315 and #316 carry the identical bug-0250 change. Pick one to carry
      `autoSignIn:false`; resolve the overlap so the rest rebase or close.
- [ ] **Re-add a real tooltip/confirmation test.** After `b3acc18` removed the `Tooltip*` mocks,
      the tables have **no** assertion on the copy/aria confirmation at all. Add one that renders
      the real behavior (ties into bug-0253's fix).
- [ ] **Add Jules guardrails:** (a) forbid `palette/*` UI PRs from modifying `apps/auth-server/**`;
      (b) skip generating a feature that already has an open PR; (c) require a non-mock assertion
      for any user-facing confirmation/announcement.

## 🟡 Low — merge backlog (root cause of the drift, unchanged since 2026-07-22)

`master` has not advanced since **2026-07-15** (30 days). Daily-review output keeps piling up in
unmerged PRs, so each run re-reviews a frozen commit and local `CHANGELOG.md` / `README.md` drift
ahead of `master`.

- [ ] **Merge or close the review/bug backlog** (bug PRs #307/#308, #318–#320, today's #322, plus
      the daily-review docs PRs #309/#321).
- [ ] **Land bug-0243's real fix on `master`** (admin Jest suite red since `a78d4a7`) so `master`
      goes green and the bundled copies become no-ops.
- [ ] **Prune the stale branch backlog** (`fix/bug-01xx`, `bugs/bug-02xx`, `palette/*`,
      `daily-review-*`, `jules-*`) — dozens of unmerged branches make review-window math
      unreliable.
- [ ] **Gate CI on the `apps/admin` Jest suite** so a red suite can't merge.

## 🔧 Infra note (this run)

- ✅ **`git fetch origin` worked** over the keyring credential (first success in a while) and
  refreshed `pr/316…321` — so `git log --all --since` and `git diff master b3acc18` had real data
  locally this run, not just `gh`. Prefer keyring credential + `gh`.
- [ ] **Rotate the `.env.local` `GITHUB_PAT`** (fine-grained: `Contents:read`,
      `Pull requests:read`, `Metadata:read` on `rlondner/sassy-auth`) and/or **allowlist
      `api.github.com`** so authenticated pushes and `curl` also stop failing and the pipeline
      doesn't depend on the keyring credential being present.

## Reference

- New bug: `bugs/BUGS_2026-08-14.md` (bug-0253). Highest bug number now **0253**.
- Review detail: `code_reviews/CR_2026-08-14.md`.
- Status flips this run: **bug-0252 resolved by revision** (`b3acc18`, not merged), **bug-0251
  test-mock half mooted**, **bug-0250 persists (Critical)**, **bug-0249 permanently moot**.
