# TODO — 2026-08-17 daily code review

`gh` (keyring `gho_…`) and `git fetch origin` both work this run. Window activity: **two new PRs,
#328 and #329**, both Jules "🎨 Palette" PRs — and both, once again, touch `apps/auth-server`. Three
new findings, all pre-merge hygiene / scope-creep / test-quality: **bug-0256** (`autoSignIn:false`
in three PRs), **bug-0257** (#328 triplicates the row-action tooltip work + smuggles a login
rewrite), **bug-0258** (#328 global Tooltip mock). `master` still frozen at `a78d4a7` — **33 days**.

## 🟠 Medium — extract the `autoSignIn: false` auth change (bug-0256)

- [ ] **Decide whether `autoSignIn: false` is wanted.** It disables BetterAuth's auto-session on
      sign-up. If yes, land it **once** on a purpose-named auth PR off `master` with a test asserting
      `POST /api/auth/sign-up/email` returns **no** session cookie and requires an explicit sign-in.
- [ ] **Strip the line from #316, #328, AND #329** — three open PRs add the identical hunk to
      `apps/auth-server/src/auth/auth.config.ts:145` → 3-way conflict. Verify against
      `POST /api/register` and the invite-accept flow, which depend on the session/JWT lifecycle.

## 🟠 Medium — collapse the three-way row-action-tooltip overlap (bug-0257)

- [ ] **Pick ONE carrier for the row-action a11y/i18n label.** #316, #324, and #328 all change the
      identical `aria-label={t('common.moreActions')}` lines in the 5 tables + the same `en/fr`
      keys. **Prefer #324** (smallest, no auth/seed payload — see bug-0254) and **close #328's and
      #316's duplicate UI half.**
- [ ] **Split #328's genuine non-UI improvements into their own PRs with tests:**
      - the `apps/admin/app/login/actions.ts` cookie-threading rewrite (needs a test for the
        `get-session` / `two-factor-status` forwarding path);
      - the `apps/admin-e2e/auth-state.setup.ts` 2FA-prompt skip handling.
- [ ] **Strip `apps/auth-server/**` from #328** (folds into bug-0256).

## 🟢 Low — don't globally stub the design-system Tooltip (bug-0258)

- [ ] **Remove the global `jest.mock('@sassy-auth/ui', …)` Tooltip stub from
      `apps/admin/jest.setup.ts`.** Render the component-under-test inside the **real**
      `TooltipProvider` (a shared `renderWithProviders`) so tests exercise Radix and would fail on a
      missing provider or a broken `asChild` composition.
- [ ] **Add one test that renders the real `Tooltip` + `TooltipTrigger asChild`** to guard the
      composition — this is the coverage that would actually settle **bug-0252** (double-`asChild`).

## ✅ Worth keeping from this window

- [ ] **Land PR #329's `ShareLinkDialog` → `useCopyFeedback` refactor** (after stripping its
      `autoSignIn` rider). It removes the unmount `setTimeout` leak, adds a real dialog test, and is
      correct. It also demonstrates the reactive-`copied` pattern that **bug-0253** wants for the
      *table* copy button — consider applying the same hook there.
- [ ] **The `app-create-drawer.test.tsx` payload-assertion fix** (in both #328 and #329) is the
      **bug-0243** fix — get it onto `master` (via #287 or a fresh standalone PR) so the admin Jest
      suite goes green and starts gating.

## 🔴 Carried forward — still-open blockers (not new this window, unchanged)

- [ ] **bug-0250 (Critical)** — remove the seed 2FA-prompt suppression
      (`twoFactorPromptedAt:new Date()` in `seed.ts` / `demo-multitenant.ts` /
      `demo-resource-server.ts`). Still present in #316/#315. Leave the column null or gate behind an
      explicit `NODE_ENV`-fenced flag; add a test that a seeded admin still hits
      `/login/two-factor-prompt`.
- [ ] **bug-0243** — `apps/admin` Jest suite red on `master` since `a78d4a7`; standalone fix #287
      never merged. CI is not gating on it.
- [ ] **bug-0253** — the native-`title` copy confirmation on the table copy button never shows
      "Copied!" under a stationary pointer (#316 `b3acc18`).

## ⚙️ Process — the real bottleneck (unchanged, worsening)

- [ ] **Merge, or the backlog compounds.** `master` has not advanced in **33 days**. Every Jules PR
      re-derives work an earlier unmerged PR already did — the row-action tooltip concern alone is
      now spread across **three** open PRs (#316/#324/#328), and `autoSignIn:false` across three
      (#316/#328/#329). Triage: land the safe admin-only slices (#324, #329-minus-auth), close the
      duplicates, quarantine the auth/seed-bundling PRs (#303/#306/#315/#316) until bug-0250 is
      stripped.
- [ ] **Add Jules guardrails:** (a) forbid `palette/*` / UI-titled PRs from touching
      `apps/auth-server/**`; (b) skip generating a PR whose file+line set is already covered by an
      open `palette/*` PR; (c) require a real (non-stubbed) assertion for every changed user-facing
      surface.
