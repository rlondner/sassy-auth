# TODO — 2026-06-28

Daily review follow-up items. No new commits for the 10th consecutive day. 9 new bugs found (4 warning, 5 minor) via deeper multi-agent scan across auth-server, admin UI, and E2E tests.

## Priority 1 — Critical bugs (blocking production)

- [ ] **bug-0001** — Org-scope `checkPermission`: add `targetOrgId` parameter and enforcement. 32 days open.
- [ ] **bug-0038** — JWT `permissions` → `scope` breaking change: add version marker or migration path. 25 days open.
- [ ] **bug-0039** — In-memory OAuth code store: migrate to Redis or DB table. 25 days open.
- [ ] **bug-0054** — `redirect_uri` validation bypass: implement origin-matching in the authorize flow. 22 days open.
- [ ] **bug-0074** — Inactive user auth bypass: add `saUser.status` check before issuing JWTs in both OAuth and direct login. 21 days open.
- [ ] **bug-0094** — `checkPermissionForApp` silent grant when `targetAppId` is undefined. 9 days open.

## Priority 2 — New warning bugs (today)

- [ ] **bug-0120** — `DeleteAlertDialog` error div missing `role="alert"`. E2E delete error detection is blind.
- [ ] **bug-0121** — `OrgsService.listOrgs`/`getOrg` cross-tenant listing via `org.users.manage` — no `targetOrgId`.
- [ ] **bug-0122** — Double `stripTrailingSlash` causes JWT `iss` / discovery `issuer` divergence on multi-slash input.
- [ ] **bug-0123** — `role-view-drawer.tsx` shows Edit/Delete to read-only users — no `canWrite` guard.

## Priority 3 — New minor bugs (today)

- [ ] **bug-0117** — `user-create-drawer.tsx` hardcoded English validation messages and "Select org" placeholder. Replace with i18n keys.
- [ ] **bug-0118** — `user-create-drawer.tsx` missing `<form>` wrapper. Enter-to-submit broken. Wrap fields in `<form onSubmit>`.
- [ ] **bug-0119** — `users-table.tsx` dropdown trigger missing `aria-label="more actions"`.
- [ ] **bug-0124** — `user-view-drawer.tsx` "Loading…" hardcoded English string.
- [ ] **bug-0125** — `deleteRole` P2003 error user count can be stale in race conditions.

## Priority 4 — Uncommitted changes

- [ ] Commit the toast/refresh integration (15 admin drawer/table surfaces) — address bug-0112 (French i18n) first.
- [ ] Commit the `resolveIssuer()` DRY refactor and `NEST_GLOBAL_PREFIX` export.
- [ ] Commit the E2E `raceSuccessOrError` scoping fix and OAuth route stub narrowing.
- [ ] Commit the roles `platform.users.manage` read-gate widening.

## Priority 5 — Open warning/minor bugs in uncommitted code

- [ ] **bug-0112** — Translate 15 French toast i18n strings in `fr.json`.
- [ ] **bug-0113** — Fix Playwright workers dead ternary (`CI_TESTS ? 1 : 1` → `CI_TESTS ? 1 : 2`).
- [ ] **bug-0114** — Add `.catch()` to `refresh()` calls in 4 admin tables.
- [ ] **bug-0115** — Add URL validation to `resolveIssuer()`.
- [ ] **bug-0116** — Add `setViewOpen(false)` to users-table delete handler.

## Project health

- **Days since last commit:** 10
- **Days since last fix merged to production:** 32
- **Open bugs:** 123 (was 114)
- **Critical bugs open:** 6
- **Warning bugs open (new today):** 4
- **Uncommitted working tree changes:** 37 files modified/added
