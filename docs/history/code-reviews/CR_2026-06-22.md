# 🔍 Daily Code Review — sassy-auth

**Date:** 2026-06-22
**Default branch:** `master`
**Repo HEAD on master:** `4e06f17` — *feat(auth-server): OAuth AS discovery doc + open orgs/perms reads to platform.users.manage* (2026-06-19 02:23 -0400)

> **Data-source note:** `api.github.com` is unreachable from this run's network (proxy allowlist denies it). PRs/commits below are derived from `git fetch origin '+refs/pull/*/head'` plus the local clone, so "open" here means *head ref not yet contained in `origin/master`*. PRs closed-without-merge cannot be distinguished from genuinely open ones without API access. Treat older entries with that caveat.

---

## Recent Commits (last 24h)

**None.** No commits landed on `origin/master` since 2026-06-21T13:52:45Z. The last master activity is the 06-19 OAuth AS discovery feature commit.

---

## Open PRs — Activity in Last 24h

### PR #136 — 🎨 Palette: Standardized accessible table row actions with tooltips
- **Author:** google-labs-jules[bot]
- **Branch:** `palette/table-action-tooltips-v2-2538777647389807112`
- **Head:** `8d28d22` (2026-06-22 08:29 UTC) — within the 24h window
- **Files changed:** 17 · **+129 / -42**
- **Scope:** Adds `Tooltip` + `aria-label="More actions"` wrapper around the row-action `…` button in all five admin tables (apps, orgs, permissions, roles, users); mounts `<TooltipProvider>` at root layout; adds `common.moreActions` / `common.copy` / `common.copied` strings to en + fr message catalogs; globally mocks `@sassy-auth/ui` Tooltip primitives in `jest.setup.ts`; adds a workspace build step before e2e and drops the hardcoded `pnpm version: 9` pin; switches Prisma `$transaction` callback typings from inferred to `any` in `seed.ts`.

**Findings**

| Sev | Area | Finding |
|---|---|---|
| 🔴 | Type safety / seed | `seed.ts` flips five `prisma.$transaction(async (tx) => …)` callbacks to `async (tx: any) => …`. This is a regression masking a real typing issue rather than fixing it — Prisma's `Prisma.TransactionClient` is the correct type. Seeds run against production-like data; losing `tx` typing means any silent schema drift won't fail compilation. Push back: require the bot to import `Prisma.TransactionClient` (or use `Parameters<typeof prisma.$transaction>[0]`) instead of `any`. |
| 🟡 | Test infra | The new global `jest.mock('@sassy-auth/ui', …)` in `jest.setup.ts` replaces Tooltip primitives across **all** admin unit tests. Tests that already `jest.mock('@sassy-auth/ui', …)` per-file will override it (fine), but tests that import other named exports from `@sassy-auth/ui` after `jest.resetModules()` will now get the mocked module instead of the actual one. Confirm the `jest.requireActual` spread truly preserves identity of non-Tooltip exports across `resetModules`. |
| 🟡 | CI | `.github/workflows/e2e.yml` drops `version: 9` from `pnpm/action-setup` — works only if `packageManager` in `package.json` is set. Verify (`package.json` does declare `"packageManager": "pnpm@…"`), otherwise the workflow will install whatever pnpm default the action picks. |
| 🟡 | i18n consistency | fr.json finally translates `save/edit/delete/confirm` (previously left as English copies). Good fix, but cherry-picked into a PR titled "tooltips" — splits unrelated change into the same atomic merge. |
| 🟢 | A11y | Wrapping the `…` button in `<Tooltip>` + adding `aria-label={t('common.moreActions')}` is the right pattern for icon-only triggers. `<TooltipProvider>` mounted at root is correct for Radix. |
| 🟢 | Repo hygiene | `.Jules/palette.md` added (3 lines) — harmless bot scratchpad; consider gitignoring `.Jules/`. |

**Recommendation:** **Request Changes.** Block on the `tx: any` regression in `seed.ts`. The accessibility/tooltip work itself is mergeable.

---

## Open PRs — Older but Material

### PR #135 — 🎨 Palette: Implement tooltips, enhance localization, and fix CI
- **Author:** google-labs-jules[bot] · **Head:** `f6dad92` (2026-06-20)
- **Status:** Superseded by **PR #136** (same scope, +60 fewer lines, cleaner table diffs). Close #135.

### PRs #117–#134 — Bug-description placeholders (18 PRs, 06-19 11:19 EDT)
Each PR adds exactly one file under `bugs/bug-00NN.md` and nothing else. They document findings awaiting fixes:

| PR | Bug | Severity | Where |
|---|---|---|---|
| #117 | bug-0094: `checkPermissionForApp` silently grants access when `targetAppId` is undefined | 🔴 **Critical** | `auth-server/src/common/permissions/check-permission-for-app.ts:51` |
| #118 | bug-0095: TOCTOU race — user org/app membership not re-validated at code exchange | 🟡 Warning | `auth-server/src/token/token.controller.ts:107-116, 207-213` |
| #119 | bug-0096: `updatePermission` allows renaming to `platform.*` prefix | 🟡 Warning | `auth-server/src/permissions/permissions.service.ts:165-170` |
| #120 | bug-0097: `removeRole` missing escalation guard | 🟡 Warning | (per file) |
| #121 | bug-0098: migration `isSystem` scope | 🟡 Warning | (per file) |
| #122 | bug-0099: migration `publicId` constraint | 🟡 Warning | (per file) |
| #123 | bug-0100: placeholder `publicId` | 🟡 Warning | (per file) |
| #124 | bug-0101: scenario server teardown | 🟢 Info | (per file) |
| #125 | bug-0102: grant-ceiling test order dependency | 🟢 Info | (per file) |
| #126 | bug-0103: permissions-table `canWrite` gap | 🟡 Warning | (per file) |
| #127 | bug-0104: users-table org filtering | 🟡 Warning | (per file) |
| #128 | bug-0105: permission-drawer error swallowing | 🟡 Warning | (per file) |
| #129 | bug-0106: layout notFound redirect | 🟢 Info | (per file) |
| #130 | bug-0107: roles-page raw error | 🟢 Info | (per file) |
| #131 | bug-0108: e2e race false positive | 🟢 Info | (per file) |
| #132 | bug-0109: playwright workers ternary | 🟢 Info | (per file) |
| #133 | bug-0110: updateRole null check | 🟡 Warning | (per file) |
| #134 | bug-0111: me.controller unsafe cast | 🟡 Warning | (per file) |

**Findings**
- 🟢 Diffs are 10-line markdown adds — zero risk, mergeable as docs.
- 🟡 **Process:** 18 separate PRs for one batch of documentation is noisy. Either fast-merge them all (squash into a single `docs(bugs): catalog bugs 0094–0111` commit) or convert to a single PR.
- 🔴 **Action required outside this review:** bug-0094 is described as Critical — schedule the fix immediately. The bypass condition (`targetAppId === undefined` → early return granting access) is an auth boundary failure and should be patched before any further auth-server release.

**Recommendation:** **Approve & merge** (or squash-consolidate). Then file fix PRs starting with bug-0094.

### PRs #110–#113 — Chore placeholders (06-18 09:17 EDT)
Empty `chore: placeholder for fix/bug-009X-…` commits with no file changes. These look like accidental empty-branch PRs from a worktree workflow.

- 🟡 **Action:** close without merge; the corresponding `fix/bug-009X-*` branches don't have any code yet.

---

## Open PRs — Stale Jules-Bot Backlog

The `palette/*` and `palette-*` branch family contains ~13 unmerged duplicate PRs (#22, #25, #35, #42, #44, #57, #62, #99–#108) all attacking the same scope: button loading state, then tooltips, then localized row actions. Each day the bot opens a fresh PR re-doing the same work. PR #136 is the latest iteration.

- 🟡 **Action:** close all earlier `palette/*` PRs (keep #136), and adjust the bot's playbook to **update** the existing PR rather than open a new branch each run. This is exactly what bug-0090 (`jules-duplicate-prs`) flags.

---

## Summary

**Items needing attention (prioritized)**
1. 🔴 Land a fix for **bug-0094** (`checkPermissionForApp` bypass) — auth boundary, Critical.
2. 🔴 Reject `tx: any` regression in PR #136's `seed.ts` change; require typed `Prisma.TransactionClient`.
3. 🟡 Consolidate or fast-merge PRs #117–#134 (bug documentation) to clear the queue.
4. 🟡 Close superseded Jules palette PRs (#22–#135 except #136) and fix the bot to update-in-place (bug-0090).
5. 🟡 Close placeholder-only PRs #110–#113.
6. 🟡 Schedule fixes for bug-0095 (OAuth TOCTOU) and bug-0096 (platform.* rename bypass) — both have clear, small fixes already specified in the bug descriptions.

**Overall repo health:** Master is quiet (no commits in 24h); the open-PR list is dominated by bot-generated duplicates and self-filed bug-doc placeholders — backlog hygiene, not engineering risk, is today's main signal.
