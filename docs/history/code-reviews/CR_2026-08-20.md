# 🔍 Daily Code Review — sassy-auth

**Date:** 2026-08-20

*(Supersedes an earlier same-day stub at `docs/history/code_reviews/CR_2026-08-20.md`,
written under the wrong directory name while GitHub API access was unavailable.
This run has working `gh` access via the OS keyring.)*

## Environment

`gh auth status` and `git fetch`/`git push` both worked (OS-keyring auth,
account `rlondner`). Local `master` was 17 commits behind `origin/master`
(stale since the 2026-08-19 review); fast-forwarded to `7a56c4e` before
reviewing.

## Commits in the last 24 hours

**None on `master`.** The last commit is `7a56c4e` (2026-08-18 21:08 UTC),
already covered by the 2026-08-19 review. `git log --all --since="24 hours
ago"` shows only the daily-review housekeeping commits from that prior run.

## New PR activity in the last 24 hours

- **#340** — 🎨 Palette: Standardize loading state in ConfirmDialog
  (`google-labs-jules[bot]`, co-author `rlondner`), opened 2026-08-20 07:10 UTC.
  Files: `packages/ui/src/components/confirm-dialog.tsx` (+2/-2),
  `packages/ui/src/__tests__/confirm-dialog.test.tsx` (+5/-2).
  - **Findings:** 🟢 Clean. Replaces `disabled={pending}` + `{pending ? '…' :
    label}` with `loading={pending}` on `AlertDialogAction`. Verified against
    `packages/ui/src/components/ui/alert-dialog.tsx` and `ui/button.tsx`:
    `loading` already sets `disabled={loading || disabled}`, renders a
    `Loader2` spinner, and sets `aria-busy="true"` — this PR only adopts an
    existing, already-wired prop. Test updated to assert both `disabled` and
    `aria-busy`. No `apps/auth-server` or seed changes bundled, unlike the
    #303/#306/#316/#328/#329 lineage.
  - **Recommendation:** Approve, merge as-is.

## CI health check

`gh run list --branch master --workflow e2e` — still **red**. Latest run
(`32186112882`, on `7a56c4e`) fails at the same `auth-state.setup.ts` step as
the 2026-08-19 review found (bug-0259): every seeded admin gets stuck on
`/login/two-factor-prompt`. The fix exists (PR #335) but is unmerged, so
`master` itself hasn't changed.

**New finding this run:** every sampled `admin-e2e` run in the last 48 hours
(6/6 checked, across `master` and several PR branches) additionally fails to
upload its Playwright report/traces —
`Failed to CreateArtifact: Artifact storage quota has been hit`. Filed as
**bug-0263** (fix: [PR #341](https://github.com/rlondner/sassy-auth/pull/341),
shortens `retention-days` 14→3 in `.github/workflows/e2e.yml`). This
degrades debuggability for every future red run, including the ongoing
bug-0262 investigation.

**Investigated and ruled out:** whether bug-0259's fix is itself flaky. A run
on `bugs/bug-0262` reproduced the pre-fix stuck-on-interstitial symptom, which
looked like a regression at first — but that branch forks from `master`
directly, not from `bugs/bug-0259`, so it never had the fix. Verified via
`git log --oneline` ancestry before concluding anything. No new bug filed for
this (see BUGS_2026-08-20.md's "Not filed" section for the full trace).

## Summary

- **Items needing attention:**
  1. **Merge backlog is now 70 open PRs** (oldest 41 days) — see
     [TODO_2026-08-20.md](../todo/TODO_2026-08-20.md) for a priority-ordered
     merge list. This is the dominant risk in the repo right now, more than
     any single code defect.
  2. Merge #335 (bug-0259) to get real e2e signal back on `master`.
  3. Merge #341 (bug-0263, filed today) — low-risk, unblocks CI artifact
     uploads.
- **Repo health:** 🟡 Quiet code day (one clean PR, zero risky changes), but
  the structural problem — reviews outrunning merges — continues to compound.
