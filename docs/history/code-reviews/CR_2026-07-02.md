# 🔍 Daily Code Review — sassy-auth

**Date:** 2026-07-02
**Reviewer:** Automated daily review

> **Note:** The sandbox proxy blocked `api.github.com`, so open-PR detection was performed from local git refs (`origin/pr/*`) after `git fetch`. "Open" is inferred as branches whose tip is not reachable from `origin/master`; a small subset may be closed-without-merge. Top 10 by most recent commit are covered below.

---

## Open PRs (10 reviewed of ~26 unmerged `pr/*` refs)

### 🟢 PR #158 — `docs: add bug-0135 description (user-create-drawer stale role IDs on org switch)` — Raphael Londner
- Files: 1 (+26 / -0) — `bugs/bug-0135.md`
- Findings: Docs-only. Bug description is accurate: `useEffect` on `orgId` change refetches roles but does not reset `roleIds` / `directPermissionIds`, which can cause silent submission of stale cross-tenant IDs. **Severity of the underlying bug is at minimum 🟡 Warning, not "Info"** — a stale roleId from another org submitted to the create endpoint should be rejected by tenant-isolation checks, but if any codepath resolves roles by publicId without re-checking `appId/orgId` it is a cross-tenant privilege risk. Recommend re-classifying to Warning.
- Recommendation: **Approve** (docs), and open the follow-up fix PR promptly.

### 🟢 PR #157 — `docs: add bug-0134 description (permissions matrix missing apps.get)` — Raphael Londner
- Files: 1 (+30 / -0) — `bugs/bug-0134.md`
- Findings: Docs-only.
- Recommendation: **Approve**.

### 🟢 PR #156 — `docs: add bug-0133 description (roles spec double-await rejects)` — Raphael Londner
- Files: 1 (+31 / -0) — `bugs/bug-0133.md`
- Findings: Docs-only.
- Recommendation: **Approve**.

### 🟢 PR #155 — `docs: add bug-0132 description (user-create-drawer error i18n)` — Raphael Londner
- Files: 1 (+22 / -0) — `bugs/bug-0132.md`
- Findings: Docs-only.
- Recommendation: **Approve**.

### 🟢 PR #154 — `docs: add bug-0131 description (raceSuccessOrError unscoped success check)` — Raphael Londner
- Files: 1 (+32 / -0) — `bugs/bug-0131.md`
- Findings: Docs-only. Bug is real (E2E success text matched anywhere on page → false positives). The proposed fix in the doc is correct.
- Recommendation: **Approve**.

### 🟡 PR #153 — `🎨 Palette: Enhanced table UX, fixed CI workflow, and TypeScript errors` — google-labs-jules[bot]
- Files: 19 (+218 / -104)
- Findings:
  - 🔴 **Type-safety regression (blocker):** All 8 `prisma.$transaction(async (tx: Tx) => …)` sites in `apps/auth-server/src/roles/roles.service.ts`, `apps/auth-server/src/users/users.service.ts`, and `apps/auth-server/src/seed/seed.ts` were rewritten as `async (tx: any) => …`. This silences the underlying Prisma tx-client typing issue rather than fixing it and removes compile-time checks on all `tx.saRole` / `tx.saUser` / `tx.saPermission` calls inside transactions — exactly the paths that mutate RBAC/tenant data. Do not merge as-is. Correct fix is either `type Tx = Prisma.TransactionClient` or the existing `Parameters<Parameters<typeof prisma.$transaction>[0]>[0]` alias; if the inference is failing, address the root cause (Prisma version mismatch or generic tx callback signature) rather than casting to `any`.
  - 🟡 **CI reproducibility regression:** `.github/workflows/e2e.yml` drops `with: version: 9` on `pnpm/action-setup@v4`. If `packageManager` in root `package.json` is not present or is a range, the runner will resolve to the latest pnpm and drift. Verify `packageManager` pinning and, if not present, keep the explicit `version: 9`.
  - 🟢 A new `Build workspace packages` step (`pnpm exec turbo build --filter=!@sassy-auth/auth-server`) is added before Prisma generate. Reasonable; confirm `!@sassy-auth/auth-server` exclusion is intentional (auth-server likely built separately downstream).
  - 🟢 UI/A11y changes across 5 tables (`apps`, `orgs`, `permissions`, `roles`, `users`) wrap copy-id and "more actions" buttons in `Tooltip`, add `focus-visible:ring-2` focus states, and route labels through i18n keys (`common.copy`, `common.copied`, `common.moreActions`). Root `layout.tsx` now mounts a `TooltipProvider`. Tooltip mocks added to all 5 Jest table tests. No behavioral concerns.
  - 🟡 Half of the changed `common.*` i18n keys land in `en.json` (+5) and `fr.json` (+11). Confirm no missing keys in `fr.json` for the new labels; delta suggests some FR translations are placeholders.
- Recommendation: **Request Changes** — revert `Tx → any` in auth-server before merge; verify pnpm pinning and i18n parity.

### 🟢 PR #152 — `fix(bug-0125): Fix stale user count in deleteRole P2003 error message` — Raphael Londner
- Files: 1 (+4 / -0) — `bugs/bug-0125.md`
- Findings: Docs-only description in this branch's delta vs master. If a code fix is also intended, it is not in the unmerged delta — verify on GitHub whether the actual fix commit is missing or already merged separately.
- Recommendation: **Approve** if intent is docs-only; otherwise **Needs Discussion**.

### 🟢 PR #151 — `fix(bug-0124): Replace hardcoded Loading text with i18n key in user-view-drawer` — Raphael Londner
- Files: 1 (+4 / -0) — `bugs/bug-0124.md`
- Findings: Docs-only delta. Same caveat as PR #152 — verify whether code fix is present in a separate commit or PR.
- Recommendation: **Approve** if docs-only.

### 🟢 PR #150 — `fix(bug-0123): Add canWrite guard to role-view-drawer Edit/Delete buttons` — Raphael Londner
- Files: 1 (+4 / -0) — `bugs/bug-0123.md`
- Findings: Docs-only delta. Underlying bug (missing `canWrite` guard on privileged actions in view drawer) is a 🟡 client-side gating gap — server-side ABAC must remain the enforcement point; confirm write endpoints reject unauthorized callers regardless of UI state.
- Recommendation: **Approve** docs; ensure server-side authz is verified in the code fix.

### 🟢 PR #149 — `fix(bug-0122): Fix double stripTrailingSlash causing JWT/discovery issuer divergence` — Raphael Londner
- Files: 1 (+4 / -0) — `bugs/bug-0122.md`
- Findings: Docs-only delta. Underlying issue (issuer divergence between JWT claim and OIDC discovery document) is a 🔴 **Critical** OIDC-conformance / RP-validation failure — mismatched `iss` will break every relying party doing strict discovery-based validation. Priority for the actual code fix.
- Recommendation: **Approve** docs; escalate the underlying fix.

---

## Recent Commits (last 24h on `origin/master`)

**None.** Most recent `origin/master` commit is `776833a` (2026-06-30 02:34 UTC — `docs: document public POST /api/register + rate-limit env`), > 24h old. The local `master` HEAD is `37f738a` (2026-07-01, ahead of `origin/master`) — feature/admin work not yet pushed. No push-to-master activity in the review window.

---

## Summary

Items needing attention (prioritized):

1. 🔴 **PR #153 — revert `Tx → any` in auth-server transactions.** Type erasure on RBAC/tenant mutation paths is unacceptable for enterprise-grade code. Root-cause the Prisma tx typing issue instead.
2. 🔴 **Bug-0122 (PR #149 docs) — OIDC issuer divergence.** Priority code fix; downstream RPs may reject tokens.
3. 🟡 **PR #158 / bug-0135 — reclassify from Info to Warning.** Stale cross-org roleIds submitted through a form is at minimum a defense-in-depth concern.
4. 🟡 **PR #153 CI change — verify pnpm version pinning** (`packageManager` field or explicit `version: 9`).
5. 🟡 **PRs #149–152 docs-only deltas** — confirm the actual code fixes exist elsewhere; otherwise these bugs are only documented, not fixed.
6. 🟡 **PR #153 i18n parity** — audit `fr.json` for new `common.*` keys.

**Overall repo health:** Active bug-triage cadence and disciplined docs-first workflow, but the Jules bot PR (#153) mixes a legitimate UX/a11y improvement with a type-safety regression in security-critical code paths — do not fast-track.

<run-summary>Reviewed 10 unmerged PRs (5 new docs-only PRs #154–158 from yesterday plus #149–153); no master activity in last 24h. Main finding: PR #153 replaces typed Prisma transaction clients with `any` across auth-server RBAC paths (blocker), and 5 new bug descriptions were filed but the corresponding code fixes are not visible in the unmerged deltas of PRs #149–152.</run-summary>
