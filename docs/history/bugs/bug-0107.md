# bug-0107: roles/page.tsx throws raw allSettled rejection to error boundary

**Severity:** Warning
**File:** `apps/admin/app/(admin)/roles/page.tsx` (lines 34-35)

`throw listResult.reason` / `throw appsResult.reason` throws the raw rejection which may contain internal API details (e.g., `"API error 403: /api/roles"`) surfaced in the error boundary.

**Fix:** Wrap the rejection in a user-friendly error or redirect to an access-denied panel.

**Tests needed:** Simulate a 403 from `getRoles` and verify the error boundary shows a generic message.
