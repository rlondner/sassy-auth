# bug-0260: PR #334 is a fourth duplicate of the row-action-tooltip/`common.moreActions` lineage — and now conflicts with `master`'s own accessibility fix

**Severity:** Medium (merge-blocked duplication, escalating)
**Status:** Open
**Fixed:** false
**Introduced by:** PR **#334** (`palette/table-actions-tooltip-a11y-5050206329042661731`, Jules bot) — opened 2026-08-18 07:32 UTC.

## Description

PR #334 ("🎨 Palette: Add localized Tooltips and accessible ARIA labels to table row
action buttons") re-does, for the **fourth** time, work already open in three other
PRs:

| PR | Opened | Same `apps-table.tsx` / `orgs-table.tsx` / `permissions-table.tsx` / `roles-table.tsx` / `users-table.tsx` `aria-label={t('common.moreActions')}` + `en.json`/`fr.json` change? |
|----|--------|---|
| #316 | 2026-08-13 | Yes |
| #324 | 2026-08-15 | Yes (flagged as duplicate of #316 — [bug-0254](./bug-0254.md)) |
| #328 | 2026-08-16 | Yes (flagged as triplicate — [bug-0257](./bug-0257.md)) |
| **#334** | **2026-08-18** | **Yes — quadruplicate** |

This continues the lineage [bug-0254](./bug-0254.md) → [bug-0257](./bug-0257.md)
without any of the prior three having merged or been closed.

**New this round:** `master` advanced past `a78d4a7` today (see today's
CHANGELOG/TODO — the 34-day freeze broke) and one of the 17 newly-landed commits,
`5e1ade4` ("fix(bug-0225,bug-0226): give table search boxes and page-size selects
accessible names"), directly edits the exact same five table components
(`apps-table.tsx`, `orgs-table.tsx`, `permissions-table.tsx`, `roles-table.tsx`,
`users-table.tsx`) plus `en.json`/`fr.json`. It is a different accessibility concern
(search-input and page-size-select names, not the row-action tooltip), but it
touches the same files closely enough that GitHub now reports **all four** of
#316/#324/#328/#334 as `mergeable: CONFLICTING` against current `master` — confirmed
via `gh pr view <n> --json mergeable,mergeStateStatus` for each.

## Why this is a bug (and not just noise)

- Four independent Jules runs have now produced near-identical diffs for the same
  UI change with no coordination, and none can land without a manual conflict
  resolution — against each other **and now against `master` itself**.
- Continuing to let Jules retry this task will only produce a fifth duplicate.

## Files

- `apps/admin/components/{apps,orgs,permissions,roles,users}-table.tsx`
- `apps/admin/messages/{en,fr}.json`

## Suggested fix

- Close #316, #324, and #328 as superseded; pick **one** carrier (recommend #334,
  the most recent, since it's the smallest and cleanest of the four per prior
  reviews) and rebase it onto current `master` to resolve the conflict with
  `5e1ade4`.
- Stop re-queuing this Jules task — the palette backlog task description evidently
  keeps re-triggering the same work item. Add a check (or pause the recurring task)
  before it produces a fifth copy.

## Verification

- `gh pr list --repo rlondner/sassy-auth --state open --json number,title,headRefName,createdAt` — confirms #316/#324/#328/#334 all open, all touching the same file set (compared via `gh pr view <n> --json files`).
- `gh pr view 334 328 329 316 324 --json mergeable,mergeStateStatus` (after `git fetch origin` to sync GitHub's merge-state cache) — all four of #316/#324/#328/#334 report `"mergeable":"CONFLICTING"`, `"mergeStateStatus":"DIRTY"` against `master` at `7a56c4e`.

## Notes

No code fix accompanies this PR — the defect lives entirely in the four still-open,
unmerged Jules PRs, not in `master`. Consistent with how [bug-0254](./bug-0254.md)
and [bug-0257](./bug-0257.md) (the same class of finding) were handled: this
writeup is the deliverable. Related: [bug-0254](./bug-0254.md), [bug-0257](./bug-0257.md).
