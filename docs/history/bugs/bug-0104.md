# bug-0104: users-table.tsx initialOrgId and canPickOrg accepted but voided

**Severity:** Warning
**File:** `apps/admin/components/users-table.tsx` (lines 30-31)

`UsersTable` accepts `initialOrgId` and `canPickOrg` in its props interface, but both are immediately discarded with `void`. No org picker UI and no server-side org filtering.

**Fix:** Wire `initialOrgId` into the `getUsers` fetch as a default filter. Add an org picker dropdown gated behind `canPickOrg`.

**Tests needed:** E2E test: as a tenant admin, verify the users table shows only users from the admin's own org.
