# Bugs — 2026-09-14 daily review

Zero commits landed on `dev` in the last 24 hours (or since 2026-09-10,
`ccf2cea` — see `TODO_2026-09-14.md`), so this run had no new diff to review.
Instead reviewed `dev`'s current typecheck state directly, since three of the
nine currently-open PRs are docs/CI-only and still show a red `typecheck`
check with no code changes of their own to explain it.

Two bugs found and fixed, both with normal fix PRs against `dev`.

---

### bug-0288 🟠 High — `App` mock literals in 7 admin test files still missing password-policy fields

**Fixed:** false (PR open, unmerged as of this review)

**Files:**
- `apps/admin/components/__tests__/app-view-drawer.test.tsx`
- `apps/admin/components/__tests__/org-create-drawer.test.tsx`
- `apps/admin/components/__tests__/orgs-table.test.tsx`
- `apps/admin/components/__tests__/permission-create-drawer.test.tsx`
- `apps/admin/components/__tests__/permissions-table.test.tsx`
- `apps/admin/components/__tests__/role-create-drawer.test.tsx`
- `apps/admin/components/__tests__/roles-table.test.tsx`

**Description:** `App`'s `passwordPolicyOverride`/`effectivePasswordPolicy`
fields (added 2026-09-05, `430f870`) are required but absent from 14 `App`
literals across these 7 test files. bug-0283 (PR #380, still unmerged) fixed
the one production call site with the same gap but didn't touch test
fixtures.

**Impact:** This is the actual reason `typecheck` (and therefore
`admin-e2e`, which depends on a successful build) has been red on every one
of the 9 currently open PRs against `dev`, including PRs that touch none of
these files — `dev`'s own baseline fails `typecheck` regardless of what a PR
changes. See `TODO_2026-09-14.md`.

**Fix:** PR #388. Added `passwordPolicyOverride: null, effectivePasswordPolicy: FALLBACK_PASSWORD_POLICY`
to every affected literal, matching bug-0283/PR #380's pattern. No suite
asserts on these fields. **Verification:** grep-audited every `App` literal
in the 7 files post-fix; not run through `pnpm typecheck` locally (worktree
has no `node_modules` installed) — CI on the PR will confirm.

---

### bug-0289 🟡 Medium — `User` mock literals missing `createdAt`/`lastLoginAt` in 2 admin test files

**Fixed:** false (PR open, unmerged as of this review)

**Files:**
- `apps/admin/components/__tests__/user-view-drawer.test.tsx`
- `apps/admin/components/__tests__/users-table.test.tsx`

**Description:** Same failure class as bug-0288 — `User`'s
`createdAt`/`lastLoginAt` fields (made required by bug-0186) are missing
from 4 `User` literals in these 2 files, last touched 2026-09-04
(`bd24b46`), predating the password-policy work.

**Impact:** Same as bug-0288 — contributes to `typecheck` failing on `dev`'s
own baseline.

**Fix:** PR #389. Added fixed ISO-string `createdAt`/`lastLoginAt` values to
the 4 literals. **Verification:** same as bug-0288 — grep-audited, not
locally typechecked (no `node_modules`); CI will confirm.

---

## Verified — not filed

- **`bug-0283`'s fix PR (#379) shows `mergeable: CONFLICTING`** against
  current `dev`. Investigated: `apps/admin/app/signup/page.tsx` (the file
  #379 touches) has moved on since #379 was opened on 2026-09-08 — the
  2026-09-08 19:44 commit `d207928` ("add check-email/resend page, redirect
  signup there on success") and later commits reworked the same file. Not
  filed as a new bug — #379 already exists and tracks bug-0282; the
  conflict just needs a rebase by whoever picks it up. Called out in
  `TODO_2026-09-14.md` since it's blocking, not because it's a new defect.
- **9 open PRs, all red on the same 3 checks** (`typecheck`, `admin-e2e`,
  `Trivy SCA & Vulnerability Scan`) regardless of each PR's own diff.
  Traced `typecheck`'s failure on two representative PRs (#386, #387) to
  the exact same `roles/page.tsx` error bug-0283 already documents — this
  is process fallout from unmerged fix PRs, not a new bug per PR. Not
  independently investigated: the `Trivy SCA & Vulnerability Scan` failure
  (separate from the `Trivy` check, which passes) — worth a follow-up look
  since it's failing identically across every open PR too and wasn't
  traced to a root cause this run.
- **Port-3000→3010 migration (`ccf2cea`)** — grepped the full tree for
  leftover `localhost:3000`/`:3000` references outside of unrelated numeric
  literals (a rate-limit test constant, a Jest timeout, and static design
  mockups under `designs/`). Migration is clean; nothing filed.
