# 🔍 Daily Code Review — sassy-auth — 2026-08-27

**Access:** `gh auth status` healthy (OS keyring, `repo`/`workflow` scopes),
`git fetch`/`git push`/`gh pr create` all worked throughout this run.

## Commit activity

Zero commits landed on `master` in the last 24 hours — `master` has been at
`ea82b95` since 2026-08-25 00:49 EDT. The 2026-08-25 review bundle (PR #359)
and its two bug-fix PRs (#357, #358) are still open, as are seven earlier
daily-review PRs back to 2026-08-24 (#349–#355). Open-PR count: 10.

**Superseded a stale wrong-path stub:** an earlier same-day attempt (~07:36,
before this run had `gh`/API access) wrote `docs/history/code_reviews/CR_2026-08-27.md`
(underscore — wrong; correct is the hyphenated `code-reviews/` used since the
2026-08-19 reorg). It reported the same proxy-403 / rejected-PAT failure mode
documented on 2026-07-20 through 2026-08-25 and found nothing beyond that —
this run's `gh` access worked fine, so the stub was superseded rather than
merged in; nothing in it was worth carrying forward.

## CI health check

With no new commits to diff, this run's finding came from checking `master`'s
live CI state instead — the standing step added 2026-08-19 after the e2e
suite was found silently red for 34 days with nobody watching. `gh run list
--branch master` showed:

| Job | Status | Since |
|---|---|---|
| `typecheck` | ✅ success | 2026-08-25 |
| `e2e` | ✅ success | 2026-08-25 |
| `unit-tests` | 🔴 **failure** | 2026-08-25 04:33 UTC — 2+ days |
| `Dependency & CVE Scan (Trivy)` | ✅ success | 2026-08-27 (daily cron) |

Traced the `unit-tests` failure to two independent, unrelated causes in the
same job, both filed and fixed (see `BUGS_2026-08-27.md` for full writeups):

- **bug-0273** (Critical): `packages/ui`'s branch-coverage floor (51%, set
  2026-08-24) has been unmet since `2c888ba`+1 day — the baseline
  verification missed an already-untested first-party file
  (`form-field.tsx`). Fixed with a direct test, not a threshold change.
- **bug-0274** (Low): the same day's Node 24 migration commit bumped three of
  four Node-version sites and missed `unit-tests.yml`.

Both were **verified against a full local run** of every suite the workflow
executes (`auth-server`, `admin`, `ui`, `db`), not just read and reasoned
about.

## Docs

Fixed a stale Node version reference found while investigating bug-0274:
README's Prerequisites section and badge both still said `Node.js >= 20`
despite the 2026-08-25 migration to `>=24.0.0`. Also added a `unit-tests`
badge alongside the existing `e2e`/`typecheck` ones, since master's own
badge row had no way to surface the exact failure this review found.

## Escalation

The open-PR backlog (10) is climbing again from the ~7 low seen on
2026-08-24-morning. Recommend a merge pass on #349–#361 before the next
daily review — see TODO for detail; none appear to conflict on files.
