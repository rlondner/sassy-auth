# Daily Code Review — sassy-auth

**Date:** 2026-06-18
**Reviewer:** Automated (Raphael's scheduled task)
**Master HEAD:** `ae9772f` (2026-06-02) — no new master commits in last 24h
**Note on methodology:** `api.github.com` is blocked by the sandbox egress proxy, so the GitHub REST API was not reachable. PR enumeration was performed via `git fetch` using the PAT from `.mcp.json` over `github.com` (which is allowlisted) and the `refs/pull/*/head` refspec. "Open" status is inferred from the presence of the source branch ref on `origin` (deleted source branches usually indicate a closed/merged PR). Cross-check on github.com is recommended.

---

## Open PRs (10 reviewed, ~15+ likely open)

All 10 reviewed PRs are authored by `google-labs-jules[bot]` and target `origin/master`. They are highly redundant — multiple competing PRs implementing the same two themes (Button/Dialog loading-state standardization and table row-action tooltips). Stack-rank below by recency.

### PR #109 — Palette: Standardize user table actions with tooltips and ARIA labels
- **Base:** `ae9772f` · **Ahead:** 3 commits · **Files:** 7 · **+34 / −11**
- **Branch:** `palette/table-row-actions-15663218071847066241`
- **Last update:** 2026-06-18 07:55 UTC (today)
- **Scope (good):** Adds `TooltipProvider` to admin root layout, wraps user-table row-actions trigger with `Tooltip` + `aria-label={t('common.moreActions')}`, adds i18n keys (en/fr), updates `users-table.test.tsx` to wrap with `TooltipProvider`. The `.Jules/palette.md` learning note correctly identifies the `TooltipProvider` test-stability gotcha.
- **Findings:**
  - 🟡 **CI hack — drops pnpm version pin.** `e2e.yml`: removes `with: version: 9` from `pnpm/action-setup@v4`. Without an explicit pin or `packageManager` field hand-off, action picks an unbounded version → non-reproducible CI. The "fix" is the bot working around a previous mismatch instead of repairing it. Verify `package.json#packageManager` is set to `pnpm@9.x` at the repo root; otherwise re-pin.
  - 🟡 **Selective build filter.** `pnpm exec turbo build --filter=!@sassy-auth/auth-server` skips the NestJS app build before e2e. If e2e exercises auth-server endpoints, this could mask compile-time regressions caught only at runtime — and contradicts the e2e suite's purpose. Confirm e2e runs auth-server via `pnpm dev` rather than the build artifact; if so, the filter is fine but should be commented.
  - 🟢 **i18n note:** `fr.json` has `"moreActions": "Plus d'actions"` — properly translated (unlike the surrounding placeholder strings `"save": "Save"` etc., pre-existing).
  - 🟢 i18n FR string is correctly escaped (`d'actions`).
- **Recommendation:** Request Changes — re-pin pnpm version, or drop the workflow change and rebase. Otherwise mergeable as the UX target.

### PR #108 — Palette: Standardize table row actions with tooltips and localized labels
- **Base:** `ae9772f` · **Ahead:** 4 commits · **Files:** 17 · **+135 / −38**
- **Branch:** `palette-table-row-actions-ux-16458577643731107590`
- **Findings:**
  - 🟡 Same `e2e.yml` pnpm-pin removal as #109.
  - 🟡 **Scope creep.** Modifies `apps/auth-server/package.json` (`seed` script gets `--transpile-only`) in a PR labeled as UI palette work. `--transpile-only` silences ts-node type-checking during seed — masks real Prisma type errors at exactly the moment the seed is most likely to break. Reject this hunk; if the seed is failing type-checks, fix the underlying types.
  - 🟢 Extends `TooltipProvider` wrapping to apps/orgs/permissions/roles/users tables consistently.
- **Recommendation:** Request Changes — remove auth-server package.json change; fix CI pnpm pin.
- **Likely supersedes** #106, #107 (same target).

### PR #107 — Palette: Add tooltips to row actions and fix CI robustness
- **Base:** `ae9772f` · **Ahead:** 4 commits · **Files:** 22 · **+128 / −71**
- **Branch:** `palette/table-action-tooltips-11423700997441048801`
- **Findings:**
  - 🔴 **Type-safety regression in backend services.** Adds `(x: any)` to callback parameters across:
    - `common/permissions/check-permission.ts` — RBAC/ABAC critical path. Untyped `.forEach((ur: any) => ur.role.permissions.forEach((rp: any) => …))` removes compile-time guarantees on the permission-name aggregation that the auth check depends on. A schema rename later silently breaks the permission set.
    - `permissions.service.ts`, `roles.service.ts`, `users.service.ts`, `invitations.service.ts`, `seed/seed.ts`.
    The real fix is `Prisma.SaUserGetPayload<{ include: typeof USER_INCLUDE }>` or generated types — not `any`. Same antipattern: `let saUser: any;` in `users.service.ts` (replacing a properly-typed `Prisma.SaUserGetPayload`).
  - 🔴 **`--transpile-only` on the seed.** Same as #108 — hides type errors in privileged seeding code (creates platform super-admin, grants `platform.*` perms). Seed regressions here = silent auth-grant defects.
  - 🟡 e2e.yml pnpm-pin removed again.
  - 🟢 The actual UI tooltip changes mirror #109 across all tables — generally fine.
- **Recommendation:** Request Changes (Critical) — strip the `(x: any)` and `--transpile-only` hunks entirely; either rebase onto a typed fix or open a separate PR for backend type cleanup with proper Prisma payload types.

### PR #106 — Palette: Standardize row action tooltips and CI robustness fix
- **Base:** `ae9772f` · **Ahead:** 4 commits · **Files:** 24 · **+133 / −57**
- **Branch:** `palette/standardize-row-actions-12882323453819521103`
- **Findings:**
  - 🔴 Same `(tx: any)` regressions in `seed/seed.ts` plus `--transpile-only` on the seed script. Also introduces non-null assertions (`role!.publicId`, `platformApp!.id`) to silence type errors — papering over the actual narrowing problem (transaction return type lost after the `any` annotation).
  - 🟡 `e2e.yml` pnpm-pin removal.
  - 🟢 Adds tooltip tests to all admin drawer/table specs — coverage win.
- **Recommendation:** Request Changes (Critical) — same as #107.

### PR #105 — Palette: Standardize and enhance loading states
- **Base:** `ae9772f` · **Ahead:** 4 commits · **Files:** 7 · **+39 / −19**
- **Branch:** `palette-standardized-loading-states-16238419606737248892`
- **Findings:**
  - 🟢 **Best-quality PR of the batch.** Pure UI change: adds `loading` prop to `Button` and `AlertDialogAction`, renders `Loader2` spinner with `aria-busy="true"`, sets `disabled={loading || disabled}`. Correctly handles `asChild` by skipping the spinner (avoids Slot single-child violation).
  - 🟢 Cleans `delete-alert-dialog.tsx` / `confirm-dialog.tsx` callsites — drops the unused `buttonVariants`/`cn` imports.
  - 🟡 Same `e2e.yml` pnpm-pin removal and `--filter=!@sassy-auth/auth-server` selective build.
  - 🟡 **Missing tests** for the new `AlertDialogAction.loading` behavior. Only the Button gets a test in this PR series (and that's in #100, #101).
  - 🟡 Type widening: `AlertDialogAction` now accepts `VariantProps<typeof buttonVariants> & { loading; asChild }` via an inline intersection rather than `Pick<ButtonProps, 'variant'|'size'|'loading'|'asChild'>` (which #100 uses). The two PRs diverge here — pick one canonical signature.
- **Recommendation:** Approve with nits — closest to mergeable. Re-pin pnpm and add an `AlertDialogAction` test (`aria-busy`, disabled, spinner present).

### PR #104 — Palette: Improve 'more actions' accessibility and fix CI
- **Base:** `ae9772f` · **Ahead:** 4 commits · **Files:** 14 · **+87 / −29**
- **Findings:**
  - 🟢 UI-only — tooltip + ARIA labels on all five tables (apps/orgs/permissions/roles/users) without backend touches.
  - 🟡 e2e.yml pnpm-pin removal.
- **Recommendation:** Approve with nits — looks like a clean alternative to #106–#108. Prefer over #107 if the goal is just the row-action tooltips.

### PR #103 — feat(ui): standardized loading states and fix CI e2e workflow
- **Base:** `ae9772f` · **Ahead:** 3 commits · **Files:** 6 · **+35 / −14**
- **Findings:**
  - 🟢 Same loading-state pattern as #105 but slightly narrower scope (no login page touch).
  - 🟡 Duplicate of #105 — pick one and close the other.
- **Recommendation:** Needs Discussion — duplicate of #105.

### PR #102 — Palette: Standardize loading states and fix CI build scope
- **Base:** `ae9772f` · **Files:** 6 · **+35 / −14**
- **Recommendation:** Close — superseded by #105/#103 (essentially identical diff).

### PR #101 — feat(ui): add unified loading state and fix CI build order
- **Base:** `ae9772f` · **Files:** 6 · **+39 / −14**
- **Findings:**
  - 🟢 Adds `packages/ui/src/__tests__/confirm-dialog.test.tsx` — bonus coverage.
- **Recommendation:** Close — superseded by #105.

### PR #100 — feat(ui): add loading prop and fix CI build
- **Base:** `ae9772f` · **Files:** 15 · **+1307 / −89**
- **Findings:**
  - 🔴 **Massive scope creep / unrelated infra change.** Switches NestJS builder to SWC (`nest build --builder swc`) and adds `@swc/cli`, `@swc/core` dependencies. `pnpm-lock.yaml` churns by +1267 / −89 lines. Not what the PR title advertises and a high-risk change to ship alongside UI work — SWC and tsc emit different decorator semantics, which NestJS DI relies on. If this is intentional, it must be its own PR with explicit `experimentalDecorators` / `emitDecoratorMetadata` parity verification and runtime smoke tests on every Nest controller.
  - 🔴 `--transpile-only` on the seed + `(tx: any)` regressions (same as #106/#107).
  - 🟡 `AlertDialogAction` change: `disabled` is passed via `...props` *and* the new explicit `disabled={loading || disabled}` — confirm no double-spread shadowing. Also `asChild` path with `disabled` on `Slot` is a no-op for `<a>` — the new test comments on it but doesn't assert correct behavior; the rendered anchor will be a fully clickable navigation target while "loading."
  - 🟢 Adds Button unit tests (loader rendering, aria-busy, asChild slot).
- **Recommendation:** Request Changes (Critical) — split into three PRs: (1) Button/AlertDialogAction loading prop, (2) NestJS SWC migration, (3) backend type cleanup. Do not merge as-is.

---

## Recent Commits (last 24h)

3 new commits, all from `google-labs-jules[bot]` on the `palette/table-row-actions-15663218071847066241` branch (PR #109). No commits to `master` in the last 24h.

- `408a37e` 🎨 Palette: Standardize user table actions with tooltips and ARIA labels — covered in PR #109 review.
- `754b7cf` (same subject — bot retry/squash predecessor) — covered in PR #109.
- `2bda2bc` Standardize table row actions with tooltips and ARIA labels — covered in PR #109.

No notable findings beyond what's captured under PR #109.

---

## Summary

**Items needing attention (prioritized):**

1. 🔴 **Bot PR backlog is out of control.** #99–#109 are 10+ overlapping Jules-bot PRs implementing two themes (Button loading state, table row tooltips). Pick one PR per theme (recommendation: #105 for loading, #104 for tooltips), close the rest, and tell the bot to stop until human triage catches up. Each open PR consumes review time and CI minutes.
2. 🔴 **`(x: any)` and `--transpile-only` are NOT a fix.** PRs #100, #106, #107, #108 strip TypeScript guarantees from auth/permission/role services and the seed script to make CI green. The real failure is a Prisma type generation or include-payload mismatch. Block these and ask for `Prisma.SaUserGetPayload<...>` or proper transactional types instead.
3. 🔴 **PR #100 silently migrates NestJS to SWC.** This is a build-system change masquerading as a UI tweak. Must be a separate PR with decorator-emission verification.
4. 🟡 **Every Jules PR drops the pnpm version pin in `e2e.yml`.** Establish the real pnpm version (likely a `packageManager` mismatch between `package.json` and the workflow), commit the canonical pin once on master, and reject every PR that removes it.
5. 🟡 **`--filter=!@sassy-auth/auth-server` in e2e workflow** — confirm auth-server's runtime path in the e2e job. If e2e depends on a built auth-server artifact, this filter breaks integration coverage.

**Repo health (one-line):** Master is quiet (no commits since 2026-06-02), but the bot PR queue is generating low-signal, high-risk churn that needs a triage policy before it lands type-safety regressions in the auth core.
