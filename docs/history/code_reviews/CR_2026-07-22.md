# Daily Code Review — sassy-auth

**Date:** 2026-07-22
**Reviewer:** Automated (scheduled task)
**Window:** last 24h (2026-07-21 05:53 UTC → 2026-07-22 05:53 UTC)

---

## Preflight notes

- **`api.github.com` still blocked at the sandbox proxy — 5th consecutive run.** `curl https://api.github.com/…` returns `HTTP 403 … X-Proxy-Error: blocked-by-allowlist`; `plugin:engineering:github` MCP did not connect in this session; `WebFetch` cannot pass an `Authorization` header for the private repo. Reviewed content assembled from **local git refs** — `origin/pr/*` are up to date through **2026-07-21 09:41 CEST** (last fetch). PRs opened/merged/closed after that time on GitHub are invisible to this run; PR merge/close status is inferred from whether the PR head is reachable from `origin/master`.
  **Action for Raphael (unchanged from prior runs):** allowlist `api.github.com`, re-authenticate `plugin:engineering:github`, **or** mint a classic PAT with `repo` scope and replace `GITHUB_PAT` in `.env.local`.
- `.git/config` clean; local plumbing worked without workarounds.

---

## Open PRs

Ordered by most recently updated. Status inferred from local refs — anything not merged into `origin/master` (`a78d4a7`, 2026-07-15) is treated as still open.

### PR #290 — docs(readme): add 2FA env-var subsection to Table of Contents

- **Author:** Raphael Londner
- **Head:** `ad076a8` (2026-07-21 07:41 UTC)
- **Base:** `a78d4a7` (`master`)
- **Diff:** 7 files, +283 / −0 (2 commits on branch `daily-review-2026-07-21`)
- **Files:**
  - `README.md` (+10 — new "Two-Factor Authentication (optional)" env-var subsection + ToC entry for `PLATFORM_REQUIRE_2FA` and `TWO_FACTOR_TRUST_DAYS`)
  - `CHANGELOG.md` (+35 — 2026-07-21 unreleased entry)
  - `bugs/BUGS_2026-07-21.md`, `bugs/bug-0243.md`, `bugs/bug-0244.md`, `bugs/bug-0245.md` (new)
  - `todo/TODO_2026-07-21.md` (new)

**Findings**

- 🟢 **Correctness.** ToC anchor `#two-factor-authentication-optional` matches the new heading slug (kebab-case, punctuation-stripped, GFM anchor rules). New env-var table lists both vars with defaults and semantics that match code (`platform-require-2fa` → strict `"true"` string check; `TWO_FACTOR_TRUST_DAYS` fallback `14` on non-numeric).
- 🟢 **Docs scope.** Partially closes **bug-0240** (undocumented `TWO_FACTOR_TRUST_DAYS`). Also adds a `See [Two-Factor Authentication (2FA)]` back-link to the behavior section — good linkage.
- 🟡 **Coverage gap (not a blocker).** Bug-0240's full ask covers `.env.example` too, not just README. The `.env.example` file was **not** updated in this PR. Recommend a follow-up commit adding the two vars to `.env.example` before marking bug-0240 fixed.
- 🟢 **Multi-purpose branch acceptable.** The branch bundles docs + bug filings + CHANGELOG + TODO — consistent with the pattern used in prior daily-review branches. No mixed-concern code changes.
- 🟢 **Security / Perf.** Docs-only.

**Recommendation:** **Approve.** Add `.env.example` follow-up before closing bug-0240.

### PR #289 — docs(bugs): file bug-0245 (2026-07-21 daily review)

- **Author:** Raphael Londner
- **Head:** `2df3860` (2026-07-21 07:40 UTC)
- **Diff:** 1 file, +36 / −0 (`bugs/bug-0245.md`)

**Findings**

- 🟢 **Verified claim 1 — empty head commit.** `git diff --stat 09391e7^..09391e7` returns empty. PR #286's tip commit is a no-op; real change is in parent `7c4e605`. Bug's process-noise / bug-0090-family framing is correct.
- 🟢 **Verified claim 2 — duplicate test edit.** Both PR #285 (`c55d026`) and PR #286 (`7c4e605`) add the identical two lines (`requireTwoFactor: false`, `twoFactorTrustDays: null`) to `apps/admin/components/__tests__/app-create-drawer.test.tsx`. Whichever merges second will conflict. Root-cause pointer to bug-0243 is accurate.
- 🟢 **Fix ordering is right:** (a) fix bug-0243 on master → (b) rebase/drop empty commit → (c) revisit Jules PR-gen (bug-0090). Doing (a) first eliminates the duplicated edit entirely.

**Recommendation:** **Approve.**

### PR #288 — docs(bugs): file bug-0244 (2026-07-21 daily review)

- **Author:** Raphael Londner
- **Head:** `0522d77` (2026-07-21 07:40 UTC)
- **Diff:** 1 file, +32 / −0 (`bugs/bug-0244.md`)

**Findings**

- 🟢 **Verified.** `apps/admin/messages/fr.json` `common.*` keys read `"Save" / "Edit" / "Delete" / "Confirm"` at lines 16–19 — English. Meanwhile `common.copyError` right beside them (line added by PR #285) is properly translated to `"Échec de la copie …"`. The "byte-identical-value lint" recommendation is well-motivated and aligns with the fix path in bug-0239.
- 🟡 **Consider bumping severity.** These are the four generic action-button labels used across every drawer/dialog — French operators see English on every Save/Edit/Delete/Confirm button app-wide. Bug is filed as "Minor" but the surface area is arguably "Warning". Not a blocker.

**Recommendation:** **Approve.** Optionally reclassify as Warning given the app-wide UI impact.

### PR #287 — docs(bugs): file bug-0243 (2026-07-21 daily review)

- **Author:** Raphael Londner
- **Head:** `331f558` (2026-07-21 07:40 UTC)
- **Diff:** 1 file, +50 / −0 (`bugs/bug-0243.md`)

**Findings**

- 🟢 **Verified.** `apps/admin/components/app-create-drawer.tsx:57–61` submits 5 fields (`name, url, callbackUrl, twoFactorTrustDays, requireTwoFactor`); `__tests__/app-create-drawer.test.tsx:33–38` asserts only the first 3 via `toHaveBeenCalledWith` (exact match). Test **is** stale on `master` since the 2FA "2b" merge `a78d4a7` (2026-07-15).
- 🟢 **CI-gate concern is legitimate.** Both #285 and #286 needed to patch the same test to get green — a signal that pre-merge CI for `apps/admin` Jest is not gating the `master` merge queue (or was silently skipped for the 2FA merge). Fix ask ("investigate why merge to master was allowed with a failing unit test") is the highest-leverage action in today's set.
- 🟢 **Severity (Warning) is appropriate** — masks future regressions in the create-app payload including any new 2FA-related fields.

**Recommendation:** **Approve.** Highest priority to actually fix on `master`.

### PR #286 — 🎨 Palette: Add standardized focus states to native checkboxes  *(carryover, unchanged since 2026-07-21 review)*

- **Author:** `google-labs-jules[bot]`
- **Head:** `09391e7` (2026-07-21 07:16 UTC)
- **Status vs previous review:** No new commits since yesterday's report; head still empty; carryover findings still apply — see [`CR_2026-07-21.md`](./CR_2026-07-21.md).
- **Blocker on merge:** merge-conflict with PR #285 on `app-create-drawer.test.tsx` (bug-0245); recommend fixing bug-0243 on master and dropping the empty tip commit before merging.

**Recommendation (unchanged):** **Approve after** bug-0243 fix on master + rebase/drop empty commit.

### PR #285 — 🎨 Palette: Localize `useCopyFeedback` clipboard error messages  *(carryover, unchanged since 2026-07-21 review)*

- **Author:** `google-labs-jules[bot]`
- **Head:** `c55d026` (2026-07-20 07:47 UTC)
- **Status vs previous review:** No new commits; carryover findings apply — see [`CR_2026-07-21.md`](./CR_2026-07-21.md).
- **Notes:** Merge first (heavier review), then rebase #286.

**Recommendation (unchanged):** **Approve.** Merge before #286.

---

## Recent commits — last 24h

**On `master`:** none. `master` HEAD is still `a78d4a7` (2026-07-15 04:22 UTC, 2FA merge) — quiet for **7 consecutive days**. No production risk introduced today.

**On feature/PR branches (covered in PR sections above):**

- `ad076a8` — Raphael — docs(readme) 2FA env-var ToC entry — PR #290
- `2df3860` — Raphael — docs(bugs) bug-0245 — PR #289
- `0522d77` — Raphael — docs(bugs) bug-0244 — PR #288
- `331f558` — Raphael — docs(bugs) bug-0243 — PR #287
- `e83e435` — Raphael — docs(review) 2026-07-21 daily code review — on `daily-review-2026-07-21`, folded into PR #290
- `09391e7` — Jules bot — empty commit on `pr/286` (see bug-0245)
- `7c4e605` — Jules bot — actual "focus states on checkboxes" change on `pr/286`

No non-merge, non-docs, non-palette code commits in the window.

---

## Summary

**Items needing attention (prioritized):**

1. **Fix bug-0243 on `master`** — stale `app-create-drawer` unit test blocks CI parity; two Jules PRs are independently working around it. Root fix collapses bug-0245's duplicate-edit merge conflict at the same time.
2. **Investigate the missing/skipped admin-Jest CI gate** that let the 2FA "2b" merge (`a78d4a7`) land with a red test. This is the process-level ask embedded in bug-0243 and is more valuable than the test edit itself.
3. **Merge PR #285 → rebase PR #286 → drop empty tip commit** — unblocks the palette work.
4. **Follow-up on PR #290:** add `PLATFORM_REQUIRE_2FA` and `TWO_FACTOR_TRUST_DAYS` to `.env.example` to fully close bug-0240.
5. **Sandbox proxy / GitHub MCP auth** — 5 consecutive daily reviews without live PR state. Every run has to fall back to local refs; this run couldn't detect any PR closed/opened on GitHub after 2026-07-21 09:41 CEST.

**Overall repo health:** Quiet and clean — 7 days of no `master` movement, docs & test-hygiene work only; one stale test on `master` is the sole open correctness concern.
