# Daily Code Review — sassy-auth

**Date:** 2026-06-26
**Repo:** `rlondner/sassy-auth`
**Reviewer:** Automated daily code review (NestJS / TypeScript / Drizzle-Prisma / PostgreSQL stack)

> **Data-source note:** GitHub REST API (`api.github.com`) is blocked by the workspace proxy allowlist. PR enumeration was performed via `git ls-remote` against `github.com` (allowed) using the PAT from `.mcp.json`. Open PRs were identified by the presence of `refs/pull/<n>/merge` (GitHub only publishes this ref while a PR is open and mergeable). Diffs were derived from PR head commits fetched via `refs/pull/*/head`. PR titles/states beyond branch/commit metadata (labels, reviewers, CI status, comments) are not available without REST access.

---

## Activity Window

| Window                         | Master commits | Notable                                                     |
| ------------------------------ | -------------- | ----------------------------------------------------------- |
| Last 24h (since 2026-06-25)    | 0              | No direct pushes to `origin/master`                         |
| Last 7d                        | 0              | `origin/master` HEAD = `4e06f17` (2026-06-19, Raphael)      |

All activity in the last 24h is in open PRs, not merged commits.

---

## Open PRs

**Total open: 58** (40 by Raphael Londner, 18 by `google-labs-jules[bot]`)

Top 10 by most-recent head-commit timestamp:

| PR    | Updated (head)      | Author             | Title                                                                          |
| ----- | ------------------- | ------------------ | ------------------------------------------------------------------------------ |
| #142  | 2026-06-26 08:07 UTC | jules[bot]        | Palette: Standardize tooltips and accessibility for table actions              |
| #141  | 2026-06-25 14:57 UTC | Raphael           | fix(bug-0116): close view drawer on user delete in users-table                 |
| #140  | 2026-06-23 08:49 UTC | jules[bot]        | Palette: Standardized table tooltips & Monorepo CI fixes                       |
| #139  | 2026-06-22 13:58 UTC | Raphael           | fix(bug-0114): handle refresh errors after successful mutations *(empty diff)* |
| #138  | 2026-06-22 13:58 UTC | Raphael           | fix(bug-0113): remove dead workers ternary in playwright.config.ts *(empty)*   |
| #137  | 2026-06-22 13:58 UTC | Raphael           | fix(bug-0112): translate French toast i18n strings *(empty diff)*              |
| #136  | 2026-06-22 08:29 UTC | jules[bot]        | Palette: Standardized accessible table row actions with tooltips               |
| #135  | 2026-06-20 08:03 UTC | jules[bot]        | Palette: Implement tooltips, enhance localization, and fix CI                  |
| #134  | 2026-06-19 15:20 UTC | Raphael           | fix(bug-0111): add bug description for me.controller unsafe cast               |
| #133  | 2026-06-19 15:20 UTC | Raphael           | fix(bug-0110): add bug description for updateRole null check                   |

Bottom 48 PRs (#17–#132 excluding the above) are stale (≥ 7 days since last head update) and require triage / rebase / close.

---

## Detailed Findings

### 🔴 PR #142 — Palette: Standardize tooltips and accessibility for table actions (`google-labs-jules[bot]`)

**Head:** `734d5427b2` · **Files:** 17 · **+293 / -190**

| Severity      | Finding                                                                                                                                                                                                                                                       |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🔴 Critical (Security) | **Adds `.env.keys` containing RSA_PRIVATE_KEY (base64-encoded `-----BEGIN PRIVATE KEY-----` block) and RSA_PUBLIC_KEY at repo root.** Verified by decoding the base64 in the diff. `.gitignore` ignores only `.env*.local` and `apps/auth-server/.env`; **`.env.keys` is NOT ignored**. If merged, the private key is leaked permanently to git history of a private (but multi-collaborator + bot-account) repo. **Block merge. Rotate the key. Add `.env*` and `*.keys` to `.gitignore`. Run `git filter-repo` on the PR branch (or close + re-open without the file).** |
| 🔴 Critical (Hygiene)  | Commits `admin.log` (31 lines) and `auth-server.log` (54 lines) — Next.js dev server + auth-server runtime logs. Currently no global `*.log` gitignore. Logs may leak request/response data, internal IPs (the log contains `192.168.0.2`), and Sentry config warnings.                                          |
| 🟡 Warning             | `apps/admin/components/__tests__/*-table.test.tsx` deletes the per-file `jest.mock('@sassy-auth/ui', ...)` blocks and centralizes them in `jest.setup.ts`. Verify the consolidated mock preserves the original passthrough semantics for `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator`, and `SidebarTrigger`. If the central mock is lossier, all 5 table test suites silently lose the "always-open menu" guarantee — tests may pass but no longer cover the click-Delete-from-row-menu path. |
| 🟡 Warning             | `apps/admin/messages/fr.json` is described as "fixing incorrect English values in French translation (save, edit, delete, confirm)." Diff overlaps with PR #137's stated scope — risk of merge conflict / one PR silently undoing the other. Decide ownership; close one.                                       |
| 🟢 Clean               | `TooltipProvider` added to `apps/admin/app/layout.tsx` at the correct top-level (inside `NextIntlClientProvider`, outside `ThemeProvider` children) — standard Radix pattern.                                                                                                                                |

**Recommendation:** 🔴 **Request Changes — DO NOT MERGE.** Strip `.env.keys`, `admin.log`, `auth-server.log` from the branch, rotate the leaked RSA key pair, and harden `.gitignore` before any further review.

---

### 🟢 PR #141 — fix(bug-0116): close view drawer on user delete in users-table (Raphael)

**Head:** `82d2211884` · **Files:** 1 · **+1 / -0**

| Severity   | Finding                                                                                                                                                                                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🟢 Clean   | Adds the missing `setViewOpen(false)` in the delete handler to match `apps-table`, `orgs-table`, `permissions-table`, `roles-table`. Symmetric, minimal, low-risk.                                                                                       |
| 🟡 Warning | No unit test added for this regression — a `users-table` Jest spec that asserts the view drawer is closed after a successful delete would prevent future regressions across all 5 table components. Consider a shared test helper given the recurring pattern. |

**Recommendation:** ✅ **Approve** (optionally request a regression test).

---

### 🟡 PR #140 — Palette: Standardized table tooltips & Monorepo CI fixes (`google-labs-jules[bot]`)

**Head:** `b68050305b` · **Files:** 31 · **+303 / -169** · **4 commits**

| Severity              | Finding                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🔴 Critical (Type Safety) | "Fixed 60+ pre-existing TypeScript errors in auth-server (transactions & implicit any)" was implemented by **adding `: any` to ~25 callback parameters across `users.service.ts`, `permissions.service.ts`, `roles.service.ts`, `orgs.service.ts`, `apps.service.ts`, `invitations.service.ts`, `permissions/check-permission*.ts`, `assert-caller-can-grant-system-perms.ts`, `seed/*`** and replacing `type Tx = Parameters<Parameters<typeof prisma.$transaction>[0]>[0]` with `type Tx = any`. This **silently destroys Prisma's generated row typing inside RBAC/ABAC enforcement paths**. Any future schema field rename will compile cleanly and ship broken auth. Example: `ur.role.permissions.map((rp: any) => ({ permissions: rp.permission.name, ... }))` — a typo would no longer be caught. |
| 🟡 Warning             | `pnpm/action-setup@v4` lost its `version: 9` pin in `.github/workflows/e2e.yml`. Now uses `packageManager` from `package.json`. Reproducibility depends on the root `packageManager` field being authoritative — confirm it is set, otherwise CI will float on whatever pnpm@v4 default is.                                                                                       |
| 🟡 Warning             | New `Build shared packages` step `pnpm exec turbo build --filter=!@sassy-auth/auth-server` runs before `prisma generate`. If any shared package imports types from the generated Prisma client, the build will fail on a clean clone.                                                                                                                                            |
| 🟡 Warning             | Adds `.Jules/palette.md` (bot scratchpad) to the repo. Consider routing bot artifacts to a `.gitattributes`-marked or ignored directory.                                                                                                                                                                                                                                       |
| 🟡 Warning             | Same scope overlap with PR #142 (admin tables + fr.json + en.json) — merge-order will conflict. Pick one canonical PR.                                                                                                                                                                                                                                                         |
| 🟢 Clean               | The user-facing tooltip / a11y additions to `apps-table`, `orgs-table`, `permissions-table`, `roles-table`, `users-table`, `theme-toggle` look reasonable in isolation.                                                                                                                                                                                                                |

**Recommendation:** 🔴 **Request Changes.** Fix TS errors properly: restore generated Prisma types, use `Prisma.TransactionClient` for `Tx`, declare explicit narrow types for `roles`/`permissions` payloads (these are the *security-critical* paths — escalation guards, role grants, perm grants). Split UI changes (low-risk) from auth-server changes (high-risk) into two PRs.

---

### 🟡 PRs #139 / #138 / #137 — empty-diff "fix" PRs (Raphael)

| PR  | Bug      | Files changed | Notes                                                       |
| --- | -------- | ------------- | ----------------------------------------------------------- |
| #139 | bug-0114 | 0             | Head commit has a fix-description message but **no diff vs `origin/master`**. |
| #138 | bug-0113 | 0             | Same — empty diff.                                          |
| #137 | bug-0112 | 0             | Same — empty diff. Scope overlaps with PR #142 fr.json fix. |

**Recommendation:** 🟡 **Needs Discussion.** These look like dangling branches where the fix was either rebased away or never committed. Either push the actual fixes or close the PRs. Likely candidates to close given PR #142 and PR #140 already touch the relevant files.

---

### 🟡 PR #136 — Palette: Standardized accessible table row actions with tooltips (`jules[bot]`)

**Head:** `8d28d220f2` · **Files:** 17 · **+129 / -42** · **4 commits**

Same shape as PR #140 (smaller subset). No `.env.keys` leak verified. Likely superseded by #140/#142.

**Recommendation:** 🟡 **Needs Discussion / Close** as duplicate of #140 / #142.

---

### 🟡 PR #135 — Palette: Implement tooltips, enhance localization, and fix CI (`jules[bot]`)

**Head:** `f6dad92bf7` · **Files:** 12 · **+202 / -94** · **4 commits**

Same family as #136 / #140 / #142. Older snapshot of the bot's iterative work.

**Recommendation:** 🟡 **Close** — superseded by #142 (after #142 is cleaned up) or #140 (after TS-fix issues addressed).

---

### 🟢 PRs #134 / #133 — bug description docs (Raphael)

| PR  | Bug      | Files                | Notes                          |
| --- | -------- | -------------------- | ------------------------------ |
| #134 | bug-0111 | `bugs/bug-0111.md`   | 10-line bug doc; documentation only. |
| #133 | bug-0110 | `bugs/bug-0110.md`   | 10-line bug doc; documentation only. |

**Recommendation:** ✅ **Approve / Squash-merge** as `docs:` commits, or batch into a single docs PR.

---

## Cross-Cutting Issues

1. **🔴 Secret-leak risk via bot PR.** PR #142 is the most concerning item. Mitigation:
   - Add `.env*`, `*.keys`, `*.pem`, `*.log` to `.gitignore` proactively (one-line fix).
   - Add a CI guard / pre-commit hook (e.g., `gitleaks`, `trufflehog`) — this would have caught PR #142 at PR-creation time.
   - Restrict the `google-labs-jules[bot]` token scope so it cannot push files matching `.env*` / `*.keys` / `*.pem`.
2. **🔴 `any`-flood in PR #140 auth-server code.** Auth/permission services are the wrong place for type erasure. Resolve by importing `Prisma` from the generated client and using `Prisma.SaUserGetPayload<...>` / `Prisma.TransactionClient`.
3. **🟡 PR backlog (58 open).** Recommend triage day:
   - Close #137 / #138 / #139 (empty diffs).
   - Close #135 / #136 (superseded by #140 / #142).
   - Decide single canonical PR for the Palette / tooltip work (one of #140 / #142).
   - Rebase or close the 48 stale PRs (#17–#132).
4. **🟡 Bot PR governance.** Two of the three most recent bot PRs touch the same admin-table surface area; one introduces a critical secret leak. Consider requiring `CODEOWNERS` review on `apps/auth-server/**`, `**/*.env*`, `**/*.keys`, and `.github/workflows/**` before any bot PR can merge.
5. **🟢 No master-branch activity** in last 24h — release pipeline is quiet. All risk is contained in the PR queue.

---

## Summary — Items Needing Attention (Prioritized)

1. **🔴 BLOCK PR #142.** Strip `.env.keys` + log files, rotate the leaked RSA key pair, harden `.gitignore`, add secret-scanning to CI.
2. **🔴 BLOCK PR #140 auth-server portion.** Replace `: any` casts with proper Prisma types in security-critical services.
3. **🟡 Triage empty PRs #137 / #138 / #139** — close or push real fixes.
4. **🟡 Choose one canonical Palette PR** between #135 / #136 / #140 / #142; close the rest.
5. **🟡 Backlog triage:** 48 stale PRs (#17–#132) need rebase-or-close decisions.
6. **🟢 Approve PR #141** (1-line `setViewOpen(false)` fix). Consider adding a regression test.

**Repo health:** 🟡 **Caution** — codebase quality is good, but the open-PR queue contains an active secret-leak risk and a type-safety regression in RBAC/ABAC enforcement paths. Master is stable (unchanged in 24h+).
