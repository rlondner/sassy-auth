# Daily Code Review — sassy-auth

**Date:** 2026-07-21
**Reviewer:** Automated (scheduled task)
**Window:** last 24h (2026-07-20 07:38 UTC → 2026-07-21 07:38 UTC)

---

## Preflight notes

- **Open PRs still cannot be enumerated via API — 4th consecutive run.** Sandbox HTTP proxy blocks `api.github.com` (`X-Proxy-Error: blocked-by-allowlist`), `plugin:engineering:github` MCP is unauthenticated in this session, and `git ls-remote` over the allowed `github.com` HTTPS endpoint fails auth against the fine-grained PAT in `.env.local` (`Invalid username or token. Password authentication is not supported for Git operations.`) with every username variant tried (`x-access-token`, `rlondner`, token-as-user). Reviewed content was assembled from **local git refs** — specifically the `origin/pr/*` refs fetched by the previous run (last fetch: 2026-07-20 09:14 CEST). Anything opened/merged/closed on GitHub after that fetch is invisible to this run.
  **Action for Raphael:** (a) allowlist `api.github.com` in the sandbox proxy, **or** (b) re-authenticate the `plugin:engineering:github` MCP, **or** (c) mint a classic PAT with `repo` scope and replace `GITHUB_PAT` in `.env.local`.
- **`.git/config` corruption looks resolved** — no trailing NUL fill this run; all local git plumbing worked without the temp-copy workaround from the 2026-07-08→10 runs. Good.

---

## Open PRs (2 with commits in the review window)

Based on local `origin/pr/*` refs. There are additional older PR refs in the local repo (bug-tracking PRs #280–#284 landed 2026-07-20 09:28–09:30 CEST, all outside this 24h window) that are not reviewed here.

### PR #285 — 🎨 Palette: Localize `useCopyFeedback` clipboard error messages

- **Author:** `google-labs-jules[bot]`
- **Head:** `c55d026` (2026-07-20 07:47 UTC — landed at the very start of this window)
- **Base:** `a78d4a7` (`master`, 2026-07-15 2FA merge)
- **Diff:** 6 files, +99 / −3
- **Files:**
  - `apps/admin/lib/use-copy-feedback.ts` (+2/−1 — swap hard-coded string for `t('copyError')`)
  - `apps/admin/lib/__tests__/use-copy-feedback.test.tsx` (+86, new — success path + error toast path)
  - `apps/admin/messages/en.json`, `apps/admin/messages/fr.json` (+1/−0 each — `common.copyError` key)
  - `.Jules/palette.md` (+4 — new palette learning)
  - `apps/admin/components/__tests__/app-create-drawer.test.tsx` (+2 — **unrelated**, see 🟡 below)

**Findings**

- 🟢 **Correctness.** `useTranslations('common')` is invoked at the top of the hook (React rules-of-hooks compliant), and `t('copyError')` resolves the key present in both locale bundles. The hook signature and return contract (`{ copiedKey, copy }`) are unchanged — no breaking call-site impact.
- 🟢 **i18n.** Both `en.json` and `fr.json` receive the new key; French translation is a real translation, not a copy-paste of English.
- 🟢 **Tests.** New Jest + RTL suite covers (i) success path with the 50 ms reset timeout, (ii) failure path asserting `toast.error` is called with the localized string. Uses `NextIntlClientProvider` correctly. `waitFor` timeout of 300 ms comfortably covers the 50 ms reset.
- 🟡 **Scope creep — unrelated test change.** The `app-create-drawer.test.tsx` diff (adds `requireTwoFactor: false, twoFactorTrustDays: null` to a mock assertion) has nothing to do with clipboard localization — it's a post-2FA-merge test-repair. **PR #286 makes the exact same edit to the same file** (see below), so whichever PR merges first will cause the other to conflict at that line. Ask Jules to either split this into its own PR, or coordinate the merge order.
- 🟡 **Broader i18n gap not addressed (bug-0239).** `fr.json`'s `common` section still contains English strings for `"save"`, `"edit"`, `"delete"`, `"confirm"` (already tracked as bug-0239 per yesterday's filings). This PR localizes one string; the underlying regression across the `common` namespace remains. Not a blocker for this PR — just noting it so the fix isn't misread as "French locale done".
- 🟢 **Perf / Security.** No new attack surface; `useTranslations` cost is negligible per render.

**Recommendation:** **Approve** — request that the `app-create-drawer.test.tsx` change be split out (or explicitly acknowledged as needed for CI), and coordinate merge order with PR #286.

### PR #286 — 🎨 Palette: Add standardized focus states to native checkboxes

- **Author:** `google-labs-jules[bot]`
- **Head:** `09391e7` (2026-07-21 07:16 UTC — landed near end of window)
- **Base:** `a78d4a7` (`master`, 2026-07-15 2FA merge)
- **Diff:** 5 files, +9 / −3
- **Files:**
  - `apps/admin/app/login/two-factor/TwoFactorForm.tsx` (checkbox for "trust this device")
  - `apps/admin/components/app-create-drawer.tsx` (checkbox for `requireTwoFactor`)
  - `apps/admin/components/app-edit-drawer.tsx` (checkbox for `requireTwoFactor`)
  - `.Jules/palette.md` (+4 — new palette learning)
  - `apps/admin/components/__tests__/app-create-drawer.test.tsx` (+2 — same unrelated change as PR #285)

**Findings**

- 🟢 **Correctness / A11y.** Adds `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` to each `<input type="checkbox">`. This is the shadcn/ui-standard focus pattern, resolves the browser-outline / no-ring visibility gap under keyboard nav, and satisfies WCAG 2.1 SC 2.4.7 (Focus Visible). CSS var `--ring` is already defined in the admin theme.
- 🟢 **Coverage.** Verified via `grep 'type="checkbox"'` on `origin/master:apps/**/*.tsx` — there are **exactly 3** native checkboxes in `apps/admin` and PR #286 touches all 3. `apps/auth-server` has none. No misses.
- 🟡 **Tech debt (not a blocker).** Same Tailwind classes are copy-pasted into 3 call sites. Recommend extracting a shared `<Checkbox />` primitive (either the shadcn/ui `Checkbox` component or a thin wrapper around `<input type="checkbox">`) so the next "we forgot a state on the checkbox" ticket doesn't require another 3-line change across 3 files. This same pattern (native inputs with duplicated Tailwind) will keep generating palette-tier PRs otherwise.
- 🟡 **Same scope-creep as PR #285** — the `app-create-drawer.test.tsx` edit is duplicated. See PR #285 finding above.
- 🟢 **Perf / Security.** Presentational only; no runtime, no attack surface.
- 🟢 **Tests.** No new unit tests, appropriate — this is a Tailwind class change on a native element. If e2e coverage is desired, a Playwright `focus()` + screenshot in `apps/admin-e2e` would suffice; not required for merge.

**Recommendation:** **Approve.** File a follow-up (or bug-0243) to extract a shared `<Checkbox />` primitive.

### Merge-order recommendation for #285 and #286

Both PRs edit the same line of `app-create-drawer.test.tsx`. Merge PR #285 first (it carries substantive logic + tests, so the review conversation is heavier), then rebase PR #286 — its remaining diff shrinks to the 3 checkbox lines + palette.md + already-conflict-free tests.

---

## Recent commits — last 24h

**On `master`:** none. Last `master` commit is still `a78d4a7` (2026-07-15 04:22 UTC, 2FA merge). Master has been quiet for ~6 days.

**On feature/PR branches (already covered above via PR #285 / PR #286):**

| SHA | When (UTC) | Author | Branch | Subject |
|---|---|---|---|---|
| `09391e7` | 2026-07-21 07:16 | `jules[bot]` | `pr/286` | 🎨 Palette: Add standardized focus states to native checkboxes |
| `7c4e605` | 2026-07-21 07:12 | `jules[bot]` | (on `pr/286` history) | (identical subject — earlier revision, superseded by `09391e7`) |
| `c55d026` | 2026-07-20 07:47 | `jules[bot]` | `pr/285` | 🎨 Palette: Localize useCopyFeedback clipboard error messages |

No non-PR commits landed in the window.

---

## Summary

**Items needing attention (prioritized):**

1. **[Infra — 4th day running] Restore GitHub API access.** Any one of: proxy allowlist for `api.github.com`, re-auth `plugin:engineering:github` MCP, or a classic PAT with `repo` scope. Until this is fixed the daily task cannot verify PR state, CI status, or reviewer coverage on GitHub — only local ref state.
2. **[Merge coordination]** PR #285 and PR #286 both edit the same line of `app-create-drawer.test.tsx`. Merge PR #285 first; rebase PR #286.
3. **[Tech debt]** Extract a shared `<Checkbox />` primitive for the admin app — 3 native checkboxes with copy-pasted Tailwind classes is a recurring source of "one checkbox forgot X" palette PRs. Suggest filing as bug-0243.
4. **[Backlog watch]** Bug-0239 (French `common.*` regression) is filed but only the `copyError` string is fixed by PR #285. The broader gap (`save`, `edit`, `delete`, `confirm` still English in `fr.json`) remains open.

**Overall repo health:** Quiet, low-risk cycle. Only two Jules-generated palette PRs landed in the window — both cleanly scoped, correctly tested (where testable), and safe to merge. Master itself hasn't moved in 6 days; no shipping risk this cycle. Infrastructure gap remains the only recurring red flag.

<run-summary>Two Jules-authored palette PRs opened in the 24h window (#285 clipboard-error i18n with new tests, #286 standardized checkbox focus states across all 3 admin checkboxes); both approve-worthy but share a duplicated unrelated test edit that will conflict — merge #285 first. Master unchanged in 6 days. GitHub API access is still blocked for the 4th consecutive run — recommend fixing proxy allowlist or re-authing the github MCP.</run-summary>
