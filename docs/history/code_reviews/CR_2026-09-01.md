# 🔍 Daily Code Review — sassy-auth

**Date:** 2026-09-01
**Reviewer:** Automated daily code review (Claude)

## Environment Note

GitHub REST API (`api.github.com`) is blocked by the sandbox egress proxy in this
run — only `github.com` (git protocol) is reachable. PR metadata (title, author,
labels, review state) could not be retrieved. The review below is derived from
`git ls-remote` and local `git show` against the fetched refs.

## Open PRs

Cannot enumerate PR objects without API access. Remote branch inventory
(`git ls-remote --heads`) shows the following non-master heads whose tip commits
are **not** contained in `origin/master` (candidate open PRs):

| Branch | Tip |
|---|---|
| `bugs/bug-0265` … `bugs/bug-0270` | (older; not fetched in this run) |
| `bugs/bug-0271-admin-react-19-mismatch` | 62ab50f |
| `bugs/bug-0272-ai-review-sanitization-glob` | f6d45fd |
| `bugs/bug-0273-ui-coverage-floor-unmet` | 9f1a08f |
| `bugs/bug-0274-unit-tests-node20` | 7c35d5e |
| `bugs/bug-0275-2fa-enable-other-sessions-amr` | 11c0b6f |
| `bugs/bug-0276-reset-password-no-session-revoke` | e75eb12 *(merged into master via #366)* |
| `chore/discovery-work-items` | bfaf64e |

Full branch fetch timed out; the sandbox cannot bulk-fetch every candidate branch
in one pass, and per-branch diff review requires API-side PR pairing (base sha,
requested reviewers, CI status) that is not reachable here. **Action for Raphael:**
if PR-level review is needed for a given day, either (a) unblock `api.github.com`
in the sandbox egress list, or (b) supply the PR list explicitly in the task
prompt.

## Recent Commits (last 48h — no commits in strict 24h window)

Master head: `5b7cfb9` — `Merge pull request #366 from rlondner/daily-review-2026-08-30`
(2026-08-31 03:19 UTC, Raphael Londner).

### `e75eb12` — fix(bug-0276): revoke other sessions on password reset

- **Author:** Raphael Londner
- **Files:** `apps/auth-server/src/auth/auth.config.ts` (+10),
  `apps/auth-server/src/auth/auth.config.spec.ts` (+14)
- **Change:** Sets `emailAndPassword.revokeSessionsOnPasswordReset = true` in the
  BetterAuth config and adds a config-level spec asserting the flag is on.

**Findings**

| Severity | Area | Finding |
|---|---|---|
| 🟢 Security | AuthN | Correct fix. Closes a real gap: `/reset-password` previously left every other active session alive, negating the primary user response to suspected credential compromise. |
| 🟢 Consistency | Config | Symmetric with the 2FA-enrollment revoke fix (bug-0275). Rationale, upstream reference, and cross-link are captured in the inline comment — future-proof against silent BetterAuth default drift. |
| 🟢 Testing | Coverage | New spec pins the flag; catches regressions if the config is refactored or the BetterAuth option is renamed. |
| 🟡 Coverage gap | Integration | Config-level assertion only. There is no end-to-end test that (i) issues a reset token, (ii) completes `/reset-password`, and (iii) verifies existing sessions for the same user return 401 afterward. Recommend adding one integration spec in a follow-up — cheap insurance if BetterAuth ever changes the flag's semantics without renaming it. |
| 🟢 Blast radius | Behavior | Users completing password reset will now be signed out on other devices. Acceptable and expected UX for a security-driven reset; no admin-only path affected. |

**Recommendation:** ✅ Already merged (PR #366). No changes required. File the
integration-test follow-up as a low-priority task.

### `b895d0f` — docs: 2026-08-30 daily code review — CHANGELOG, README, TODO, BUGS bundle

- Docs-only bundle. Not reviewed for logic. 🟢 Clean.

### `5b7cfb9` — merge commit (PR #366)

- Merge only. 🟢 Clean.

## Summary

- **Items needing attention (prioritized):**
  1. 🟡 Add an end-to-end integration test for the bug-0276 reset-password
     session-revocation behavior (not just the config assertion).
  2. ⚙️ Unblock `api.github.com` in the sandbox egress allow-list so future
     daily reviews can enumerate open PRs and their diffs. Alternative: switch
     the task to `gh` CLI once installed in the sandbox image.
- **Repo health:** 🟢 Healthy — the only substantive change in-window is a
  well-scoped, well-documented, tested security fix that mirrors an existing
  pattern.
