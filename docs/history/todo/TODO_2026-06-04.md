# TODO — Follow-ups from 2026-06-04 Daily Review

Living list of risky patterns, missing tests, and design-level follow-ups surfaced by the daily code review. Mechanical fixes are tracked in [BUGS_2026-06-04.md](../bugs/BUGS_2026-06-04.md); this file collects everything that needs **judgement, planning, or larger context** before action.

## PR backlog — 29 open, 0 merged

The most pressing concern today is not new code but the **growing backlog of unimplemented bug fixes**. 29 PRs are open across 4 daily reviews (bug-0001 through bug-0047), all with empty branches (no implementation commits). This means:

- 3 critical bugs (bug-0001 RBAC isolation, bug-0038 JWT breaking change, bug-0039 in-memory code store) are open with no progress.
- The PKCE feature branch (`docs/pkce-resource-server-design`) cannot ship until at minimum bug-0038 and bug-0039 are resolved.
- The sidebar fix branch (`fix/sidebar-active-highlight`) has a PR (#41) but the actual fix commit (`a401cfd`) is on this branch, not on a bug-fix branch.

**Recommendation:** Prioritize implementing the 3 critical bugs (bug-0001, bug-0038, bug-0039) before opening more bug-tracking PRs. Consider closing the empty stub PRs and re-creating them only when implementation is ready.

## New bugs from today

- [ ] **bug-0048 — Magic link and OTP console logging** — `console.log` sends full magic-link URLs and OTP codes to stdout with no production guard. Would allow authentication hijacking via log access.
- [ ] **bug-0049 — Hardcoded JWT lifetime** — `exp: now + 3600` in `token.service.ts` is not configurable. Low urgency but should be an env var.

## Carried from 2026-06-03 (still open)

### Critical — must resolve before PKCE ships

- [ ] **bug-0038 — JWT breaking change needs migration plan** — `permissions` array -> `scope` string rename.
- [ ] **bug-0039 — Move authorization codes to persistent storage** — In-memory `Map` is lost on restart.
- [ ] **bug-0041 — Add RFC 7636 format validation** — `code_verifier` lacks format check.

### Missing test coverage (unchanged — no tests added)

- [ ] No PKCE format validation tests
- [ ] No code expiration boundary test
- [ ] No cross-app code exchange test
- [ ] No code reuse e2e test
- [ ] FastAPI OAuth routes untested
- [ ] `safe-next.ts` missing edge cases
- [ ] No integration test for demo seed
- [ ] Previous gaps from 2026-06-02 and earlier still open

### Risky patterns (unchanged)

- [ ] FastAPI token exposure to JavaScript (bug-0042)
- [ ] JWKS cache lifespan (10-minute window)
- [ ] TOCTOU in `resolveAppScopedIds`
- [ ] Audit log asymmetry
- [ ] Hardcoded `take: 50` in detail views
- [ ] Redundant count queries

### Admin UI follow-ups (unchanged)

- [ ] `LOGIN_NEXT_ALLOWED_ORIGINS` env var undocumented in `.env.example`
- [ ] Direct permissions missing app context in view drawer (bug-0036)
- [ ] No loading indicator during delete operations
- [ ] No toast/success feedback after mutations
- [ ] Duplicated pagination component
- [ ] `NAME_REGEX` defined locally in multiple files
- [ ] Server actions have no Sentry integration
- [ ] Sidebar `startsWith` matching (bug-0033, PR exists)

### Configuration / ops follow-ups (unchanged)

- [ ] `.env.example` missing: `LOGIN_NEXT_ALLOWED_ORIGINS`, `SEED_DEMO`, `E2E_ADMIN_EMAIL`, `E2E_ADMIN_PASSWORD`, `CI_TESTS`, `JWT_TOKEN_LIFETIME_SECONDS`
- [ ] No `pnpm lint` or type-check in CI
- [ ] No Python linting/formatting or CI workflow for FastAPI resource server

---

*Generated as part of the 2026-06-04 daily code review. See [CHANGELOG.md](../CHANGELOG.md), [BUGS_2026-06-04.md](../bugs/BUGS_2026-06-04.md) for the bug catalog.*
