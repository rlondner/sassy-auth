# TODO — 2026-08-19

Follow-up items from the daily code review covering 2026-08-17 → 2026-08-19.

## Headline: the 34-day master freeze broke

`master` advanced from `a78d4a7` (2026-07-15) to `7a56c4e` (2026-08-18) — 17 commits,
all pushed **directly to `master`** by an AI coding agent ("Kanna", `claude/opus`,
co-authored commits, no PR). See [CHANGELOG.md](../../CHANGELOG.md) for the full
list. This closes out a long list of previously-catalogued bugs (0214, 0215, 0220,
0221, 0223, 0225, 0226, 0227, 0232, 0233, 0234) and lands a 3-phase public-release
prep (LICENSE, SECURITY.md, seed-password hardening, docs/history reorg,
CONTRIBUTING/CODE_OF_CONDUCT, a CI typecheck gate).

## Items needing attention

### 1. `master`'s e2e CI has been red since the 2FA merge — fix confirmed, but it unmasked bug-0262
See [bug-0259](./bug-0259.md) / PR #335. `auth-state.setup.ts` never learned to
dismiss the `/login/two-factor-prompt` interstitial that ships with the 2FA feature
merged at `a78d4a7`. This had been silently failing **every** e2e run on `master`
for 34 days — through the entire freeze — because the daily review checks PR/commit
activity via the GitHub API, not the Actions dashboard. **The fix is confirmed
working** (PR #335's own CI run: all 6 setup logins now pass in 5-6s each). But
this was exactly the "re-baseline" risk flagged when this bug was first found: with
the blocker gone, the same CI run reached the real suite for the first time since
2026-07-09 and surfaced **5 new failing tests** — see
[bug-0262](./bug-0262.md) / PR #339 (investigation needed, root cause not yet
confirmed). **Action: once #335 and #339's investigation land, re-run e2e on
`master` and confirm a fully green run before treating `master` as healthy.**

### 2. Add a CI-health check to the daily review's own routine
This review only found the 34-day-red e2e workflow by manually running
`gh run list --branch master`. Add that check as a standing step in the daily
review process, not just PR/bug bookkeeping — a red `master` is a more urgent
finding than any Palette PR review.

### 3. Direct pushes to `master` bypass all review — no branch protection configured
`gh api repos/.../branches/master/protection` returns 403 (private repo, no
GitHub Pro). All 17 of today's commits landed with zero PR review. In this instance
the changes are well-reasoned and tested, but the auth-server changes in particular
(`178d003` disabling sign-up auto sign-in, `3b6078f`'s seed-password hardening) are
exactly the kind of change this daily-review process exists to catch *before* it
ships. Consider: enabling branch protection (may require a paid plan or making the
repo public — the repo is now public-release-ready per today's commits, which may
resolve this), or at minimum treating direct-to-master auth/security commits as
requiring a same-day retroactive review.

### 4. Four-way duplicate Palette PR pileup — now also conflicts with master
See [bug-0260](./bug-0260.md) / PR #336. PRs #316 (08-13), #324 (08-15), #328
(08-16), and #334 (08-18) are four independent Jules attempts at the same
row-action-tooltip/`common.moreActions` localization, all mutually conflicting, and
now also conflicting with master's own `5e1ade4` (bug-0225/0226) accessible-names
fix on the same 5 table files. Recommend closing #316/#324/#328 and rebasing #334.

### 5. Large stale-PR backlog (26 open PRs, oldest from 2026-08-06)
Beyond the 4-way duplicate above, PRs #310–#329 (Palette/Jules UI-polish work, plus
this project's own daily-review and bug-filing PRs #305–#333) are all still open.
With `master` now moving again after the reorg (`02261fb` moved `bugs/`→
`docs/history/bugs/`, `todo/`→`docs/history/todo/`), **every one of these PRs is
based on the pre-reorg file layout** and will need at minimum a rebase, and likely
a fresh look at whether the underlying UI work still applies. Recommend a triage
pass: close what's superseded, rebase what's still wanted.

### 6. CI typecheck gate excludes all test files — noted as accepted debt, worth tracking
`4b48e45` added `.github/workflows/typecheck.yml` gating `apps/admin` and
`packages/ui` via `tsconfig.ci.json`, but both configs exclude `__tests__` because
of 64 pre-existing type errors, all in test files (missing `@types/jest`/
`@jest/globals`, two fixtures predating `createdAt`/`lastLoginAt` on `User`). The
commit message documents this as a deliberate, scoped tradeoff rather than an
oversight — leaving it here as a standing reminder rather than re-filing it as a bug.
No ESLint config exists anywhere in the workspace either (also called out as a known
gap in the same commit).

### 7. Long-open question: `bug-0161` seed idempotency — largely addressed, not fully closed
`178d003` reworks `seed.ts` so a run that died mid-way (BetterAuth user created,
`SaUser` link not yet written) resumes on the next run instead of being permanently
stranded — this was the core complaint in
[bug-0161](./bugs/tracking/bug-0161.md) / `docs/history/bugs/BUGS_2026-07-04.md:269`.
Not marked closed here because it wasn't independently re-verified against the
original repro in this review (the commit's own verification — a real `flox`
database run — is a strong signal but this review didn't re-run it). Recommend
closing `bug-0161` explicitly once someone re-confirms.

### 8. Only genuinely new bot activity since the 2026-08-17 review: PR #334
Everything else in the "new since last review" window (PRs #330–#333) was this
review process's own bug-filing output from 2026-08-17. The only new external PR is
#334, covered by item 4 above.

### 9. `seed.ts` admin-repair path: narrow non-idempotency window (Low, not filed)
The `SaUser` insert for a repaired half-provisioned admin runs in its own
transaction, but the subsequent permission/role grant runs afterward,
uninstrumented. A crash between the two leaves an admin with no permissions that
the "already exists" guard will never retry. Unlikely today (permissions seed
before admins in step order), but consider switching the grant to `upsert` —
matching the pattern already used in `demo-multitenant.ts` /
`demo-resource-server.ts` — as a defensive follow-up.

### 10. `apps/admin/tsconfig.ci.json` exclude glob has a small gap (Low, not filed)
`**/__tests__` doesn't catch `apps/admin/lib/safe-next.spec.ts`, which lives outside
a `__tests__` directory. Currently benign (the file typechecks cleanly), but worth
tightening the glob or moving the file for consistency with the stated exclusion
policy.

### 11. README env-var reference had two real gaps — fixed in this review's bundle
`PLATFORM_REQUIRE_2FA` was documented only in prose (Auth Flows section), not in
the Environment Variables reference tables; `TWO_FACTOR_TRUST_DAYS` wasn't
documented anywhere, including `.env.example` — this is long-standing bug-0240
(filed 2026-07-20, never merged because its PR sat in the same frozen backlog as
everything else). Both are now added to `.env.example` and README as part of this
review's README pass, closing bug-0240. `EMAIL_FROM`/`EMAIL_SMTP_*`/
`RESEND_API_KEY` were also missing from the main reference tables (only mentioned
narratively under "Local email testing") — added for completeness.

## New bugs filed today (4)

See [BUGS_2026-08-19.md](./bugs/BUGS_2026-08-19.md) for full detail.

- **[bug-0259](./bugs/bug-0259.md)** (Critical) — `master` e2e CI red for 34 days;
  `auth-state.setup.ts` never handled the 2FA interstitial. Fixed and confirmed
  working in PR #335.
- **[bug-0261](./bugs/bug-0261.md)** (Critical) — `resolveSeedPassword` silently
  no-ops when `NODE_ENV` is unset, defeating the safety check added the same day
  (`3b6078f`). Fixed in PR #337.
- **[bug-0262](./bugs/bug-0262.md)** (Critical) — the first e2e run to reach the
  real test suite in 34+ days surfaced 5 failing tests across unrelated areas;
  probable dev-server resource contention on the CI runner, not yet confirmed.
  PR #339 (investigation, no code fix yet).
- **[bug-0260](./bugs/bug-0260.md)** (Medium) — PR #334 is a 4th duplicate of the
  row-action-tooltip lineage, now conflicting with `master` itself. PR #336
  (no code fix — the defect is in the unmerged Jules PRs).

## Carried-forward open bugs (all in unmerged PRs; unaffected by today's master changes)

- 🔴 bug-0250 (seed 2FA-prompt suppression, PR #316/#315)
- 🟡 bug-0251 (PR #316 scope creep), bug-0252 (nested double-`asChild`), bug-0253
  (native-`title` copy tooltip)
- 🟠 bug-0254/bug-0255 (PR #324 duplication + coverage) — see item 4/5 above
- 🟠 bug-0256 (`autoSignIn:false` 3-way collision — **note:** master's own `178d003`
  already ships the correct, tested version of this exact change; #316/#328/#329
  carrying their own untested copies is now *also* a guaranteed conflict with
  master, not just with each other)
- 🟠 bug-0257, 🟢 bug-0258 (PR #328 triplication + global Tooltip stub)
