# TODO — follow-ups from the 2026-08-20 daily review

## Top priority: the merge backlog is now the biggest risk to this repo

**70 PRs are open, the oldest from 2026-07-10 (41 days).** Every daily review
since 2026-07-20 has said some version of "the bottleneck is merging, not
filing" — that has not changed, and it is now getting materially worse
(41 → 70 open PRs). Filing more bug/fix PRs on top of an unmerged pile has
diminishing value. Concrete recommendation: **spend the next review cycle
merging, not reviewing.** In priority order:

1. **#335 (bug-0259 fix)** — this single PR is why `admin-e2e` is red on
   `master`. Merging it unblocks real CI signal for everything downstream,
   including bug-0262's investigation. Highest-leverage merge available.
2. **#341 (bug-0263, filed today)** — mechanical, low-risk (retention-days
   14→3). Safe to merge immediately, independent of #335.
3. **#337 (bug-0261 fix)** — Critical: `resolveSeedPassword` currently treats
   unset `NODE_ENV` as `development`, silently defeating its own safety
   check. Small, self-contained, no known conflicts.
4. **#336, #339 (bugs 0260, 0262 — docs-only)** — no code risk, safe to merge
   for recordkeeping even if their underlying issues need more work.
5. **The row-action-tooltip lineage (#316, #324, #328, #329, #334)** — four-way
   duplicate work (bug-0254/0256/0257/0260). Pick ONE (recommend #324 — the
   only admin-only variant with no `apps/auth-server` rider) and close the
   other three rather than trying to merge all four.
6. Everything else (#265–#333 minus the above) needs a human triage pass —
   there are now enough of them that another automated review cycle will
   just keep adding to the pile without a merge decision-maker.

## bug-0262 investigation (5 e2e failures once setup was unblocked)

The only data point so far is the single CI run on `bugs/bug-0259`
(`32265048205`, 108 passed / 5 failed). No second run with the same fix has
happened since — every other run since checked either predates the fix or
is a branch built from `master` directly (see BUGS_2026-08-20.md's
"Not filed" note). **Once #335 merges**, the next `master` push's e2e run is
a genuine second data point: if the same 5 specs fail again, that's strong
evidence against pure CI resource contention and worth a real root-cause
pass per failing spec. If a different set fails (or none), that supports the
contention hypothesis and the remaining lever is CI runner tuning (fewer
parallel steps, more timeout headroom), not code changes.

## bug-0263 follow-up (artifact quota)

PR #341 shortens retention to 3 days but does not clear artifacts already
counted against the quota — GitHub recalculates usage on its own 6-12h
cycle. If `Failed to CreateArtifact` is still appearing in `admin-e2e` runs
a few days after #341 merges, the next step is manually deleting old
artifacts via `gh api /repos/{owner}/{repo}/actions/artifacts` rather than
waiting out the natural expiry.

## Smaller items

- **README/`.env.example`**: added `PLATFORM_REQUIRE_2FA` and
  `TWO_FACTOR_TRUST_DAYS` to the Environment Variables reference table and
  to `.env.example` (previously only mentioned in prose, or not mentioned at
  all — bug-0240's original fix in PR #338 only partially covered this).
  No further action needed unless another 2FA-related env var is added
  without doc coverage.
- Re-run `gh run list --branch master --workflow e2e` at the start of the
  next review — if it's still all-red, that's still bug-0259 until #335
  merges; don't re-file it.
