# TODO — 2026-09-14 daily review

## `dev` has had zero commits in 4 days, and a 9-PR unmerged backlog

The last commit on `dev` is `ccf2cea` (2026-09-10, port migration). This
run's "look at the last 24 hours" brief found nothing to review because
nothing has landed since then. Meanwhile 9 PRs sit open against `dev`,
oldest from 2026-09-08 (6 days):

| PR | Opened | Title |
|----|--------|-------|
| #387 | 09-10 | docs: 2026-09-10 daily code review bundle |
| #386 | 09-10 | fix(bug-0287): revert auth-server local-dev URLs from https to http |
| #385 | 09-09 | docs: 2026-09-09 daily code review bundle |
| #384 | 09-09 | fix(bug-0286): add 10 missing fr.json translation keys |
| #383 | 09-09 | fix(bug-0285): reject private/link-local/metadata IP literals in app URLs |
| #382 | 09-08 | docs: daily code review bundle — 2026-09-08 |
| #380 | 09-08 | fix(bug-0283): roles page App mock missing password-policy fields |
| #379 | 09-08 | fix(bug-0282): signup page.tsx invalid export breaks next build |
| #378 | 09-08 | Activation webhook + pending-account signup |

This run adds #388 (bug-0288) and #389 (bug-0289) on top, for 11 total.
**This is now the top risk to this project's velocity**, ahead of any
individual code defect: every day's automated review finds real bugs, opens
a well-formed fix PR, and then... nothing merges. The backlog compounds
daily-review-over-daily-review (see `[[project_daily_review_infra]]` memory
— proxy/PAT issues have also been interrupting some runs entirely, which is
a second, compounding cause). Recommend whoever owns `dev` block time to
triage and merge this queue before more automated PRs pile onto it further —
past a certain size, review fatigue alone makes each individual PR less
likely to get looked at.

## Root cause of the red CI on every open PR: unmerged bug-0283 fix, now compounded by bug-0288/0289

Traced why PRs with completely unrelated diffs (e.g. #386, a one-line URL
scheme revert, and #387, a docs-only bundle) both show `typecheck` and
`admin-e2e` failing on the exact same `app/(admin)/roles/page.tsx` error:
bug-0283's fix (PR #380) was opened 2026-09-08 and never merged, so `dev`'s
own baseline has been failing `typecheck` since before that date. Every
subsequent PR inherits the failure regardless of its own diff, because GitHub
Actions checks out `base + PR diff`, and the base itself is broken.

This run found the same root-cause shape extends further than bug-0283 knew
about — 7 more test files with the identical `App`-type gap (bug-0288) and 2
more with a `User`-type gap (bug-0289) — so merging #380 alone will not turn
`typecheck` green; #388 and #389 need to land too. **Recommend merging #379
(after its now-stale rebase — see BUGS_2026-09-14.md), #380, #388, and #389
together**, in that order, since #388/#389 were branched from the same `dev`
tip #380 was and may need a rebase of their own once #380 lands.

## `dev` has no branch protection — carried forward, still unresolved

Repeated from `TODO_2026-09-04.md`: `gh api repos/rlondner/sassy-auth/branches/dev/protection`
still returns `404` as of this run (spot-checked). This is *why* the backlog
above is possible at all — there's nothing stopping a merge with red checks,
so the growing pile is a matter of no one having merged, not something
technically blocked. Given how much of this run's signal came from tracing
red checks back to an unmerged fix, this is worth revisiting: even a
non-blocking status check would have made the `typecheck`-is-broken-on-`dev`
condition more visible sooner than "someone reads 9 PRs individually and
notices they're all red for the same underlying reason."

## `Trivy SCA & Vulnerability Scan` failing identically on every open PR — not traced this run

Distinct from the `Trivy` check (which passes), `Trivy SCA & Vulnerability
Scan` fails on all 9 open PRs. Spot-checked two (#386, #387) — same failure
shape as the `typecheck` cascade, but not traced to a root cause this run
(out of scope once the `typecheck` cause was confirmed and the two new bugs
were filed). Worth a follow-up: could be the same "broken `dev` baseline"
shape, or a separate, unrelated CI issue (e.g. a scanner config or
rate-limited external call) that happens to coincide.

## `pnpm install` not run in this session's working copy

This review's typecheck findings (bug-0288, bug-0289) were verified by
running `tsc --noEmit` directly rather than through `pnpm typecheck`, and
turned up two additional errors that are environmental, not real bugs:
`Cannot find module '@marsidev/react-turnstile'` and `Cannot find module
'@jest/globals'` — both packages exist in `package.json` but `node_modules`
was never installed in this checkout. Excluded from the bug filing since
they're a local-environment gap, not a `dev`-branch defect; noting only so a
future run doesn't need to re-derive that these two are false positives.

## Carried over from prior runs

- **Nonce validation is the relying party's job, undocumented in the
  README** — still true, not touched this window.
- **`ai-security-review.yml`'s stray trailing `)`** — not re-checked this
  run (no diff touched that file); presumed still present.
- **`packages/db/index.ts`'s eager `DATABASE_URL` throw** — local dev
  ergonomics gap, still open, no README callout added yet.
- **`apps/admin-e2e/tests/signup.spec.ts` — happy path only** — gaps noted
  2026-09-04 (`appNotFound`, `emailTaken`) not re-checked this run.
- **`changePassword` / `revokeOtherSessions` default** and **no
  `SaAuditEvent` for password resets** — both still true, no new work
  landed this window.
- **`packages/ui` coverage gaps** (`use-mobile.tsx` 0%, `data-table.tsx`
  69.23% branches, `confirm-dialog.tsx` 76.92% branches) — not touched.
