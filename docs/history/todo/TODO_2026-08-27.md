# TODO — 2026-08-27 daily review

## Escalate: the review-PR backlog is growing again, unmerged review PRs are compounding

As of this run there are **10 open PRs**, all daily-review output going back to
2026-08-24 (#349–#361, minus a couple already accounted for): #349, #350, #351,
#352, #353, #354, #355, #357, #358, #359, plus today's #360/#361/this bundle.
That's healthier than the 72+ peak on 2026-08-21, but it's climbing again from
the ~7 seen on 2026-08-24-morning, and — because every review branches from
`master` rather than the prior day's unmerged bundle — `CHANGELOG.md`/
`README.md`/`BUGS_*.md` are diverging across them (this run's `BUGS_2026-08-27.md`
doesn't know about #357/#358's `BUGS_2026-08-25.md` content, because `master`
never absorbed it). **Recommend a merge pass on #349–#361 before the next daily
review**, oldest first — none of them touch overlapping files as far as this
run could tell, so a straightforward merge queue should apply cleanly.

## Follow-ups from today's CI-health check

- `packages/ui/src/hooks/use-mobile.tsx` is at 0% first-party coverage — called
  out by name in `2c888ba` as a known gap, not addressed here since it isn't
  what's failing the coverage floor (see bug-0273's "Verified — not filed").
  Worth a direct test (`matchMedia` mock + resize) next time someone's in
  `packages/ui`.
- `data-table.tsx` (69.23% branches) and `confirm-dialog.tsx` (76.92% branches)
  are the next-lowest first-party files in `packages/ui` — not blocking
  anything today, but the closest to becoming the next coverage-floor surprise
  if the floor is ever ratcheted up.

## Carried over from prior runs (still open)

- `ai-security-review.yml`'s stray trailing `)` in the `claude_args` string
  (introduced in `16e6055`, line 50) — confirmed cosmetic (traced through
  `claude-code-action`'s pre-escaping of shell control characters), one-line
  cleanup whenever someone's next in that file. Not urgent.
- Root-level untracked `code_reviews/` directory drift: another wrong-path
  `docs/history/code_reviews/CR_2026-08-27.md` stub (underscore, pre-reorg
  path) was found and superseded this run — same failure mode as 2026-08-20,
  08-21, and 08-25 (an earlier same-day attempt with no `gh`/API access falls
  back to a local-only review and writes to the old path). It had no findings
  beyond the auth-failure report, so it was not preserved. This keeps
  recurring often enough to be worth a structural fix rather than a
  per-occurrence cleanup — e.g. having the scheduled task check `gh auth
  status` first and skip writing any file at all if it's unauthenticated,
  rather than producing a stub that has to be found and reconciled by the
  next run.
