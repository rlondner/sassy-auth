# TODO — 2026-09-09 daily review

## Growing unmerged-PR backlog on `dev`

Before this run, four PRs against `dev` were already open — none from
yesterday's run had merged except bug-0284 (#381):
- #378 `feat/activation-webhook` (open since 2026-09-08)
- #379 `fix(bug-0282)` (open since 2026-09-08)
- #380 `fix(bug-0283)` (open since 2026-09-08)
- #382 `docs: daily code review bundle — 2026-09-08` (open since 2026-09-08)

This run adds three more: #383 (bug-0285), #384 (bug-0286), and this docs
bundle. That's seven open PRs against `dev` as of today, several sitting
unmerged for over 24 hours already. Nothing is auto-blocking since `dev`
has no branch protection (see below), so the backlog isn't caused by red
CI — it looks like nobody with merge authority has been reviewing/merging
day-to-day. Worth flagging to whoever owns the merge queue: at this rate
the gap between "reviewed and fixed" and "actually in `dev`" keeps
widening, which increases rebase/conflict risk for every subsequent day's
review (this run's fixes were at least rebase-clean against a
freshly-fetched `dev`, but that gets less likely to hold as the queue
grows).

## Local `dev` was 5 commits behind `origin/dev` at the start of this run

Fast-forwarded cleanly to `53b2360`. Worth a process note: if the machine
running these daily reviews doesn't always start from a freshly-fetched
`dev`, a review can silently work from stale code (missing the last day's
merges) or — worse — recreate bug numbers already used on `origin/dev`
(this run's bug-0285/bug-0286 numbering was chosen only after confirming
`origin/dev`'s actual latest bug file, `bug-0284.md`, via the fetch — a
review that skips the fetch step risks a bug-number collision).

## Orphaned worktree: `.worktrees/bug-0285-webhook-ssrf`

Found checked out to `feat/activation-webhook` (PR #378's branch) with
staged-but-uncommitted changes: a new
`apps/auth-server/src/common/config/webhook-target-policy.ts` (+ spec) and
edits to `notify-activation.ts`/`.spec.ts`. This looks like leftover,
in-progress work from a prior session that was investigating the same
webhook-SSRF area this run's bug-0285 also found (independently, via
`app-url-policy.ts` rather than a new `webhook-target-policy.ts`) — and
looks further along toward a request-time/DNS-rebinding-safe fix than
bug-0285's static IP-literal check. Left entirely untouched this run (not
safe to force-commit someone else's uncommitted WIP without more context
on what state it's in or whether it's still wanted). Whoever picks up PR
#378 next should either finish and commit that worktree's change or
`git worktree remove` it if it's abandoned — as-is it's invisible to
`git status` on the main worktree and easy to lose track of.

## `feat/activation-webhook` (PR #378) — SSRF hardening still incomplete

bug-0285 (fixed this run) closes the IP-*literal* gap in
`isAppUrlAllowed()`, which the PR's new `webhookUrl` field also relies on.
But the PR's actual delivery code
(`apps/auth-server/src/activation/notify-activation.ts`) does a single
`fetch()` to whatever `webhookUrl` string is stored — a hostname that
validates as public at admin-PATCH time and later *resolves* to a private
address (DNS rebinding) isn't caught by any static check. The PR's own
final commit (`fix: address final review findings (SSRF redirect guard...)`
already added `redirect: 'error'` to close the redirect-based variant of
this, so the team is clearly already tracking this class of issue — the
remaining gap is specifically the initial-resolution case. Given the
orphaned worktree above already has a `webhook-target-policy.ts` in
progress, this is likely already being worked — worth confirming with
whoever owns that branch before PR #378 merges.

## Carried over from prior runs

- **`dev` has no branch protection** — CI checks (`typecheck`, `unit
  tests`, `e2e`, `trivy`, `ai-security-review`) remain informational only.
  Not re-verified this run (no new signal), carrying forward unchanged.
- **`packages/db/index.ts`'s eager `DATABASE_URL` throw** — local dev
  ergonomics gap, still undocumented beyond the README's general env-setup
  step. Carrying forward.
- **Nonce validation is the relying party's job, undocumented in the
  README** — still true, not touched this window. Carrying forward.
- **`ai-security-review.yml`'s stray trailing `)`** — not re-checked this
  run; presumed still present. Carrying forward as a whenever-convenient
  cosmetic fix.
- **`packages/ui` coverage gaps** (`use-mobile.tsx` at 0%,
  `data-table.tsx`/`confirm-dialog.tsx` branch coverage) — not touched by
  this window's diff; carrying forward unchanged.
- **`changePassword` / `revokeOtherSessions` default** and **no
  `SaAuditEvent` for password resets** — both still true, no new work
  landed this window; carrying forward.
- **PrismaPg adapter singleton / pool sizing / transaction test coverage**
  (from the 2026-09-04 Prisma 7 migration review) — not re-verified this
  window; carrying forward.
