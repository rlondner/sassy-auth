# TODO — 2026-07-01

Daily review follow-up items. No new commits for the 13th consecutive day. 5 new bugs found (2 warning, 2 minor, 1 info) via focused scan across E2E test reliability, admin UI dropdown completeness, auth-server test coverage, and configuration completeness.

## Priority 1 — Critical bugs (blocking production)

- [ ] **bug-0001** — Org-scope `checkPermission`: add `targetOrgId` parameter and enforcement. 35 days open.
- [ ] **bug-0038** — JWT `permissions` → `scope` breaking change: add version marker or migration path. 28 days open.
- [ ] **bug-0039** — In-memory OAuth code store: migrate to Redis or DB table. 28 days open.
- [ ] **bug-0054** — `redirect_uri` validation bypass: implement origin-matching in the authorize flow. 25 days open.
- [ ] **bug-0074** — Inactive user auth bypass: add `saUser.status` check before issuing JWTs in both OAuth and direct login. 24 days open.
- [ ] **bug-0094** — `checkPermissionForApp` silent grant when `targetAppId` is undefined. 12 days open.

## Priority 2 — New warning bugs (today)

- [ ] **bug-0126** — `raceSuccessOrError` silently passes when both promises timeout. All 5 E2E page objects affected. Add `null` outcome check to throw on "neither toast appeared".
- [ ] **bug-0127** — UsersTable has 4 non-functional dropdown menu items (Reset Password, Resend Invitation, Deactivate, Activate). Either implement handlers or remove/disable the items.

## Priority 3 — New minor/info bugs (today)

- [ ] **bug-0128** — `getRole` test suite missing `ForbiddenException` coverage for caller-not-found path. Add test case.
- [ ] **bug-0129** — `.env.example` missing `SEED_DEMO_MULTITENANT` variable. Add it after `SEED_DEMO=`.
- [ ] **bug-0130** — `check-key.js` debug script left in `apps/auth-server/`. Delete or gitignore.

## Priority 4 — Uncommitted changes

- [ ] Commit the toast/refresh integration (15 admin drawer/table surfaces) — address bug-0112 (French i18n) first.
- [ ] Commit the `resolveIssuer()` DRY refactor and `NEST_GLOBAL_PREFIX` export.
- [ ] Commit the E2E `raceSuccessOrError` scoping fix and OAuth route stub narrowing.
- [ ] Commit the roles `platform.users.manage` read-gate widening.

## Priority 5 — Open warning/minor bugs from prior reviews

- [ ] **bug-0112** — Translate 15 French toast i18n strings in `fr.json`.
- [ ] **bug-0113** — Fix Playwright workers dead ternary (`CI_TESTS ? 1 : 1` → `CI_TESTS ? 1 : 2`).
- [ ] **bug-0114** — Add `.catch()` to `refresh()` calls in 4 admin tables.
- [ ] **bug-0115** — Add URL validation to `resolveIssuer()`.
- [ ] **bug-0116** — Add `setViewOpen(false)` to users-table delete handler.
- [ ] **bug-0117** — Replace hardcoded English validation strings in user-create-drawer with i18n keys.
- [ ] **bug-0118** — Wrap user-create-drawer fields in `<form onSubmit>`.
- [ ] **bug-0119** — Add `aria-label="more actions"` to users-table dropdown trigger.
- [ ] **bug-0120** — Add `role="alert"` to DeleteAlertDialog error div.
- [ ] **bug-0121** — Add `targetOrgId` to `OrgsService.listOrgs`/`getOrg` permission checks.
- [ ] **bug-0122** — Remove double `stripTrailingSlash` in `oauth-metadata.ts`.
- [ ] **bug-0123** — Add `canWrite` guard to role-view-drawer Edit/Delete buttons.
- [ ] **bug-0124** — Replace hardcoded "Loading…" in user-view-drawer with i18n key.
- [ ] **bug-0125** — Fix `deleteRole` P2003 error user count race condition.

## Project health

- **Days since last commit:** 12
- **Days since last fix merged to production:** 35
- **Open bugs:** 128 (was 123)
- **Critical bugs open:** 6
- **Warning bugs open (new today):** 2
- **Minor/Info bugs open (new today):** 3
