# Follow-up Items — 2026-07-08

Post-fix regression scan after the overnight autonomous session that closed ~76 bugs (including all 9 Critical). Today's review focused on correctness and completeness of the fixes, not just new surface-level bugs.

## Critical (2) — timing side-channels that undermine the bug-0209 fix

- [ ] **bug-0214** — Move org/app mismatch check in `directLogin` to AFTER password verification; return `INVALID_CREDENTIALS` (not `USER_ORG_MISMATCH`). The current placement defeats the timing guard from bug-0209.
- [ ] **bug-0215** — Add dummy `verifyPassword` call for social-only users (no credential row) in `directLogin`. Without it, the absence of scrypt leaks the authentication method.

## Warning (6) — fix this week

- [ ] **bug-0216** — Build the full path for the post-login OAuth redirect: `/${NEST_GLOBAL_PREFIX}/${TOKEN_CONTROLLER_PATH}/${OAUTH_AUTHORIZE_ROUTE}?...`. The current relative path is always rejected by the admin's `validateNextUrl`.
- [ ] **bug-0217** — Disable Helmet CSP in non-production environments so Swagger UI works. The code comment saying "CSP is only in prod" is incorrect.
- [ ] **bug-0218** — Guard ALL transitions to `active` in `updateUser`, not just `pending→active`. The two-step bypass `pending→inactive→active` circumvents invitation acceptance.
- [ ] **bug-0219** — Add self-modification guard for `status` field in `updateUser`, matching the pattern in `deleteUser`, `setUserRoles`, `setUserDirectPermissions`.
- [ ] **bug-0220** — Add a scheduled cleanup job for expired `SaOauthCode` rows. The in-memory store was migrated to DB (bug-0039 fix) but the unbounded-growth issue from bug-0040/0073 persists.
- [ ] **bug-0221** — Add try/catch to `updateUserAction` that re-throws NEXT_REDIRECT errors and returns a structured result for other errors, matching sibling actions.

## Warning — still open from prior catalogs (selected)

- [ ] **bug-0057** — `setLocaleAction` open redirect via unvalidated `pathname` parameter.
- [ ] **bug-0075** — `listUsers` accepts `appPublicId` filter parameter but silently ignores it.
- [ ] **bug-0151** — `deleteUser` orphans BetterAuth `User`, `Account`, `Session` rows.

## Minor (7) — backlog

- [ ] **bug-0222** — Add `let cancelled = false` + cleanup return to `user-view-drawer` initial-load effect (matching sibling drawers).
- [x] **bug-0223** — Add `Strict-Transport-Security` header to admin `next.config.ts` security headers array, gated behind production check.
- [ ] **bug-0224** — Add `role="alert"` to accept-invite-form error paragraph.
- [ ] **bug-0225** — Add `aria-label` to 5 search inputs across table components.
- [ ] **bug-0226** — Add `aria-label` to pagination page-size selects.
- [ ] **bug-0227** — Localize the hardcoded English error in `use-copy-feedback` hook.
- [ ] **bug-0228** — Add `prisma.saOauthCode.deleteMany()` to E2E `afterAll` cleanup.

## Items from the overnight report needing user input

These were flagged in `OVERNIGHT_REPORT_2026-07-08.md` — no autonomous action taken:

- [ ] **bug-0038** — Confirm no external clients read the `permissions` JWT claim before shipping the `scope`-only format.
- [ ] **bug-0143** — Choose between `onDelete: Restrict` (admin-friendly) vs. keeping cascade (current) for permission-role/permission-user relations.
- [ ] **bug-0157** — Decide whether JWT `scope` should be app-scoped or include all user perms (current).
- [ ] **bug-0161** — Decide seed idempotency strategy for half-provisioned platform admins.

## README updates applied today

- Removed 11 Known Limitations items that were fixed in the overnight session (bug-0080, 0097, 0034, 0115, 0153, 0154, 0164, 0165, 0179, 0191, 0197).
- Remaining Known Limitations: bug-0047 (redirect_uri granularity), bug-0041 (PKCE format), CI lint, bug-0151 (deleteUser orphans).
