# TODO — 2026-08-28 daily review

## Escalate: 14 open PRs, none merged since 2026-08-25 — recommend a merge pass

As of this run there are **14 open PRs**, all daily-review output going back to
2026-08-24: #349–#355, #357–#363. That's every bundle since 2026-08-24 plus
every individual bug-fix PR it spawned, still sitting open. Because each
review branches from `master` rather than from the prior day's unmerged
bundle, `CHANGELOG.md` / `README.md` / `BUGS_*.md` keep diverging across them
— none of them know about each other's doc changes, only `master` could
reconcile that, and `master` hasn't moved since `ea82b95` on 2026-08-25.

This is now the fourth consecutive day this note has been raised (2026-08-24
noted #349–#354 open; 2026-08-25 flagged the pattern by name; 2026-08-27
recommended a merge pass explicitly). **Repeating the same recommendation a
fourth time without it landing suggests the note itself isn't the
bottleneck** — worth deciding explicitly whether unmerged review PRs are
acceptable as a permanent backlog (in which case the daily review should stop
flagging it) or whether merging needs to be delegated/automated rather than
requested.

Concretely, still recommended: a merge queue over #349–#363 oldest-first — as
of the 2026-08-27 pass none of them were found to touch overlapping files, so
a straightforward sequential merge should apply cleanly. #360 and #361 in
particular are worth prioritizing — they're the fix for `master`'s currently
red `unit-tests` CI job (see below).

## `master`'s `unit-tests` CI job is still red

Confirmed still failing as of this run's live CI check (last run against
`ea82b95`, red since 2026-08-25 04:33 UTC). Root causes were diagnosed and
fixed on 2026-08-27 (bug-0273, bug-0274), but both fixes live in still-open
PRs #360/#361. No new root cause found today — re-diagnosing the same failure
a third day running would be wasted effort; merging #360/#361 is the
remaining step.

README's `Node.js >= 20` prerequisite and badge were already stale against
the Node 24 migration (`5e21bfe`, 2026-08-25) and were fixed once already in
the unmerged #362. Fixed again directly on `master` via today's bundle (this
review branches from `master`, so the fix needed to be reapplied rather than
inherited from #362) — see CHANGELOG.

## Follow-ups from today's bug-0275 fix

- The revoke-other-sessions call in `confirmEnable` is best-effort and
  currently silent on failure (no logging exists anywhere in
  `apps/admin/app/account/security/actions.ts` today, so this matches the
  file's existing style). If observability is ever added to this file, a
  failed revoke here is worth surfacing — it's a security-relevant no-op, not
  routine noise.
- The same gap doesn't apply to `disable2fa` — disabling 2FA rotates the
  caller's own session and does not need to touch others, since the
  `twoFactorEnabled` flag flipping to `false` only makes `amr` claims more
  conservative for other sessions, not less.
- Backup-code regeneration (`regenerateBackupCodes`) does not rotate or
  affect session state at all and was not in scope for this bug.

## Carried over from prior runs (still open)

- `ai-security-review.yml`'s stray trailing `)` in the `claude_args` string
  (introduced in `16e6055`, line 50) — confirmed cosmetic in the 2026-08-27
  review (traced through `claude-code-action`'s pre-escaping of shell control
  characters), one-line cleanup whenever someone's next in that file. Not
  urgent.
- `packages/ui/src/hooks/use-mobile.tsx` is at 0% first-party coverage —
  called out by name in `2c888ba` as a known gap, not addressed since it
  isn't what's failing the coverage floor. Worth a direct test (`matchMedia`
  mock + resize) next time someone's in `packages/ui`.
- `data-table.tsx` (69.23% branches) and `confirm-dialog.tsx` (76.92%
  branches) are the next-lowest first-party files in `packages/ui` — not
  blocking anything today, but the closest to becoming the next
  coverage-floor surprise if the floor is ever ratcheted up.
- A stray wrong-path `docs/history/code_reviews/CR_2026-08-28.md` stub
  (underscore, pre-reorg path) was found untracked at the start of this run —
  same failure mode as 2026-08-20, 08-21, 08-25, and 08-27 (an earlier
  same-day attempt with no `gh`/API access falls back to a local-only review
  and writes to the old path). It had no findings beyond an auth-failure
  report and was removed; this run's own `gh` access worked fine. This keeps
  recurring often enough across five occurrences now to be worth a structural
  fix — e.g. the scheduled task checking `gh auth status` up front and
  skipping the write entirely when unauthenticated, rather than producing a
  stub every retry has to find and reconcile.
