# TODO — 2026-08-30 daily review

## Escalate: 16 open PRs, none merged since 2026-08-25 — recommend a merge pass

As of this run there are **16 open PRs**, all daily-review output going back
to 2026-08-24: #349–#365. That's every bundle since 2026-08-24 plus every
individual bug-fix PR it spawned, still sitting open — one more than the
2026-08-28 count (14), since that run's own bundle (#363, #364) and this
run's (#365, plus today's bundle PR once opened) keep adding to the pile
faster than anything merges.

This is now the **fifth** consecutive daily review to raise this exact note
(2026-08-24, 08-25, 08-27, 08-28, 08-30). Per the 2026-08-28 note's own
observation, repeating the recommendation isn't moving it — this run is not
repeating it a sixth time with new urgency language, just recording that the
backlog is still growing and flagging it once more for visibility. The
concrete unblock is unchanged: a merge queue over #349–#365 oldest-first;
no cross-PR file-overlap conflicts have been found in any pass so far, so a
straightforward sequential merge should apply cleanly. #360 and #361 remain
worth prioritizing first — they fix `master`'s currently-red `unit-tests`
CI job (see below).

## `master`'s `unit-tests` CI job is still red

Confirmed still failing as of this run's live CI check (last run against
`ea82b95`, red since 2026-08-25 04:33 UTC — coverage-floor miss on
`packages/ui` plus a Node-20 workflow runner, per bug-0273/bug-0274). Fixes
live in still-open PRs #360/#361. No new root cause investigation today —
this would be the fourth day re-confirming the same finding; merging
#360/#361 is the only remaining step.

## Follow-ups from today's bug-0276 fix

- `changePassword` (the authenticated in-session "change my password" flow)
  exposes `revokeOtherSessions` as a caller-supplied boolean on BetterAuth's
  `update-user` endpoint rather than defaulting it on server-side. No caller
  of that endpoint was found wired up in this codebase today — only the
  token-based forgot/reset flow is in use. If a settings-page password-change
  feature is ever added, make sure that call site passes
  `revokeOtherSessions: true` (or an explicit user-facing choice) rather than
  silently inheriting the same gap bug-0276 just fixed for the token flow.
- Neither `requestPasswordResetAction` nor `resetPasswordSubmitAction`
  (`apps/admin/app/{forgot,reset}-password/actions.ts`) writes a
  `SaAuditEvent`. Not filed as its own bug — no login-adjacent action writes
  one today — but worth folding in if/when audit coverage is extended to
  session/security-state changes, since "sessions were mass-revoked by a
  password reset" (now that bug-0276 makes that happen) is a more useful
  audit-trail entry than "a password changed."

## Carried over from prior runs (still open)

- `ai-security-review.yml`'s stray trailing `)` in the `claude_args` string
  (introduced in `16e6055`, line 50) — confirmed cosmetic (2026-08-27), one-
  line cleanup whenever someone's next in that file. Not urgent.
- `packages/ui/src/hooks/use-mobile.tsx` is at 0% first-party coverage —
  called out by name in `2c888ba` as a known gap. Worth a direct test
  (`matchMedia` mock + resize) next time someone's in `packages/ui`.
- `data-table.tsx` (69.23% branches) and `confirm-dialog.tsx` (76.92%
  branches) are the next-lowest first-party files in `packages/ui` — not
  blocking anything today, but the closest to becoming the next
  coverage-floor surprise if the floor is ever ratcheted up.
- The recurring stray wrong-path `docs/history/code_reviews/` (underscore,
  pre-reorg path) stub, seen on 2026-08-20/21/25/27/28, was **not** found
  today — first run in a while without one. Leaving the structural fix
  suggestion (check `gh auth status` up front, skip the write entirely when
  unauthenticated) on the table in case it recurs, but not re-escalating it
  since it didn't happen this time.
