# TODO — 2026-07-02

Follow-up items from the daily code review of commit `37f738a`.

## Priority 1 — Fix before next release

- [ ] **bug-0131** — Scope `raceSuccessOrError` success text check to `[data-sonner-toaster]` (currently matches anywhere on the page). All 5 E2E page objects.
- [ ] **bug-0126** (prior) — Add null-outcome check to `raceSuccessOrError` — neither toast appearing within 10s should throw, not silently pass.
- [ ] **bug-0132** — Fix `user-create-drawer.tsx` error handling to use `errorKey` + `t()` pattern matching other drawers. Also translate hardcoded validation strings.
- [ ] **bug-0112** (prior) — Translate the 15 French toast i18n strings in `fr.json` (apps/orgs/permissions/roles/users `.toast.created/.updated/.deleted`).

## Priority 2 — Fix soon

- [ ] **bug-0135** — Clear `roleIds`/`directPermissionIds` in `user-create-drawer.tsx` when org changes, to prevent stale cross-org role assignments.
- [ ] **bug-0133** — Fix double-await on rejected promise in `roles.service.spec.ts` `deleteRole` P2003 test.
- [ ] **bug-0134** — Add `apps.get` operation to the permissions matrix to cover the `GET /api/apps/:id` endpoint.
- [ ] **Extract `raceSuccessOrError` and `escapeRe`** into a shared E2E module — currently duplicated 5x across page objects.

## Priority 3 — Backlog

- [ ] **bug-0119** (prior) — Add `aria-label` to users-table dropdown trigger (the only table missing it).
- [ ] **bug-0117** (prior) — Replace hardcoded English validation messages in `user-create-drawer.tsx` with i18n keys.
- [ ] **bug-0124** (prior) — Replace `"Loading..."` in `user-view-drawer.tsx` with `t('common.loading')`.
- [ ] **bug-0113** (prior) — Simplify Playwright workers from `CI_TESTS ? 1 : 1` to `workers: 1` and update the stale "cap at 2" comment.

## Observations (not bugs)

- The Sonner toast integration is well-executed — consistent `onSuccess` callbacks, theme-aware via `next-themes`, and all 15 CRUD paths covered. The `users-table` correctly uses `router.refresh()` since `/users` is a Server Component, while other tables use client-side list actions.
- The `resolveIssuer()` + `ISSUER_PLACEHOLDER` refactor correctly unifies the JWT `iss` and discovery `issuer`. The startup warning for unset `BETTER_AUTH_URL` is a good observability improvement.
- The broadened `platform.users.manage` gate on `listRoles`/`getRole` mirrors the established pattern for orgs/permissions and eliminates the cross-page permission grant requirement. Matrix tests and unit tests were updated.
- The E2E scoping fix (restricting error detection to toast/dialog containers instead of the whole page) correctly addresses the Next.js Dev Tools `role="alert"` false-win issue.
