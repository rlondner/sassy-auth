# 🔍 Daily Code Review — sassy-auth
**Date:** 2026-08-13
**Status:** ✅ Review executed — GitHub access available via `gh` CLI (keyring token)

> **Data-source note.** `curl https://api.github.com` is still blocked by the sandbox proxy
> allowlist, and a raw `git fetch` still fails on the rejected `.env.local` PAT
> (`Invalid username or token`). This run used the authenticated **`gh` CLI** (keyring
> `gho_…` token) — `gh pr list/view/diff` worked and pulled live PR metadata + diffs from
> `origin`. Prefer `gh` for future runs; rotate the `.env.local` PAT and/or allowlist
> `api.github.com` regardless (see TODO).

---

## Scope of the window

- **Window:** ~2026-08-12 09:00 → 2026-08-13 09:07 UTC.
- **`master`:** still frozen at `a78d4a7` (2026-07-15, the 2FA merge) — **29 days** with no
  merge. No direct pushes to `master`.
- **New commits on `master` in window:** 0.
- **PR activity in window:** exactly **one** new PR — **#316** (`google-labs-jules[bot]`),
  created 2026-08-13 08:09 UTC, last updated 09:07 UTC. Reviewed in full below. (PRs #315/#314
  were reviewed 2026-08-12 in `CR_2026-08-12.md`; no further activity on them this window.)

## PR #316 — 🎨 Palette: Standardized Table Copy and Row Action Accessible Tooltips

- Author: `google-labs-jules[bot]` (opened by @rlondner) · Branch:
  `palette/accessible-table-tooltips-6451768424245745641` · Base: `master`
- 18 files · **+163 / −79**

**What it intends:** wrap the Sqid copy button and the `more_vert` row-action trigger of the 5
admin tables in Radix `<Tooltip>` from `@sassy-auth/ui`, with localized `aria-label`s + a
dynamic "copied!" announcement; expand `common.*` en/fr strings; update table tests.

### Findings

- 🔴 **Critical — Tooltip with no provider → every table page crashes ([bug-0249](../bugs/bug-0249.md)).**
  `@sassy-auth/ui`'s `Tooltip` is `TooltipPrimitive.Root`. In `@radix-ui/react-tooltip@1.2.8`
  the provider context is created with **no default** (`createTooltipContext(PROVIDER_NAME)`),
  so `Root` **throws** `` `Tooltip` must be used within `TooltipProvider` `` at render. The admin
  app has **zero** `TooltipProvider` and the PR adds none; #316 is the first `<Tooltip>` in
  `apps/admin`. Apps/Orgs/Roles/Permissions/Users all render the offending cells → all crash.
  **Fix:** mount one `<TooltipProvider>` around `AdminShell` / in `app/layout.tsx`.
- 🔴 **Critical — bundled seed 2FA-prompt suppression ([bug-0250](../bugs/bug-0250.md)).**
  `seed.ts`, `demo-multitenant.ts`, `demo-resource-server.ts` add
  `twoFactorPromptedAt: new Date()`. `shouldPromptTwoFactor` returns `true` only when
  `promptedAt` is null, so seeded platform admins + demo users are treated as "already prompted"
  → **no 2FA enrollment prompt** for `TWO_FACTOR_TRUST_DAYS` (14). Not cleared by re-seed (write
  is in the create branch of an idempotent upsert). Identical change also in PR #315. Absent on
  `master`. **Fix:** leave the field null, or gate behind an explicit
  `SEED_SUPPRESS_2FA_PROMPT` flag fenced off production.
- 🟡 **Warning — scope creep + self-blinding tests ([bug-0251](../bugs/bug-0251.md)).** A
  UI-titled PR silently edits `auth.config.ts` (`autoSignIn: false`) and all three seed files
  with no mention in title/body — the bug-0247/0248 pattern again. The table tests
  `jest.mock('@sassy-auth/ui', …)` with `Tooltip: Passthrough` / `TooltipTrigger: Trigger`, so
  the real primitive never runs and CI can't catch bug-0249. This is bug-0093 recurring.
- 🟡 **Minor — nested double-`asChild` on the row-action trigger ([bug-0252](../bugs/bug-0252.md)).**
  `TooltipTrigger asChild` → `DropdownMenuTrigger asChild` → one `<button>` in all 5 tables.
  Fragile Radix Slot merge; tooltip may not dismiss when the menu opens. Secondary to bug-0249.
- 🟢 **Positives.** The `aria-label` for the copy button now uses `t('common.copy')` /
  `t('common.copied')` and the `more_vert` button's previously **hardcoded** `"more actions"` is
  now `t('common.moreActions')` — a genuine i18n/a11y improvement. en/fr `common.*` additions are
  clean and symmetric. `autoSignIn: false` is defensible hardening **in isolation** (the problem
  is that it rides in unreviewed under a tooltip title, and duplicates #315).

**Recommendation:** 🔴 **Request Changes / do not merge as-is.** Blockers: add a
`TooltipProvider` (bug-0249) and de-mock at least one table test to prove it; drop the
`twoFactorPromptedAt` seed writes (bug-0250). Then split the auth/seed edits out of the UI PR
(bug-0251) and de-duplicate against #315/#314.

## Recent commits (0 new on `master` in last 24h)

`master` HEAD is still `a78d4a7` (2026-07-15). All window activity is on PR #316's branch. The
local clone has **no commits in the strict `--since="24 hours ago"` window** (local `master`
== `origin/master` == `a78d4a7`; Jules branches are not fetched locally — PR data came from
`gh`).

## Cross-PR / merge-hygiene state (carried forward)

- **Three open Jules PRs collide.** #314 (FormField/drawers), #315 (validation split +
  `autoSignIn:false` + seed 2FA writes), #316 (tooltips + `autoSignIn:false` + seed 2FA writes)
  all touch `auth.config.ts` + seeds + admin table/drawer surface. #315 and #316 carry the
  **identical** bug-0250 seed regression. Whichever merges second conflicts.
- **`master` red since `a78d4a7`.** The `apps/admin` Jest suite (bug-0243) has been independently
  patched by several Jules PRs because the standalone fix never merged; CI is not gating on it.
- **Backlog is the root cause.** Daily-review output (bugs, CR, CHANGELOG, README) keeps stacking
  in unmerged PRs, so `master`'s own `CHANGELOG.md`/`README.md` lag these branch copies.

## Summary — items needing attention (prioritized)

1. 🔴 **bug-0249 — add a `TooltipProvider`** before PR #316 can render at all. De-mock one table
   test so this class of failure fails CI.
2. 🔴 **bug-0250 — remove the seed 2FA-prompt suppression** from #315 **and** #316 (or gate it).
3. 🟡 **bug-0251 — split #316's auth/seed edits out of the UI PR** and de-duplicate #314/#315/#316.
4. 🟡 **bug-0252 — fix the nested-`asChild` row-action trigger** (after 0249).
5. 🟠 **Merge/close the review backlog** and **land bug-0243's real fix** so `master` goes green.
6. 🔧 **Infra:** standardize the scheduled task on the `gh` CLI (worked this run); rotate the
   `.env.local` PAT and/or allowlist `api.github.com` so `git fetch` and `curl` stop failing.

**Overall repo health:** `master` unchanged and safe (none of this is merged), but the only new
PR in the window would take the admin console down on render and silently weaken 2FA enrollment
if merged — both must be fixed before #316 lands.
