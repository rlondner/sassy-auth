# 🔍 Daily Code Review — sassy-auth
**Date:** 2026-08-05
**Status:** ✅ Review executed — GitHub access restored via `github` MCP connector

> Supersedes an earlier "Review NOT executed" stub from this date: at ~11:50 the connector was
> offline; on this run it was authenticated and available.

## Access (changed from prior runs)

Runs from **2026-07-27 → 2026-08-04** reported "Review NOT executed" because the sandbox proxy
blocks `api.github.com` and the `.env.local` PAT is rejected. **This run**, the `github` MCP
connector was authenticated: `get_me` succeeded (`rlondner`), and both the open-PR list and PR
#306's diff were pulled live from `origin`. No `curl`/PAT path was needed.

## Scope of the window

- **Last-24h window:** 2026-08-04 ~08:00 → 2026-08-05 ~08:00 UTC.
- **`master`:** still frozen at `a78d4a7` (2026-07-15, the 2FA merge). No new commits on the
  default branch.
- **New PR in window:** **#306** — `jules-...-28989156` (Jules bot), "🎨 Palette: Interactive
  Show/Hide Password Toggle for LoginForm", opened 2026-08-05 07:49 UTC. Only new source
  activity; reviewed in full.
- Local clone is still on stale branch `daily-review-2026-07-22`; remote bug PRs below were
  created off `origin/master` (`a78d4a7`) via the connector to sidestep the local drift.

## What PR #306 actually changes

Titled as a password-toggle UI tweak, but the diff spans four concerns:

| Concern | Files | Verdict |
|---|---|---|
| ✅ Password show/hide toggle | `login-form.tsx`, `messages/{en,fr}.json`, `login-form.test.tsx` | Fine — `type="button"`, correct aria-labels, `pr-10` padding, ships a unit test |
| 🔴 Session-gate bypass | `auth/auth.config.ts`, `seed/seed.ts` | **bug-0247** — unfenced `SEEDING==='1'`; reintroduces bug-0246 under a new var name |
| 🟡 Bundled bug-0243 fix | `app-create-drawer.test.tsx` | Good change, wrong place — buried in a UI PR (bug-0248) |
| 🟡 Unrelated i18n reformat | `messages/fr.json` | Whitespace-only diff noise (bug-0248) |

## Findings (2 new bugs)

### 🔴 bug-0247 — Warning (security): second, divergent unfenced session-gate bypass

PR #306 adds `if (process.env.SEEDING === '1') return;` to the BetterAuth `create.before`
session hook, disabling the account-status gate (bug-0074 hardening) process-wide with no
`NODE_ENV` fence, no test, no docs. PR #303 (bug-0246) adds the *same* bypass under a different
name (`SEED_RUNNING === "true"`). Two open PRs now patch the same hook incompatibly. Confirmed
absent on `master`. → `bugs/bug-0247.md`.

### 🟡 bug-0248 — Minor (process): triplicate toggle PRs + #306 scope creep

#294, #296, #306 all implement the same login toggle → guaranteed conflicts + wasted review.
#306 also bundles the bug-0243 test fix and an unrelated `fr.json` reformat under a cosmetic
title, hiding the security-relevant `auth.config.ts` edit. → `bugs/bug-0248.md`.

## Risk / missing-test callouts

- No regression test asserts the session gate still runs under `NODE_ENV=production` when the
  seed flag is set — the single test that would catch both bypass variants.
- The `apps/admin` Jest suite has been red since `a78d4a7` and independently patched by three
  Jules PRs (bug-0245, now #306) because bug-0243's standalone fix never merged → CI is not
  gating on this suite.

## Repo health assessment

- **Security posture:** one in-flight Warning (bug-0247) that would regress a former Critical
  (bug-0074) if merged unfenced. Not on `master` yet — catchable before merge.
- **Process:** the dominant problem remains **merge starvation** — `master` frozen 3 weeks,
  daily-review output and Jules PRs accumulating unmerged (#287–#290, #304, #305,
  #294/#296/#306, and today's bug PRs). See `todo/TODO_2026-08-05.md`.

## PRs created this run

Two bug PRs opened off `master` via the connector (one per new bug):

- `bugs/bug-0247` → **PR #307** (bug-0247.md)
- `bugs/bug-0248` → **PR #308** (bug-0248.md)

Both branched off `origin/master` (`a78d4a7`) via the github connector.
