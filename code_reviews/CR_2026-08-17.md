# 🔍 Daily Code Review — sassy-auth
**Date:** 2026-08-17
**Status:** ✅ Review executed — `git fetch origin master` succeeded and `gh` CLI available (keyring `gho_…`).

> **Data-source note.** `gh pr list/view/diff` and `git fetch origin master` both worked this run
> over the keyring credential (`gho_…`, `repo` scope). `curl https://api.github.com` and the
> `.env.local` PAT remain unreliable — prefer `gh`. See `todo/TODO_2026-08-17.md`.

---

## Scope of the window

- **Window:** ~2026-08-15 08:30 → 2026-08-17 08:00 UTC (since the last review).
- **`master`:** still frozen at `a78d4a7` (2026-07-15, the 2FA merge) — **33 days** with no merge.
  New commits on `master` in window: **0**. No direct pushes. `git log --since="24 hours ago"` on
  `master` = empty.
- **Two new PRs in the window** (both Jules bot, both admin-UI titled, both touching `apps/auth-server`):
  - **[#328](https://github.com/rlondner/sassy-auth/pull/328)** — "🎨 Palette: Add localized ARIA
    labels and tooltips to row action triggers" (2026-08-16 07:40 UTC) — 17 files, `+121/−50`.
  - **[#329](https://github.com/rlondner/sassy-auth/pull/329)** — "🎨 Palette: Standardize
    ShareLinkDialog copy feedback hook" (2026-08-17 07:09 UTC) — 4 files, `+80/−5`.
- PRs #325/#326/#327 (updated 2026-08-16 02:06 UTC) were the **2026-08-15 review's own outputs**
  being pushed — excluded from this window.

## PR #329 — ShareLinkDialog copy-feedback refactor

- Branch: `palette/share-link-copy-feedback-16997803198602290621` · Base: `master`.
- **Files (4):** `share-link-dialog.tsx`, `__tests__/share-link-dialog.test.tsx` (new),
  `__tests__/app-create-drawer.test.tsx`, `apps/auth-server/src/auth/auth.config.ts`.
- **Good:** replaces inline `useState`+`setTimeout` with the existing `useCopyFeedback` hook
  (`apps/admin/lib/use-copy-feedback.ts`, **present on `master`** — verified via
  `git cat-file -e master:…`), removing the unmount `setTimeout` leak. Adds a real dialog test.
  Updates the `app-create-drawer` payload assertion (the **bug-0243** fix). All correct.
- **Bad:** also adds `autoSignIn: false` to `auth.config.ts` — unrelated to copy feedback → **bug-0256**.

## PR #328 — row-action tooltips (duplicate + over-scoped)

- Branch: `jules-16680163062728024582-5c7c94b2` · Base: `master`.
- **Duplicate:** re-implements the `aria-label={t('common.moreActions')}` + `en/fr.json`
  localization already carried by open PRs **#316 and #324** (superset of #324, overlaps #316) →
  three-way merge conflict on the 5 tables + 2 message files → **bug-0257** (extends **bug-0254**).
- **Scope creep under a UI title:**
  - `apps/admin/app/login/actions.ts` — rewrites `forwardSessionCookie` to return
    `{ ok, sessionToken }` and thread the fresh token into the `get-session` / `two-factor-status`
    fetches. Plausible robustness improvement to the admin login server action, **no test**.
  - `apps/auth-server/src/auth/auth.config.ts` — `autoSignIn: false` → **bug-0256**.
  - `apps/admin-e2e/auth-state.setup.ts` — waits for `/login/two-factor-prompt` and clicks "Skip for
    now" (implies seeded admins hit the prompt — the *correct* direction, opposite bug-0250).
  - `apps/admin/jest.setup.ts` — **globally** stubs the `@sassy-auth/ui` Tooltip primitives →
    **bug-0258** (escalates bug-0251, masks bug-0252).

## Verification highlights (false-positive discipline)

- **`useCopyFeedback` exists on `master`** → #329's refactor is not a broken import. Not a bug.
- **#328's `login/actions.ts` change is NOT proof of a `master` defect.** On `master`,
  `cookies().set(...)` followed by `cookies().toString()` in the same request **does** include the
  just-set session token, so the 2FA-status lookup is not reading a stale cookie. #328's change is a
  legitimate explicit-threading refactor; the only filable angle is missing-test / scope-creep
  (folded into bug-0257). *(Same discipline that retracted bug-0249.)*

## Findings

| ID | Severity | One-liner |
|----|----------|-----------|
| bug-0256 | 🟠 Medium | `autoSignIn: false` added by 3 open PRs (#316/#328/#329) — untested auth change, 3-way conflict |
| bug-0257 | 🟠 Medium | #328 = third overlapping row-action-tooltip PR + smuggles login/auth/e2e edits under a UI title |
| bug-0258 | 🟢 Low | #328 globally stubs the UI Tooltip in `jest.setup.ts` → admin suite blind to real tooltip behavior |

Prior open findings unchanged: 🔴 bug-0250, 🟡 bug-0251, 🟡 bug-0253, 🟠 bug-0254, 🟢 bug-0255,
🔴 bug-0243 (all in unmerged PRs; `master` unaffected).

## Net

Two more Jules "Palette" PRs, both again riding an `apps/auth-server` change under a cosmetic title;
one (#328) triplicating a UI concern already spread across two open PRs. #329 carries a genuinely
good `useCopyFeedback` refactor worth landing (minus its auth rider). The systemic blocker remains
the **33-day merge freeze**, which keeps regenerating overlapping PRs. See
`bugs/BUGS_2026-08-17.md` and `todo/TODO_2026-08-17.md`.

---

## Second run — 2026-08-17 ~21:03 EDT (verification only, no new artifacts)

The scheduled daily review fired a **second time today**. Re-checked the window with `gh` (keyring
credential, working) and `git log --all --since="24 hours ago"`:

- **No new activity since the 10:36 EDT run.** `git log --all --since="24 hours ago"` returns **only
  this morning's own four review commits** (`34017bf` + `3e955d7`/`50851cf`/`e72f86a` for
  bugs 0256–0258). `master` still frozen at `a78d4a7` (33 days).
- **No PR created or updated after 14:37 UTC.** Newest non-review PRs remain **#329** (07:30 UTC) and
  **#328** (2026-08-16) — both already fully reviewed this morning. This morning's outputs are open as
  **PRs #330 (bug-0256), #331 (bug-0257), #332 (bug-0258), #333 (review bundle)**.

**Decision:** no new bugs filed, no new PRs opened, no duplicate CHANGELOG/BUGS entry. The window is
already fully covered; minting bug-0259+ or duplicate PRs for the same three findings would violate
the repo's dedup discipline. Highest real bug number stays **0258**. This note is the only artifact
of the second run.
