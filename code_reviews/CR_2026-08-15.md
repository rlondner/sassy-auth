# 🔍 Daily Code Review — sassy-auth
**Date:** 2026-08-15
**Status:** ✅ Review executed — `git fetch origin` succeeded and `gh` CLI available (keyring `gho_…`).

> **Data-source note.** `gh pr list/view/diff` and `git fetch origin master` both worked this run
> over the keyring credential (`gho_…`, `repo` scope). `curl https://api.github.com` and the
> `.env.local` PAT remain unreliable — prefer `gh`. See `todo/TODO_2026-08-15.md`.

---

## Scope of the window

- **Window:** ~2026-08-14 15:30 → 2026-08-15 08:30 UTC.
- **`master`:** still frozen at `a78d4a7` (2026-07-15, the 2FA merge) — **31 days** with no merge.
  New commits on `master` in window: **0**. No direct pushes.
- **One new PR in the window: [#324](https://github.com/rlondner/sassy-auth/pull/324)** — Jules
  "🎨 Palette: Add localized ARIA labels to table row action buttons" (opened 2026-08-15 07:36 UTC).
  Everything else surfaced by `gh pr list` last updated 2026-08-14 or earlier (the 15:30 updates on
  #322/#323 were the *prior* run's own outputs).

## PR #324 — localized `common.moreActions` on the 5 admin tables

- Author: `google-labs-jules[bot]` (opened by @rlondner) · Branch:
  `palette/localize-table-row-actions-aria-label-14231760063474182685` · Base: `master`.
- **Files (8):** `apps-table.tsx`, `orgs-table.tsx`, `permissions-table.tsx`, `roles-table.tsx`,
  `users-table.tsx`, `__tests__/apps-table.test.tsx`, `messages/en.json`, `messages/fr.json`.
- **What it does:** replaces the hardcoded `aria-label="more actions"` on each table's `more_vert`
  row-action button with `aria-label={t('common.moreActions')}`; adds the `common.moreActions` key
  to `en.json` (`"More actions"`) and `fr.json` (`"Plus d'actions"`); and **translates the top-level
  `common` block in `fr.json`** (`save/edit/delete/confirm` English placeholders → French).

### Assessment

- ✅ **Functionally correct.** All 5 tables call root-scope `useTranslations()` (verified), so
  `t('common.moreActions')` resolves. No scope/key defect.
- ✅ **Cleanly scoped** — admin UI + messages only. **No `apps/auth-server`/seed edits**, unlike the
  #303/#306/#316 lineage. This PR does **not** carry the Critical bug-0250 payload.
- ✅ **Fixes [bug-0244](../bugs/bug-0244.md)** (fr.json `common` action labels were untranslated) —
  in the PR head, not on `master`.
- 🟠 **But it duplicates the still-open PR #316** (see bug-0254): #316 already contains the identical
  5 `aria-label` hunks + the same `en/fr` `common.moreActions` and `fr.json` `common` fix. Both open
  → guaranteed merge conflict. #324 is the *clean subset*; #316 does more (copy-button `copy`/`copied`
  localization) but is blocked by bug-0250/0251.
- 🟢 **Test coverage asymmetry** (see bug-0255): the change lands in 5 tables; only `apps-table.test.tsx`
  gets an assertion.

## New bugs filed

| ID | Severity | Summary | PR |
|----|----------|---------|----|
| [bug-0254](../bugs/bug-0254.md) | 🟠 Medium | #324 duplicates open PR #316's `common.moreActions` a11y-label localization (same 5 tables + message keys) → merge conflict; recurring Jules duplicate pattern | [#325](https://github.com/rlondner/sassy-auth/pull/325) |
| [bug-0255](../bugs/bug-0255.md) | 🟢 Low | #324 localizes the label in 5 tables but tests only 1 (apps-table); 4 sibling suites unverified | [#326](https://github.com/rlondner/sassy-auth/pull/326) |

This review bundle → **[PR #327](https://github.com/rlondner/sassy-auth/pull/327)** (`daily-review-2026-08-15`).

Full catalog: [../bugs/BUGS_2026-08-15.md](../bugs/BUGS_2026-08-15.md). Follow-ups:
[../todo/TODO_2026-08-15.md](../todo/TODO_2026-08-15.md).

## Prior findings — status this run (all in unmerged PRs; `master` unaffected)

- 🔴 [bug-0250](../bugs/bug-0250.md) (seed 2FA-prompt suppression) — persists in #316/#315; **not**
  in #324.
- 🟡 [bug-0251](../bugs/bug-0251.md) (#316 auth/seed scope creep) — stands.
- 🟡 [bug-0253](../bugs/bug-0253.md) (native-`title` copy confirmation) — stands in #316; #324
  doesn't touch the copy button.
- 🔴 [bug-0243](../bugs/bug-0243.md) (`apps/admin` Jest suite red on `master`; fix #287 unmerged) —
  carried forward; CI not gating.

## README

Reviewed `README.md` (808 lines) — it already documents prerequisites, the 6-step Getting Started
flow, RSA key generation, the full required/optional environment-variable matrix, and both apps'
dev ports (auth-server :3000, admin :3001). No configuration changed in the window (PR #324 is
admin-UI-only), so no README edit was needed; it remains accurate for running the app on a dev
machine.

## Bottom line

Healthy, correct, cleanly-scoped PR — the first Jules PR in this lineage that *doesn't* smuggle an
auth-server change. Its only real problems are pre-merge hygiene: it re-does work already sitting in
the blocked #316 (bug-0254) and under-tests its own change (bug-0255). The systemic issue is
unchanged: **31 days without a merge** means Jules keeps re-generating overlapping PRs. Triage +
merge the safe ones (#324 first) is the highest-leverage action.
