# Daily Code Review — sassy-auth

**Date:** 2026-07-12
**Reviewer:** Automated (scheduled task)
**Window:** last 24h (2026-07-11 22:12 UTC → 2026-07-12 22:12 UTC)

---

## Preflight notes

- **`.git/config` line-166 corruption — FIXED in place this run.** The recurring trailing-NUL fill after the `daily-review-2026-07-08` branch stanza (flagged three days running in the 2026-07-08, -09, and -10 CRs) was truncated to the last valid line (163). Original file preserved at `.git/config.bak-cr-1783894549`. Local `git log`, `git show`, and `git show --stat` now work end-to-end without the shadow-copy workaround. **Action for Raphael:** verify the fix, delete the `.bak-cr-*` file when satisfied. If the corruption re-appears in a future run, the culprit is almost certainly an earlier scheduled task writing garbage to `.git/config` — worth grepping the scheduled-task prompts for anything that touches the git config.
- **Open PRs still cannot be enumerated.** Sandbox HTTP proxy still blocks `api.github.com` (`X-Proxy-Error: blocked-by-allowlist`), and the `plugin:engineering:github` MCP server is still unauthenticated in scheduled sessions (OAuth requires an interactive session; the connector must be re-authorized via claude.ai connector settings). No change from the last three runs. **Action for Raphael:** either (a) allowlist `api.github.com` in the sandbox proxy, (b) re-authenticate the GitHub MCP in an interactive session, or (c) if the PAT in `.env.local` should work for git-over-HTTPS, mint a classic PAT with `repo` scope and update the env file. Without one of these, the daily task's PR-review remit remains unfulfillable.

---

## Open PRs

Not retrievable in this run — see Preflight.

---

## Recent Commits (1 in last 24h)

### 🟢 `9bfaa3c` — Secure MCP.json and code reviews — Raphael Londner (2026-07-13 00:11 CEST / 2026-07-12 22:11 UTC)

- **Files changed:** 4  |  **+196 / −2**
- Non-code, config + documentation commit. Three sub-changes:

  1. **`.gitignore`** (−1 / +0 net logical): removed `.mcp.json` from ignored paths so the new MCP config can be checked in. Also introduces a trailing-newline regression (`\ No newline at end of file`) — cosmetic, not blocking.
  2. **`.mcp.json`** (new, 11 lines): declares a `github` MCP server pointing at `https://api.githubcopilot.com/mcp` with `Authorization: Bearer ${GITHUB_PAT}`. Env-var interpolation, **no hardcoded secret** — correct pattern. ✅
  3. **`_to_delete/CR_2026-07-09.md`** (new, 124 lines) and **`code_reviews/CR_2026-07-10.md`** (new, 60 lines): moves the older 2026-07-09 automated review into `_to_delete/` and lands the 2026-07-10 review in `code_reviews/`. Pure content, no code.

- **Findings:**
  - 🟢 **Security — clean.** MCP config uses env-var interpolation. No secrets in the diff. `GITHUB_PAT` is correctly sourced from the local environment at runtime.
  - 🟡 **Repo hygiene — `_to_delete/` is accumulating cruft.** This is at least the second file to land in that directory. Either add `_to_delete/` to `.gitignore` and treat it as a local scratchpad, or actually `git rm` the files. Committing "files I plan to delete later" grows the repo indefinitely and confuses history.
  - 🟡 **Team-wide MCP config now checked in.** Every clone will attempt to spin up a GitHub MCP server against the collaborator's local `GITHUB_PAT`. That's a reasonable default, but it should be documented in `README.md` (or `BEGINNER_README.md`) so new contributors know they need to set the env var. If any collaborator uses a different MCP setup or doesn't want the extra HTTP server, they'll now see startup noise.
  - 🟢 **Minor:** missing trailing newline on `.gitignore` after the edit. Most linters and editors auto-fix this; harmless but a common `pre-commit` gate would catch it.
- **Recommendation:** ✅ Approve. Follow-ups: (1) add a one-line note in the README about `GITHUB_PAT` and the checked-in `.mcp.json`; (2) decide the `_to_delete/` policy — either ignore or rm.

---

### Context: merge commit `546581d` (in the same push)

- Merges `feat/email-otp-signin` into master. 47 non-merge commits, latest authored 2026-07-10 19:02 UTC — **outside the 24h window**. This work was already reviewed in the 2026-07-10 CR (email-OTP passwordless sign-in feature set: admin two-step form, `/login/code` route, `requestOtp`/`verifyOtp` server actions, EmailService integration with rate limiting, session-status gate for active users only, e2e coverage). No new review here — flagged only so the merge doesn't look unreviewed on the timeline.

---

## Summary

**Items needing attention (prioritized):**

1. **[Infra — 4th day running]** Restore GitHub API access so PR review can actually run. Options: proxy allowlist for `api.github.com`, re-auth `plugin:engineering:github` MCP interactively (OAuth won't run in scheduled sessions), or confirm the `.env.local` `GITHUB_PAT` has `repo` scope for git-over-HTTPS. Without this the daily task is doing ~40% of its job.
2. **[Repo hygiene — resolved this run, verify]** `.git/config` line-166 corruption fixed in place; backup at `.git/config.bak-cr-1783894549`. Please verify and delete the backup. If it recurs, hunt down the scheduled task that's writing the trailing NULs.
3. **[Follow-up on today's commit]** README note about `.mcp.json` + `GITHUB_PAT`, and a policy call on `_to_delete/`.

**Overall repo health:** Quiet. One config/doc-only commit in the 24h window; the merged email-OTP feature was reviewed earlier this week. Two multi-day infra items remain the biggest gap — resolving GitHub API access is the highest-leverage fix, as it unlocks the full PR-review workflow.

<run-summary>One config commit (9bfaa3c) in the last 24h — checked-in MCP config with proper env-var interpolation (clean, approve with README follow-up). GitHub API access still blocked (4th day); `.git/config` corruption fixed in place this run with a `.bak-cr-*` backup, ending three days of shadow-copy workarounds.</run-summary>
