# 🔍 Daily Code Review — sassy-auth

**Date:** 2026-06-08
**Window:** 2026-06-07T12:45Z → 2026-06-08T12:45Z (last 24h)
**Reviewer:** Automated daily code review

---

## ⚠️ Data-source note

The GitHub REST API (`api.github.com`) is **blocked by the workspace proxy allowlist** in this environment — `curl` returns `403 blocked-by-allowlist`. `git fetch origin` also fails (no credential helper for the HTTPS remote inside the sandbox). As a result, this run reviews only **local git history**; the **open-PR list could not be retrieved**.

To unblock automated PR review, either (a) configure a `GITHUB_TOKEN`-backed credential helper for the local git remote so `git fetch` succeeds, or (b) allowlist `api.github.com` for outbound HTTPS in the workspace proxy.

---

## Open PRs

**Could not retrieve** — `api.github.com` blocked. Yesterday's CR (`CR_2026-06-07.md`) noted **82 open PRs, 0 merged**; no signal of change since.

The local clone shows the following bug-fix / feature branches still present (subset, ~73+ total bug branches indexed):

- `fix/bug-0001-rbac-org-isolation` through `fix/bug-0041-pkce-verifier-validation` (40+ branches)
- `fix/bug-0016/0017/0018/0019/0020/0022` are checked out as locked agent worktrees under `.claude/worktrees/`
- `docs/pkce-resource-server-design` (worktree at `.worktrees/pkce-rs`, marked `prunable`)
- `feat/admin-e2e`, `feat/apps-admin-ui`, `feature/platform-admin-seed`, `feature/swagger-openapi-docs`

Yesterday's report flagged that **all `bug-0001…bug-0073` branches remain empty** (no implementation commits). Without API access this cannot be re-verified for net-new branches today.

---

## Recent Commits (1 in last 24h)

### `8a81122` `docs: daily code review 2026-06-07 — changelog, bugs, TODO` — Raphael Londner (2026-06-07 02:47 EDT)

**Files:** 5 changed, +510 / -0
- `.env.example` (+7) — adds `LOGIN_NEXT_ALLOWED_ORIGINS`, `SEED_DEMO`
- `CHANGELOG.md` (+39) — appends 2026-06-07 "Unreleased" section listing 16 new bugs
- `README.md` (+6) — documents bug-0074 (inactive-user auth bypass) and bug-0080 (no rate limiting)
- `bugs/BUGS_2026-06-07.md` (+361, new file) — bug-0074 through bug-0089
- `todo/TODO_2026-06-07.md` (+97, new file)

**Findings:** 🟢 Clean

Docs-only commit; no executable changes. The newly catalogued issues (bug-0074 inactive-user JWT bypass, bug-0076/0077 RBAC cross-tenant gaps, bug-0080 no auth rate limiting, bug-0082 unverified `client_secret`) are correctly documented but **none are fixed in this commit**. The `.env.example` additions match referenced features (`LOGIN_NEXT_ALLOWED_ORIGINS` for next= redirect validation per bug-0037 / 4da3fa1; `SEED_DEMO` for resource-server demo seed per bf7673f) — values are blank by default, no secret leakage.

**Recommendation:** Approve (docs only).

---

## Summary

**Items needing attention (prioritized):**

1. 🔴 **bug-0074 (inactive-user JWT bypass)** — newly catalogued critical, still unfixed. `saUser.status !== 'active'` not checked in either `oauthAuthorize` or `directLogin`. Add guard in `apps/auth-server/src/token/token.controller.ts` after lines 89 and 226.
2. 🔴 **bug-0001, bug-0038, bug-0039, bug-0054** — pre-existing criticals, branches exist but remain empty. RBAC org-isolation, JWT scope breaking change, in-memory OAuth codes, redirect_uri bypass.
3. 🟡 **89 open bugs total / 82 open PRs / 0 merged in 12 days.** Backlog growth rate has decoupled from merge throughput — schedule a triage to close stale PRs and start landing bug-fix branches.
4. 🟡 **Workspace proxy blocks `api.github.com`** — automated daily review cannot inspect PRs until this is fixed (see Data-source note).

**Repo health:** 🟡 Documentation discipline strong; merge throughput at zero — bug backlog compounding while no fixes ship. No code regressions in the last 24h (commit was docs-only).
