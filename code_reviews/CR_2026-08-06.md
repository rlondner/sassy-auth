# 🔍 Daily Code Review — sassy-auth
**Date:** 2026-08-06
**Status:** ⚠️ Review NOT executed — GitHub access unavailable in this run

> Regression from 2026-08-05: yesterday's run had the `github` MCP connector authenticated
> and pulled PR #306 live. Today the connector is not loaded in this session, and every
> fallback path is blocked (details below). Recent-commit analysis was done from the local
> clone only.

## Access — what was tried and what happened

| Path | Result |
|---|---|
| `github` MCP connector (yesterday's working path) | ❌ Not available — `plugin:engineering:github` not loaded; `ToolSearch` for `github` / `pull_request` / `list_pull_requests` returned no matches |
| `curl -H "Authorization: Bearer $GITHUB_PAT" https://api.github.com/...` | ❌ Sandbox HTTP proxy (`localhost:3128`) returns `403` on CONNECT for `api.github.com`, `raw.githubusercontent.com`, `codeload.github.com`, `uploads.github.com` — allowlist blocks every `*.github.com` API/CDN host. Only `github.com` itself is reachable (`200`) |
| `mcp__workspace__web_fetch` on `api.github.com` | ❌ Returns empty body; no way to attach the PAT anyway, so private-repo endpoints would 404 |
| `git ls-remote https://<PAT>@github.com/rlondner/sassy-auth.git` | ❌ `remote: Invalid username or token. Password authentication is not supported for Git operations.` — tried `x-access-token:PAT`, `rlondner:PAT`, `PAT:x-oauth-basic`, and `http.extraHeader: Authorization: Bearer …`; all rejected. PAT prefix in `.env.local` is `github_pat_11AD4S7M…` (fine-grained format, 92 chars) → **likely expired or scoped without repo access**; needs rotation regardless of the proxy issue |
| `GITHUB_PAT` as OS env in the sandbox shell | ❌ Not present in shell env; only readable from `.env.local` on disk |

## Scope of the window

- **Last-24h window:** 2026-08-05 08:10 UTC → 2026-08-06 08:10 UTC.
- **`master`:** still frozen at `a78d4a7` (2026-07-15, the 2FA merge). **22 days** with no
  merge to `master`.
- **Commits on any branch in the window:** exactly **1**, on `daily-review-2026-08-05`:
  - `6e0aaf4` — *docs(review): 2026-08-05 daily code review — PR #306 reviewed; bugs 0247,
    0248 filed* — Raphael Londner, 2026-08-05 09:56 UTC.
    - 7 files, +410/-0, **100% Markdown**: `CHANGELOG.md`, `README.md`,
      `bugs/BUGS_2026-08-05.md`, `bugs/bug-0247.md`, `bugs/bug-0248.md`,
      `code_reviews/CR_2026-08-05.md`, `todo/TODO_2026-08-05.md`.
    - No source, config, schema, or workflow changes — nothing to review for
      security/perf/correctness.

## Open PRs

**Not fetched** (see Access table). Last known state from `CR_2026-08-05.md`:

- **#306** — Jules · "🎨 Palette: Interactive Show/Hide Password Toggle for LoginForm" —
  🔴 blocked pending bug-0247 (`SEEDING=1` unfenced session-gate bypass in
  `auth/auth.config.ts`). Do **not** merge until fenced by `NODE_ENV`.
- **#307** — `bugs/bug-0247` (report), opened off `master`.
- **#308** — `bugs/bug-0248` (report), opened off `master`.
- **#303, #294, #296** — earlier palette/toggle PRs from the triplicate cluster (bug-0248).

Whether these are still open, whether #306 has been amended, and whether any new PRs
arrived overnight — **unknown this run**.

## Findings

- 🟢 **Commits:** nothing to review — the one commit in the window is docs-only.
- 🔴 **Automation health:** the daily review pipeline has regressed from yesterday's
  working state. Two independent blockers must both be fixed or the report will keep
  degrading:
  1. `plugin:engineering:github` needs to be pre-loaded (or auto-loaded) in the scheduled
     task's session, matching the 2026-08-05 configuration.
  2. The `GITHUB_PAT` in `.env.local` is being rejected by GitHub for git auth. Rotate it
     (fine-grained token → `Contents: read`, `Pull requests: read`, `Metadata: read` on
     `rlondner/sassy-auth`) and re-test with `curl -H "Authorization: Bearer $PAT"
     https://api.github.com/user`. The current token's `github_pat_11AD4S7M…` prefix
     matches the file that has been present since at least the 07-27 → 08-04 blocked runs,
     so this is a persistent PAT problem, not a transient proxy issue.
  3. Proxy allowlist blocks `api.github.com`. Even with a fresh PAT, `curl` from the
     scheduled-task sandbox will fail until `api.github.com` (and ideally
     `raw.githubusercontent.com`) are allowlisted, **or** the review is standardized on the
     MCP connector path.

## Summary

- **Items needing attention (prioritized):**
  1. 🔴 Restore GitHub access for the scheduled task (MCP connector auto-load + rotate PAT
     + proxy allowlist). Without this, no PR review runs.
  2. 🔴 Merge-starvation on `master` (22 days) — carried forward from
     `todo/TODO_2026-08-05.md`. Still the highest-impact process risk.
  3. 🟡 Confirm PR #306 hasn't been merged with bug-0247 still present — needs a manual
     check via `gh` / browser until automation is restored.
- **Overall repo health:** unchanged from 2026-08-05 (no code merged); automation
  visibility has regressed and needs an operator fix before the next daily run.
