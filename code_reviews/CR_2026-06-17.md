# 🔍 Daily Code Review — sassy-auth

**Date:** 2026-06-17
**Repo:** rlondner/sassy-auth
**Window:** last 24h + currently open PRs (focus on activity since 2026-06-16 CR)
**Base ref reviewed against:** `origin/master` @ `ae9772f` (no commits on master in 15 days)

> ⚠️ **Data-source note:** the workspace sandbox cannot reach `api.github.com` (proxy allowlist returns `blocked-by-allowlist`), so PR open/closed state could not be verified from the GitHub API. The "open" set was reconstructed from `refs/pull/*/head` via `git ls-remote` over the git transport (which is allowed against `github.com`), filtered to refs whose tip is **not** an ancestor of `master`. Bot iteration PRs typically supersede earlier ones, but only PRs explicitly closed on GitHub will disappear from this list. Treat the carry-over set as a **superset**.

---

## Recent Commits (last 24h on `master`)

**None.** `master` HEAD is still `ae9772f` (`chore(i18n): drop unused users.fields.directPermission* keys`, 2026-06-02, Raphael Londner). **Master has now been quiet for 15 consecutive days.** All ongoing development continues on PR branches that have not been merged — same integration-risk pattern flagged on 2026-06-16, now one day worse.

---

## Open PRs (focus: net-new since 2026-06-16)

### PR #108 — 🎨 Palette: Standardize table row actions with tooltips and localized labels  ✨ NEW

- **Author:** `google-labs-jules[bot]` (co-authored by `rlondner`) · **Head:** `7294691` · **Updated:** 2026-06-17T08:18Z
- **Files:** 17 · **+135 / −38** · **4 commits, 0 behind master**
- **Scope:** Cleaner re-iteration of the PR #104→#106→#107 tooltip series. Touches: `TooltipProvider` in admin root layout; 5 admin tables (`apps`, `orgs`, `permissions`, `roles`, `users`) wrap "more actions" trigger in `<Tooltip>` with `aria-label={t('common.moreActions')}`; 5 corresponding table tests add per-file mocks for `Tooltip*`; new **global** mock for `@sassy-auth/ui` Tooltip components in `jest.setup.ts`; `en.json` + `fr.json` add `common.moreActions`; `fr.json` also **backfills** `save`/`edit`/`delete`/`confirm` (previously English placeholders); `e2e.yml` drops `version: 9` from `pnpm/action-setup` and adds `pnpm exec turbo build --filter=!@sassy-auth/auth-server`; `auth-server/package.json` seed switched to `ts-node --transpile-only`; new `.Jules/palette.md` agent journal entry.

- **Findings:**
  - 🟢 **Resolves yesterday's 🔴 type-safety regression.** PR #108 does **not** touch `apps/auth-server/src/*` and does **not** introduce the `: any` casts that #107 added to `check-permission.ts`, `permissions.service.ts`, `roles.service.ts`, `users.service.ts`, `invitations.service.ts`, and `seed.ts`. This is the cleaner branch — merge this, not #107.
  - 🟢 **Closes yesterday's 🟡 i18n FR gap.** `fr.json` `common` block now reads `"save":"Enregistrer"`, `"edit":"Modifier"`, `"delete":"Supprimer"`, `"confirm":"Confirmer"`, `"moreActions":"Plus d'actions"`. The "enterprise-ready FR locale" claim is materially closer to true.
  - 🟢 **Stealth a11y fix in `UsersTable`** — the row-action `<button>` previously had **no** `aria-label` at all (other tables had `"more actions"` literal). It now gets `aria-label={t('common.moreActions')}`, localized. Worth a dedicated changelog line; the PR description does not call this out.
  - 🟢 **Composition is correct.** `<Tooltip><TooltipTrigger asChild><DropdownMenuTrigger asChild><button>…</button>` correctly composes Radix's two `asChild` Slot wrappers into a single rendered child (the `<button>`). Verified pattern from #107 carries forward without regression.
  - 🟡 **`ts-node --transpile-only` on `seed` carries over (now repeat finding).** `apps/auth-server/package.json` flips seed to `--transpile-only`, which silently disables type-checking in the CI seed step before e2e. Same concern as yesterday on #107. **Action:** add a separate `seed:typecheck` script (`tsc --noEmit -p apps/auth-server/tsconfig.json`) and gate it in CI before `seed`. Don't let the CI shortcut become permanent.
  - 🟡 **`turbo build --filter=!@sassy-auth/auth-server` masks the real problem.** Excluding `@sassy-auth/auth-server` from the pre-e2e build means CI never catches auth-server type errors — only the `: any`-laden PR #107 makes that surface visible. This filter is a pragmatic short-term fix but it must be paired with a tracked ticket to (i) restore Prisma typegen ordering in turbo pipeline / tsconfig project refs, (ii) drop the filter. Otherwise auth-server type drift accumulates silently.
  - 🟡 **Global `@sassy-auth/ui` mock in `jest.setup.ts` ≠ per-file overrides.** The new global mock uses `jest.requireActual('@sassy-auth/ui')` and spreads it, then overrides `Tooltip*`. Each of the 5 per-table test files **also** mocks `@sassy-auth/ui` with a hand-rolled object that does NOT spread `requireActual` — those local mocks fully replace the global one. The in-file comment ("ensure you also update any file-level mocks that might override this one") acknowledges the fragility but doesn't fix it. **Action:** factor a shared `__mocks__/sassy-auth-ui.ts` (or a helper that returns the override object) so the table tests and the global path stay in sync. As-is, adding a future `@sassy-auth/ui` component will silently fail in 5 of the table tests.
  - 🟡 **Drawer tests not covered.** Same observation made yesterday on PR #106: `access-denied-panel.test.tsx`, `app-create/edit/view-drawer.test.tsx`, `org-create/edit/view-drawer.test.tsx`, `user-view-drawer.test.tsx` — none of these add Tooltip mocks. The global `jest.setup.ts` mock catches them today, but only because those test files **don't** override `@sassy-auth/ui`. The moment any drawer test file adds its own `jest.mock('@sassy-auth/ui', …)` for an unrelated reason, the safety net disappears. Port these tests onto the shared mock pattern proactively.
  - 🟡 **Functional duplicate of PR #107 (and the predecessors #104, #106).** Same scope, cleaner execution. Three "Palette tooltip + CI" PRs are concurrently open; pick exactly one — PR #108 — and close the rest.
  - 🟢 `.Jules/palette.md` dates are now `2026-05-30` (in-period, not 2025 like #107). Still pre-dated by ~2.5 weeks; harmless but worth flagging that agent-authored journal entries should use the PR creation date so they line up with commit archaeology.

- **Recommendation:** **Approve / Merge** — this is the cleanest of the iteration set. Close #104, #106, and **#107** in favor of #108. Land #108 and immediately file two follow-ups: (a) restore auth-server typegen ordering and drop the `--filter=!@sassy-auth/auth-server` workaround; (b) shared `@sassy-auth/ui` test mock to eliminate the per-file/global divergence.

---

### Carry-over from 2026-06-16 CR (status unchanged — no new pushes since yesterday)

All head SHAs below are byte-identical to those captured in `CR_2026-06-16.md`; no bot iteration on these PRs in the last 24h.

| PR | Title | Head | Status today | Action |
|---|---|---|---|---|
| #107 | 🎨 Palette: Add tooltips to row actions and fix CI robustness | `3702b99` | Still open, **superseded by #108** | **Close** |
| #106 | 🎨 Palette: Standardize row action tooltips and CI robustness fix | `c2f403c` | Still open, **superseded by #108** | **Close** |
| #105 | 🎨 Palette: Standardize and enhance loading states | `b45682a` | Still open | **Land** as the consolidated Button-loading PR (per yesterday's recommendation) |
| #104 | 🎨 Palette: Improve 'more actions' accessibility and fix CI | `3fe8485` | Still open, **superseded by #108** | **Close** |
| #103 | feat(ui): standardized loading states and fix CI e2e workflow | `3557ba2` | Still open, **superseded by #105** | **Close** |
| #102 | 🎨 Palette: Standardize loading states and fix CI build scope | `93e7b1a` | Still open, **superseded by #105** | **Close** |
| #101 | feat(ui): unified loading state and CI build order | `4ce76fd` | Still open, **superseded by #105** | **Close** |
| #100 | feat(ui): add loading prop and fix CI build | `bfbd44f` | Still open, original site of the `: any` regression | **Close** (fix re-rooted in #108 / #105) |
| #99  | 🎨 Palette: Standardized Button loading state and CI fixes | `e1c1065` | Still open, **superseded by #105** | **Close** |
| #43  | feat(auth): OAuth2 PKCE + FastAPI resource server + observability | (per prior CR) | **Still unmerged — 16 days now** | 🔴 **Resolve & merge — highest-value, security-critical, only non-bot PR in flight** |
| #42, #44, #57, #62 | Older button-loading iterations | — | Likely still open | **Close** in batch in favor of #105 |
| #41 | docs: daily code review backlog | — | Likely still open | Approve / rebase + split docs |

### Stale bot-wave PRs (#72–#98)

`git ls-remote` shows PR refs **#72 through #98** all share the **same head SHA `5337b2a`** (`docs(plan): sidebar active-route highlight implementation` by Raphael, 2026-06-02). That's 27 PR numbers pointing to a single 2-week-old commit — almost certainly auto-generated by a tool that re-creates the same branch as a new PR each run. Most are likely already closed on GitHub (cannot verify without API), but if any are still open they should be closed in bulk. **Action:** when API access is restored, run a one-shot cleanup: close all PRs in `[72..98]` whose `head.sha == 5337b2af1168b09b5d8ad3c4e7c38d4a07abd52a` and that are not the canonical `feat/admin-e2e` branch PR.

---

## Summary — Items needing attention (prioritized)

1. 🟢 **Yesterday's 🔴 type-safety regression is resolved on the merge candidate.** PR #108 cleanly avoids the `: any` casts that blocked #107. Land #108, close #107.
2. 🔴 **PR #43 (OAuth2 PKCE + observability) is now 16 days unmerged.** This was the only security-relevant PR in flight and was reviewed clean. It is rotting in the queue while the bot churns out duplicate UI PRs. **Drain this today.**
3. 🟡 **`ts-node --transpile-only` on `seed` survives into #108.** Add a separate `tsc --noEmit` typecheck gate in CI before `seed`. (Repeat from 2026-06-16.)
4. 🟡 **`turbo build --filter=!@sassy-auth/auth-server` masks auth-server type errors.** Pragmatic, but file a follow-up to restore Prisma typegen ordering and drop the filter. The longer this stays, the more auth-server drift accumulates undetected.
5. 🟡 **PR backlog hygiene is degrading.** ~10 active open PRs in the bot-iteration set (#99–#108) plus the 27-PR stale wave (#72–#98) plus the pre-existing button-loading set (#42, #44, #57, #62). Net-new PR creation (`#108`) is outpacing closures. **Recommend:** freeze bot PR generation for 48h, drain to {#43 (PKCE), #105 (Button-loading consolidated), #108 (Tooltips consolidated)}, then resume.
6. 🟡 **Master inactive 15 consecutive days.** One day worse than yesterday. Long-running PR branches diverge from base, increasing merge risk. Establish an integration cadence (e.g., 2x/week merge window).
7. 🟡 **Test mock fragility (new).** Global `@sassy-auth/ui` mock added in `jest.setup.ts` does not compose with the 5 per-file table mocks. Refactor to a shared mock module before this pattern propagates to the drawer tests.

**Overall repo health:** ⚠️ → 🟡 **Marginally improved on code quality, materially worse on flow.** The merge-candidate quality improved (PR #108 clears yesterday's blockers) and FR i18n was completed, but the queue continues to grow without master moving. The fact that the only security-relevant PR (#43) has been frozen for 16 days while 4 functionally-identical tooltip PRs (#104, #106, #107, #108) accumulated is a process problem more than a code problem. Recommend: drain {#43, #105, #108} today, freeze bot PR generation for 48h, then audit and close the #72–#98 wave.
