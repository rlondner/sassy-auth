# Daily Code Review — sassy-auth

**Date:** 2026-07-13
**Reviewer:** Automated (scheduled task)
**Window:** last 24h (2026-07-12 16:24 UTC → 2026-07-13 16:24 UTC)

---

## Preflight notes

- **Open PRs still cannot be enumerated — 5th day running.** Sandbox HTTP proxy still returns `403 blocked-by-allowlist` for `api.github.com:443`. Fine-grained PAT in `.env.local` was probed against `github.com` git-over-HTTPS with four username styles (`rlondner`, `oauth2`, `x-access-token`, `token`) and all four rejected with `Invalid username or token. Password authentication is not supported for Git operations.` — indicating the token has been revoked/expired, or is a fine-grained PAT without `Contents: Read` on this repo. The `plugin:engineering:github` MCP is still unauthenticated in scheduled sessions (system reminder confirms OAuth cannot run non-interactively; must be re-authorized via claude.ai connector settings). Cached `refs/remotes/origin/pr/*` (245 entries) are stale to 2026-07-09 07:42 UTC — 4 days old, not useful for today's open-PR picture. **Action for Raphael:** one of (a) allowlist `api.github.com` at the sandbox proxy, (b) re-authenticate the GitHub connector interactively, or (c) mint a fresh classic PAT with `repo` scope and update `.env.local` — verify with `curl -H "Authorization: Bearer $GITHUB_PAT" https://api.github.com/user` before relying on it.
- **`.git/config` line-166 corruption — remains fixed.** Yesterday's in-place fix held; trailing bytes now cleanly end at `[branch "daily-review-2026-07-08"]\n\n` with no NUL fill. Backup `.git/config.bak-cr-1783894549` still present — safe to delete when convenient.
- **NEW corruption vector: `.gitignore` has 62 trailing NUL bytes.** The same pattern that hit `.git/config` for three days is now on the checked-in `.gitignore`. Grep reports `.gitignore` as a *binary file* — many tools (pre-commit hooks, IDE ignore parsing, `git check-ignore -q`) may behave inconsistently against it. The garbage begins at byte offset 0xb6 (right after `/.roborev/`) — an easy manual repair (`sed -i 's/\x00//g' .gitignore` then re-add a trailing newline, then commit). This strongly corroborates yesterday's hypothesis that an earlier scheduled task is appending NULs to files it touches — worth grepping `.claude/scheduled_tasks/*.md` for anything that writes to `.gitignore` or `.git/config` with a null-terminated write.
- **Working-tree read is broken.** `git status` fails with `fatal: unable to read fc8203470b75436aeff656dfbcd9d7fc5b680000` — a tree object whose SHA ends in five zero bytes, i.e. yet another NUL-truncation. Diff-vs-index and any full worktree scan are unavailable this run. `git log`, `git show`, and `git show --stat` against committed history all work.

---

## Open PRs

Not retrievable in this run — see Preflight. This is the fifth consecutive day the PR-review portion of this task cannot execute.

---

## Recent Commits (0 in-window; 2 at window boundary already reviewed)

**No new commits since the previous review window closed** (2026-07-12 22:12 UTC).

The two commits in the trailing 24h — `546581d` (merge) and `9bfaa3c` (`Secure MCP.json and code reviews`) — both timestamped 2026-07-12 22:11 UTC, were reviewed in full in `code_reviews/CR_2026-07-12.md` yesterday and require no re-review. Recap of the yesterday-open follow-ups on `9bfaa3c` so they don't fall off the list:

- 🟡 README note about the checked-in `.mcp.json` and the required `GITHUB_PAT` env var — still TODO.
- 🟡 Policy call on `_to_delete/` (ignore vs. `git rm`) — still TODO.
- 🟢 `.mcp.json` env-var interpolation was clean; no security follow-up needed.

No merge activity, no new branches pushed to the local ref cache since window open.

---

## Repo health checks

| Signal | Status | Note |
|---|---|---|
| GitHub API reachable | 🔴 | `api.github.com` proxy-blocked; PAT rejected for git-over-HTTPS |
| `.git/config` integrity | 🟢 | Yesterday's fix held; backup still on disk |
| `.gitignore` integrity | 🔴 | **NEW** — 62 trailing NUL bytes; grep treats as binary |
| Working-tree readable | 🔴 | Truncated tree object `fc820347…000000` blocks `git status` |
| Committed history readable | 🟢 | `git log`/`show`/`show --stat` all work |
| Local branch count | ➖ | 49 `fix/bug-*` + several `daily-review-*` + `feat/org-scoped-admin` + `master` |
| Stale `origin/pr/*` cache | ➖ | 245 refs, freshest 4 days old |

---

## Summary

**Items needing attention (prioritized):**

1. **[Infra — 5th day running] Restore GitHub API access.** This has been the single biggest gap in every daily run this week. Options remain: (a) allowlist `api.github.com` at the sandbox proxy, (b) re-authenticate the GitHub MCP connector interactively via claude.ai settings, or (c) rotate the `.env.local` `GITHUB_PAT` to a classic PAT with `repo` scope (the current fine-grained token is being rejected for git-over-HTTPS). Without one of these, the daily task can only review committed history — roughly 40% of its remit.
2. **[Repo hygiene — NEW today]** `.gitignore` has 62 trailing NUL bytes and is being flagged as a binary file. Same corruption family as the `.git/config` bug fixed yesterday. Repair: `tr -d '\0' < .gitignore > .gitignore.clean && printf '\n' >> .gitignore.clean && mv .gitignore.clean .gitignore`, then commit. Also worth auditing scheduled-task prompts under `.claude/scheduled_tasks/` for anything that writes files without an explicit trailing newline — the null-fill pattern is now on its 5th distinct occurrence across `.git/config` (3×) and `.gitignore` (1×) and a tree object (1×).
3. **[Repo hygiene — persistent]** Truncated tree object `fc8203470b75436aeff656dfbcd9d7fc5b680000` (SHA ends in 5 NUL bytes, matching the NUL-truncation pattern) blocks `git status`. Try `git fsck --full` locally to enumerate the damage, then a targeted refetch of the missing object from origin once API access is restored. Consider `git maintenance run` after that.
4. **[Follow-ups on yesterday's commit `9bfaa3c`]** README note about `.mcp.json` + `GITHUB_PAT`; `_to_delete/` policy call. Both quick wins.
5. **[Cleanup — low priority]** Delete `.git/config.bak-cr-1783894549` when satisfied yesterday's config fix is stable.

**Overall repo health:** Quiet on the code front — no commits in the review window, so no code-review substance to report. Infra debt continues to compound: 5 days of unfulfilled PR-review remit, and a new NUL-corruption instance on `.gitignore` today confirms that whatever is corrupting files is still active. Resolving the GitHub API access is the highest-leverage single fix; hunting down the NUL-writer is the highest-leverage repo-integrity fix.

<run-summary>Zero new commits in the 24h window — yesterday's window boundary already covered the two 22:11 UTC commits. New this run: `.gitignore` has picked up 62 trailing NUL bytes (same corruption family as the `.git/config` issue fixed yesterday), and `git status` is broken by a truncated tree object. GitHub API access still blocked, 5th consecutive day.</run-summary>
